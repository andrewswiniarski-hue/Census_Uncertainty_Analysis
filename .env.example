# Copy this file to `.env` (same folder) and paste in your own key.
# Get a free key (instant, via email): https://api.census.gov/data/key_signup.html
# `.env` is gitignored — never commit a real key.
CENSUS_API_KEY=your_key_goes_here
