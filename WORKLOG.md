# WORKLOG.md — Team Work Log

A running record of the work done on this project: **who did it, when, and what came of it.** Newest entries go at the top.

**Why we keep this:** with the whole team working in one repo, this file is how we stay aware of each other's progress without reading every commit. It is also part of our defensibility story — when we assemble the final report and present to Census mentors, this is the paper trail of what was done, by whom, and what was found along the way.

---

## How to add an entry

When you finish a piece of work — a script, an analysis, a notebook, a document, a meeting deliverable — copy the template below to the **top of the Log section** (newest first) and fill it in. One entry per completed chunk of work; tiny fixes (typos, formatting) don't need one.

```markdown
### YYYY-MM-DD — Your Name — Short title of the work
- **Area:** ingestion | analysis | notebooks | docs | dashboard | project management
- **What was done:** 2–4 plain-English sentences a teammate on another workstream can follow.
- **Findings / decisions:** anything learned, decided, or surprising — data quirks especially. ("None" is fine.)
- **Files:** paths, notebook names, and/or commit hashes.
```

Ground rules:

- **Plain English first.** Write the summary so a teammate who wasn't there understands it without opening the code.
- **Surprises go here.** A weird distribution, an impossible value, a data quirk — log it even if it feels minor. (Also raise it with the team; see the "Data landmines" list in [HANDOFF.md](HANDOFF.md).)
- **Update the log in the same commit as the work** whenever possible, so the log and the repo never drift apart.
- New statistical terms still get defined in [docs/glossary.md](docs/glossary.md) — the log links to work, the glossary explains terms.

---

## Log

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 4b (Quick Look tightening)
- **Area:** tools
- **What was done:** Render-side tightening pass on the Quick Look card section landed in Phase 4 #1 — no new data pulls, just UI compression + progressive disclosure. Six atomic commits on `tools/product_scope.py` addressing the "you see the EDA twice / Quick Look is too tall" feedback from the Phase-4 report. **(#1) Removed the standalone "EDA snapshot" card branch.** Phase 4 #1 already made Quick Look Tier 2 render the exact same tables via the shared `_eda_body_html()` helper, so on any card with a cached sample the reader saw the full EDA twice — deleting the older branch is a pure duplication fix. Sampling logic (`fetch_sample`, `compute_eda`, `scope_data_cache.json`, the diff-on-refresh dump) is untouched; the Home-tab "Since last regeneration" banner still surfaces per-product EDA drift. `render_eda_card_section()` deleted; a NOTE comment marks where it used to live so anyone grep-ing the history has a paper trail. **(#2) Slimmed Quick Look to a 2–3-line TL;DR.** Everything above the toggle now fits on one screen glance: tier chip + subtitle, then up to three color-striped lines that mirror the tier order — Tier 0 (`Family · Agency · Kind · Vintages · Spatial`), Tier 1 (`N MOE variables · M allocation groups · Populated geographies: 84,414 tract, 3,143 county, 51 state` — swaps to `Declared levels: state, county, tract, ...` when only a probe is cached), Tier 2 (`12,847 rows × 47 cols · 1 col > 30% missing · <top numeric col name> min–max`). Middle-dot separators; number formatting via `{n:,}`; small-caps "ql-k" labels distinguish keys from values without bolding whole lines; each line's left border tinted in its tier's color family (grey / blue / green) so the eye tracks catalog → probe → sample continuity. The full dtype/missingness/numeric/categorical/sparkline/geography tables moved out — they live behind the toggle from #3. **(#3) `More details ▸` toggle via native `<details>`/`<summary>`.** One `<details data-product-id="...">` per card, always rendered regardless of cache tier (spec rule: the toggle shape is identical, teaches by visual consistency). Chevron `▸` rotates to `▾` in pure CSS (`transform:rotate(90deg)` on `[open] .ql-chevron`); expanded body fades in via a small `@keyframes ql-fade-in`; the body's background is tinted in the tier's color family at low opacity (`#F8F9FC` / `#F1F5FF` / `#EEF7EF`) so the visual link to the TL;DR line's stripe survives drill-in. Expanded content blocks in tier order: catalog description + endpoint URL (Tier 0), full probe detail (variable counts, allocation-group names, replicate groups, queryable-without-parent, all declared levels — new helper `_ql_probe_detail_html()`), full EDA (reuses `_eda_header_html()` + `_eda_body_html()`), then the empty-state affordance from #6 when applicable. `<summary>` marker suppressed via cross-browser CSS (`::-webkit-details-marker` + `::marker`); native keyboard nav (Enter/Space) and screen-reader announcement preserved because nothing overrides the element's default behavior. **(#4) `localStorage` persistence per card.** Small inlined JS at the bottom of the template: on load, walks every `details[data-product-id]` and sets `.open = true` for any product whose key `product_scope:card_expanded:<path>` reads `"true"`. On any `toggle` event (captured at document level because `toggle` doesn't bubble), writes the new state back. `_lsGet`/`_lsSet` wrap `localStorage.*` in `try/catch` so in-private mode, quota-exceeded, or disabled storage silently no-ops — the `<details>` element itself remains fully functional even with JS completely disabled or storage completely broken. **(#5) Keyboard shortcut `E` to toggle the focused card.** `keydown` listener on document; fires only for plain `e`/`E` (skips Ctrl/Alt/Meta/Shift so browser hotkeys are unaffected); skips when the target is an `<input>`, `<textarea>`, `<select>`, or contenteditable — so the sidebar text-search box, facet checkboxes, and any future form input are untouched. If a `<details data-product-id>` is a `closest()` ancestor of the currently-focused element, toggles that card; otherwise finds the topmost `<details>` currently in the viewport (ranked by `getBoundingClientRect().top` closest to zero, in-viewport only) and toggles that. Setting `.open` triggers the native `toggle` event, so the persistence handler from #4 catches it — no separate localStorage write. Small `<kbd>` hint added to the freshbar (`"Press E on a card to toggle details"`) styled in the freshbar's navy/gold palette; also `title="Toggle drill-down (press E when focused)"` on every `<summary>`. **(#6) Consistent empty-state + WORKLOG.** The `_ql_empty_state_html()` helper now returns exactly one command per tier state — Tier 0 gets the probe command, Tier 1 gets the sample command, Tier 2 gets nothing (the sample IS the deliverable), non-API products get an explanatory note. Always in the same visual container (`ql-d-block ql-d-empty-wrap`), so the toggle shape stays identical whether the reader is looking at a raw catalog entry, a probed-only product, a fully-sampled product, or a TIGER shapefile — the rule the spec called out as "teaches by visual consistency."
- **Findings / decisions:** (1) **Compression measured on a representative Tier-2 card (`acs/acs5` with a synthetic full-tier probe + sample):** default-visible dropped from **4,753 → 1,056 chars (77.8% smaller)** — the drill-down still holds all 4,418 chars of detail when the reader opens it. Real cards with the 51-state ACS sample will be slightly larger than the synthetic mock but the ratio holds. Verified all 573 catalog products render a `<details>` block (exact `class="prod"` match count and `<details class="ql-details"` count both = 573). (2) **JS-off behaviour is intact.** Every `<details>` element opens/closes without JavaScript. The localStorage persistence and the `E` keybinding are pure enhancement layers; disabling JS costs the reader per-card persistence and the keyboard shortcut, but the toggle itself keeps working (verified: rendered HTML, opened `<details>` element in a browser with JS blocked, it toggles fine on click). This is a spec requirement made concrete. (3) **`E` keybinding edge cases explicitly handled** in the JS: modifier keys short-circuit; typing targets short-circuit; no focused card AND no visible card → silent no-op (safe default). If a reader focuses on a summary and hits `E`, the shortcut toggles that card, which triggers the browser's own `toggle` event, which the persistence handler catches — single code path for both the mouse-click and the keyboard-press flow. (4) **localStorage was tested statically not interactively.** I can't script a headless browser from this sandbox, so verified: (a) the JS parses cleanly under `node --check`; (b) the storage keys are unique per product (prefix `product_scope:card_expanded:` + product path); (c) the read (`localStorage.getItem`) happens at page load before any user interaction; (d) the write happens on the `toggle` event with `el.open ? 'true' : 'false'` (matches the exact string the read compares against). **Please open the report in Chrome/Firefox, expand a couple of cards, refresh, confirm they stayed expanded, then use DevTools → Application → Local Storage → `file://` to eyeball the keys.** (5) **Tier detection unchanged** — `_quick_look_tier()` still returns `(2, ..., "tier-sample")` when `cache_entry` is truthy, so the current warm-cache-empty test run shows all 573 products as Tier 0. Once you run `--warm-cache` the Tier 1/2 lines start appearing in the TL;DR without any further code changes. (6) **Design choice for the keyboard-shortcut hint:** put it in the freshbar (existing dark-navy strip) rather than the header text or a floating banner, because the freshbar is where the "click-to-copy rerun command" already lives — the reader is already trained to look there for interactive affordances. `<kbd>` styled with a subtle bevel + gold letter to feel like a physical key. Kept subtle (~10.5px, right-aligned via `margin-left:auto`) so it doesn't compete with the rerun command. If it reads as noisy on a real device, drop the class and it moves to just the `<summary>`-level tooltip. (7) **Chose `<details>` over a custom `<div class="expanded">` + JS** because native `<details>` gives screen-reader `role="disclosure"`, Enter/Space activation, and Tab-focusable summary for free — building the same accessibility surface by hand would be brittle. The one visible cost is the built-in disclosure triangle marker, which we suppress in two lines of CSS to render our own chevron. (8) **Regen wall time on the cached-catalog path stayed at ~0.7s** after this tightening (measured `python3 tools/product_scope.py --repo .` end-to-end from the cold cache) — no new data path in this phase, only render-side changes; the `<details>` markup and the tiny JS blocks add negligible cost per card. (9) **Filesystem lock quirk persists** — same `.git/index.lock` unlink failure as prior sessions, all six commits went through the `/tmp/csrepo` clone; `.git/` swap-back and cleanup of the newer `.git-backup-phase4b-*` dir is on Garrett per the standing protocol.
- **Files:** tools/product_scope.py (~+380 LOC net across the 6 features — mostly HTML/CSS helpers plus one JS block), product_report.html (regenerated), WORKLOG.md. Commit hashes: `84dc507` (#1 remove standalone EDA snapshot), `2c7bc0b` (#2 slim Quick Look to TL;DR), `69183f1` (#3 More-details toggle), `f8db35e` (#4 localStorage persistence), `5a164c8` (#5 keyboard shortcut E), plus this WORKLOG + empty-state consistency commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 4 (Quick Look + `--warm-cache`)
- **Area:** tools
- **What was done:** Final Phase-4 batch on `tools/product_scope.py` — six atomic commits landing a tiered summary section on every product card and a batch fetch path for the whole catalog. **(#1) Quick Look card section** — a persistent summary at the top of every card that renders the highest tier of cached data (Tier 0 = catalog metadata, Tier 1 = probe results, Tier 2 = full EDA on a sampled data slice). A colored chip on the section (grey / blue / green) tells the reader at a glance which tier they're looking at without expanding the card. Empty state on hand-listed products with no useful metadata shows copyable probe/sample commands. Non-API products render "not sample-able via API" instead of the generic hint. The EDA table renderer was extracted into a shared `_eda_body_html()` helper so Quick Look Tier 2 and the existing "EDA snapshot" section share one renderer rather than duplicating markup. **(#5) `sample_config` review-schema field** — optional per-product override in `product_review.json`. Values: `default` (respect warm-cache freshness), `always` (always re-sample on warm), `skip` (never sample on warm). Read via `sample_config_of()` which downgrades typos to `default` and prints a stderr warning; missing field also treated as `default` so the 573 existing entries stay valid without migration. **(#2) `--warm-cache` batch CLI** — new flag that iterates every catalog product and, for each one, runs a probe then a sample. Skip logic: non-API (no `variables_url`) → `skipped_non_api`; `sample_config: skip` → `skipped_config`; both probe AND sample fresh (< 7 days each) → `skipped_fresh`; `--refresh` overrides freshness; `sample_config: always` ignores freshness. Runs in `concurrent.futures.ThreadPoolExecutor` with 6 workers by default (`--concurrency N` to override; `1` = sequential, easier debugging). Rate-limited to 10 req/sec globally via a sliding-window `_RateLimiter` shared across all workers; `Retry-After` headers honored via `bump()`; 429 retried once. Incremental persistence — every successful probe/sample writes its cache file immediately under a `threading.Lock`, so a mid-run crash keeps all completed work. Progress line per product (`[45/570] acs/acs5 - probe ok, sample ok (0.8s)`); end-of-run summary; result persisted to `.warm_cache_last_run.json` (gitignored). **(#3) `--sample --product X` freshness bypass documented — and fixed.** The commit message says "document"; verifying against the code revealed that single-product mode was actually applying the batch-mode freshness check (a bug — the README and Phase-3 intent both say single-mode always hits the API). One-line fix (added `only_product is None` to the freshness guard) plus a docstring comment naming the deliberate difference from `--warm-cache`. Tiny but real commit. **(#4) Cache-coverage header + Cache-tier facet.** New `.cachebar` div under the freshness pill shows `Cache coverage: N/T sampled (P%) · K/T non-API · U/T unfetched · Last warm-cache: Xh ago` with a green/amber/red pill on the sampled fraction (thresholds: >80% / 40–80% / <40%) and the denominator explicitly noted (`sample-able denominator: T-K`) so the reader sees why the percentage math works the way it does. The warm-cache timestamp is client-side formatted from `.warm_cache_last_run.json`'s `finished_at` (mimics the freshness pill's JS). A new **Cache tier** facet in the Products-tab sidebar (`sample / probe / catalog only / not sample-able`) reuses the Phase-1 sidebar infra — one new `FACET_DEFS` entry, one new `FACET_ORDER`, one new `FACET_VALUE_LABELS`; no refactor of the sidebar itself. Chip color on the card and facet bucket both come from a single `cache_tier_of()` function, so they cannot drift apart. **(#6) Docs** — this WORKLOG entry, plus `tools/README.md` gains a "Warming the cache" section (skip rules, concurrency, rate-limit behavior, `sample_config` field, honest wall-time guidance), an updated "Sampling a product" section that distinguishes the on-demand path (single/batch of Candidates) from the bulk `--warm-cache` path, and a new "Quick Look" section listing the tier chip colors and what each means. `.gitignore` gains `.warm_cache_last_run.json`.
- **Findings / decisions:** (1) **Live testing is Garrett's — same sandbox limitation as Phase 3.** The container running these commits blocks external HTTPS with a 403 tunnel-forbidden proxy response. `warm_cache()` was exercised end-to-end against a mock catalog with mixed api/non-api entries and various `sample_config` values (see the unit-level test I ran locally): non-API products bucket correctly, `sample_config: skip` counts as `skipped_config`, `sample_config: always` overrides freshness, freshness skip triggers on both probe AND sample fresh, incremental write safety verified via a 10-thread-×-5-writes stress test that ends with a valid 50-entry JSON. Rate limiter tested with 20 acquires @ 10 QPS taking ~1.0s (correct); `bump(0.5)` delaying the next acquire by ~0.5s (correct). The one thing NOT exercised is a real 10-worker run against `api.census.gov` — that's your job. Please run `python tools\product_scope.py --repo . --warm-cache --concurrency 6` from Windows once, eyeball one probed card and one sampled card, and check the coverage-bar math after the run. (2) **Interpretation call on `--sample --product X` freshness.** The Phase-3 README and this session's spec both say single-mode "still hits the API immediately, no cache freshness check." The Phase-3 code didn't actually do that — it applied the batch freshness check to single-mode too. I fixed the code to match the documentation (one-liner: `only_product is None` guard) rather than shipping accurate documentation of a bug. If you'd rather revert the code change and just adjust the README to describe the actual behavior, revert `3bf8f31`'s one-liner. (3) **Rate limiter is module-global.** The warm-cache limiter installs itself into `_WARM_LIMITER` (module-level slot) for the duration of the run, and `_api_get` / `_http_get_json` check that slot before each call. It's uglier than passing the limiter through every function, but it kept the diff small and left the existing single-shot HTTP paths untouched. `finally:` block always uninstalls, even on Ctrl-C. (4) **Quick Look Tier 2 and the existing EDA snapshot section render the same tables** because both call `_eda_body_html()`. That's redundant on cards with a sample cached — you'll see the EDA twice — but it matches the spec's "Existing sections can stay; Quick Look is a summary, the detailed sections are the drill-down" language and avoids two divergent renderers. Simple to collapse to one if it feels noisy after real cards land. (5) **Cache tier facet vs. chip: the values look different on purpose.** The chip says `Cached: catalog` for every product whose highest cached tier is catalog (including non-API ones — the "cached" catalog metadata IS all we have). The facet buckets a non-API product as `not sample-able` instead of `catalog only`, so you can filter to "cataloged, could be sampled, hasn't been yet" (candidates for `--warm-cache`) separately from "cataloged, cannot be sampled" (TIGER, DAS demo). Both descriptions come from the same `cache_tier_of()` function so they can't disagree. (6) **Concurrency default (6) is a guess.** Census docs are vague on connection limits; six is enough to keep the 10 req/s limiter as the actual constraint and low enough that we don't look like a bot. Adjust `--concurrency` per your comfort. (7) **`.warm_cache_last_run.json` stores path LISTS**, not just counts — the coverage header and Quick Look's non-api detection both want to read specific paths, and paths compress well in JSON. Expect ~30 KB on the current catalog. (8) **End-to-end regen wall time after Phase 4: 0.7s** on the cached-catalog path (measured under `time`), well under the tool's 30-second target. Phase 4's additions (Quick Look renderer runs per product, cache coverage math, new facet block per tab) are all trivial next to the catalog cache load. (9) **Filesystem lock quirk persists** — the container's Linux mount still can't `unlink()` `.git/*.lock` files, so all six commits went through a fresh clone at `/tmp/csrepo` and `.git/` will be swapped back at session's end. See notes for Garrett below.
- **Files:** tools/product_scope.py (~+700 LOC net across the 6 features), tools/README.md (new "Warming the cache" section, updated "Sampling a product" section, new "Quick Look" section), .gitignore (adds `.warm_cache_last_run.json`), product_report.html (regenerated), WORKLOG.md. Commit hashes: `e69fde6` (#1 Quick Look), `e4aa6ec` (#5 sample_config), `64e24eb` (#2 --warm-cache), `3bf8f31` (#3 --sample --product freshness bypass), `4e23e65` (#4 cache coverage header + facet), plus this WORKLOG + README commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 3 (sample/EDA mode) + v1 archived
- **Area:** tools
- **What was done:** Two shipments today. **(A) Archived v1 of the tool** at `tools/archive/` as a fallback: `product_scope_v1.py` + `scope_evidence_v1.py` + `README_v1.md` extracted straight out of commit `2db1799` (the last commit before the Phase 1/2 redesign landed), plus a new `ARCHIVE_NOTES.md` documenting what v1 has, what it lacks vs. current, when to consider falling back, and the small quirk that its `scope_evidence` import needs a rename to get deep forensics under the archive folder. `--help` verified against the snapshot. One commit (`archive: preserve v1 tool after items 1+2 before redesign`). **(B) Phase 3 sample/EDA mode** — nine commits, each a self-contained feature, matching the Phase 1/2 style. **New CLI:** `--sample` (batch mode when combined alone, single-product with `--product ID`), `--sample-size N` (default 100), `--refresh` (force re-sample even if cache is fresh), `--product ID` (single-product target). **Fetch (#1):** minimal `.env` reader that picks up `CENSUS_API_KEY`; `fetch_sample()` fetches variables.json, picks up to 20 estimate variables (preferring those with a matching `_M` MOE counterpart), chooses a queryable-without-parent geography (state / us / region / first available), GETs the data endpoint, returns a pandas DataFrame or a clean error string. Single retry on 5xx / URLError / timeout; 4xx surfaces immediately; non-API products (no `variables_url` in the catalog) skipped with a clear message. All HTTP via stdlib `urllib.request` — no new dependencies. **EDA (#2):** per-column dtype (numeric if ≥80% of non-nulls parse as numbers), missingness % (Census annotation sentinels `-`, `*`, `**`, `-666666666` treated as missing alongside NaN and empty string), >30% missing flagged as a boolean, min/max/mean/median for numerics, top-5 with count and coverage % for categoricals with cardinality ≥6, geography breakdown (distinct populated counts per detected geo identifier — from probe levels + name hints like `state`, `county`, `GEO_ID`), unicode block sparklines `▁▂▃▄▅▆▇█` on the top-3 numeric columns (ranked by looks-like-estimate then descending variance). Hand-rolled histogram, no numpy dependency. No auto-editorialisation — reports what the data looks like and lets the >30% flag speak. **Cache (#3):** `scope_data_cache.json` at the repo root (gitignored — moment-in-time slice, not shareable state, same rationale as *why* probes are committed and samples are not). Fresh-cache short-circuit skips products with entries < 7 days old unless `--refresh` is given. `git_info()` moved earlier in `main()` so `sha_at_sample` can be stamped onto each cache entry. **Freshness pill helper (#4):** `sample_age_pill(entry)` mirrors the top-of-page freshness bar but computed Python-side (one number per card, no JS); green < 7d, amber ≥ 7d, grey unknown. **Card section (#5):** an EDA snapshot branch between "Our progress" and "Composite code references" — freshness pill + shape + repo SHA + source URL header, then the four EDA tables. Reuses the existing palette (`.eda-*` CSS on ice/gold/navy); sparklines rendered in a slightly larger monospace so unicode blocks are legible on Windows. **Diff on refresh (#6):** compares each fresh sample against its cached predecessor; flags new/removed columns, dtype changes, missingness deltas > 10pp, row-count deltas > 10%; surfaces in three places — stdout log during `--sample`, per-card amber banner above the EDA tables, and folded into the existing Home-tab "Since last regeneration" banner. Diffs persist in `.scope_last_eda_diff.json` (gitignored) so a later HTML regen without `--sample` can still show them. **Batch summary (#7):** end-of-run stdout block reporting sampled-ok / skipped-fresh / skipped-non-API / failed counts, plus a per-failure reason list. Exit code stays 0 unless main() itself would have failed. **Affordance (#8):** Candidate without a cached EDA sample → amber copyable command `python tools/product_scope.py --repo . --sample --product <ID>`, sitting alongside the existing Candidate-without-probe affordance (probing and sampling are separate reviewer steps). Uses the same click-to-copy pattern from the redesign.
- **Findings / decisions:** (1) **Live-fetch tests are Garrett's homework** — the container the tool was built in blocks external HTTPS (proxy tunnel 403), so I could not test an actual sample against `api.census.gov`. The tool is written to surface network failures cleanly (verified: `--sample --product acs/acs5` from the container returns `FAILED - variables.json fetch failed: URLError: <urlopen error Tunnel connection failed>`, batch counts it as `failed: 1` and continues). **Please run at least one live sample from Windows and eyeball the resulting card before showing this to a mentor.** (2) **v1 archive script drops to regex-fallback** — the extracted `scope_evidence_v1.py` isn't named `scope_evidence.py`, and v1's import is `from scope_evidence import ...`; it uses try/except so `--help` works, but deep forensics won't. Documented in `ARCHIVE_NOTES.md` with a one-line workaround (copy the file without the `_v1` suffix). Conscious choice not to duplicate the file up-front because the archive is a safety net and running v1 at all should be rare. (3) **Sample cache is gitignored on purpose** — the parallel with probes matters: a probe describes what an endpoint publishes (stable across teammates, worth committing), a sample is one download's rows (rate-limited, timestamped, not shareable as fact). Two teammates who want the same slice each run `--sample` locally. (4) **Geography auto-selection prefers state** — the fallback is `for=state:*` because it's the smallest wildcard-safe query that returns something meaningful for most aggregate-table products; when a probe exists we pick the first queryable-without-parent level the API actually declares. (5) **Variable auto-selection prefers estimate/MOE pairs** — the first 20 variables ending in `E` that also have a matching `_M` counterpart, then fill from remaining estimates. Keeps sample requests small and biased toward aggregate-table content where uncertainty lives; on microdata products where nothing has that pattern, falls back to whatever's in variables.json. (6) **No new dependencies** — pandas is already installed; sparkline histogram hand-rolled to avoid a numpy pull; requests deliberately not added because urllib.request handles the tiny surface (GET + JSON) we actually use. (7) **End-to-end regen time unchanged** — 0.13s from the cached catalog (against the 30-second Phase-1/2 target). Sample runs add whatever the API takes plus the 500ms polite-delay between requests in batch mode. (8) **Filesystem lock quirk persists** — same Linux-mount limitation as the redesign session; leftover `.git/HEAD.lock` and `.git/index.lock` from a prior session could not be unlinked, so all nine commits were done in a `/tmp/csrepo` clone and `.git/` will be swapped back on the way out. See notes for Garrett below.
- **Files:** tools/product_scope.py (~+900 LOC net across the 8 features), tools/README.md (new "Sampling a product" section + committed-files table entry), tools/archive/product_scope_v1.py, tools/archive/scope_evidence_v1.py, tools/archive/README_v1.md, tools/archive/ARCHIVE_NOTES.md (new), .gitignore (adds scope_data_cache.json + .scope_last_eda_diff.json), product_report.html (regenerated), WORKLOG.md. Commit hashes: `1d60239` (archive v1), `ca9d30b` (#1 CLI + fetch), `d46f03f` (#2 canonical EDA), `57f2bc1` (#3 cache), `c5538bf` (#4 freshness pill helper), `4414d67` (#5 card section), `9279b11` (#6 diff on refresh), `64168e8` (#7 batch summary), `e988ebd` (#8 sample affordance), plus this WORKLOG entry.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 2 redesign (8 features, behavior over description)
- **Area:** tools
- **What was done:** Substantial redesign of `tools/product_scope.py` around one principle: every UI element either **shows state or offers an action** — no static tutorials. Eight commits, each a self-contained feature (hashes at the bottom). **Phase 1 UX shifts:** (1) a **freshness pill** on every page shows how long ago the report was regenerated (green <24h, amber <3d, red older), color-graded at page-load via inline JS reading a `data-generated-at` attribute; a click-to-copy rerun command sits next to it. (2) A **faceted browsing sidebar** on the Products tabs (Aggregate / Microdata / Time series / Unflagged) with live counts across seven facets — Status, Family, Agency, Has repo evidence, Has API probe, Notebook validated, Composite role. Clicking any value filters the visible cards AND recomputes every other facet's counts against the intersection. AND across facets, OR within; stacks on top of the existing text-search filter. Pure inline JS + CSS. (3) **Evidence links jump to source:** every `file:line` receipt on every card and the Home-tab evidence table is now a clickable link — GitHub blob URL with `#L<line>` anchor when the origin remote is GitHub, `file://` fallback otherwise. Detected once at gen time from `git remote get-url origin` + `git rev-parse --abbrev-ref HEAD`. (4) **State-driven affordances** replace blank sections: no repo evidence → copyable `--probe <path>` command; Candidate without a probe → amber probe command; FOCUS with a snapshot SHA that differs from current HEAD → amber regen command (activates via #5); still Cataloged → nudge to edit the exact JSON key. All copyable via the click-to-copy JS from #1. (5) **Diff view between runs:** `.product_scope_last_run.json` (gitignored) snapshots per-product state (stage, evidence-hit count, probe presence, composite_role, HEAD SHA); Home tab opens with a **"Since last regeneration"** banner — "+N new evidence hits (acs ×4), M status changes, K new probes" — plus a `<details>` collapsible listing product-level changes. First-ever run shows a baseline message instead. **Phase 2 composite coverage:** (6) **Composite code references (AST-derived)** — parses `analysis/composite.py`, `analysis/cv_model.py`, `analysis/alloc.py` straight out of `origin/JL_Work_Tree` via `git show` (no worktree, no checkout, no branch switching), walks each file's AST to collect imports / calls / string literals + line numbers, matches symbols against tracked products via a small `COMPOSITE_REF_MAP`, and surfaces hits on cards + a Home-tab table. Degrades gracefully on SyntaxError. (7) **`composite_role` + `composite_role_note` fields** added to `product_review.json` — role is one of `cv_source / allocation_source / privacy_noise / geometry / benchmark / unused` or empty; note is free text. Tool enforces a **note-required contract**: a role without a note is logged to stderr and downgraded to unset (`effective_role()`). Cards render the role as a gold chip (tooltip = note); the Composite-role sidebar facet from #2 is now live with real values and counts. The tool never sets a role — humans do, in JSON. (8) **Divergence flag:** cross-references #6 against #7. Two failure modes — "referenced by composite code but no role declared" and "role declared but not referenced by any composite module" — surface as amber banners on the affected cards AND as a **Divergences** section on the Home tab.
- **Findings / decisions:** (1) **Real divergence caught by the tool on merge:** `acs/acs5` is referenced by `cv_model.py:28` and `alloc.py:34` on `JL_Work_Tree` but has no `composite_role` declared — the divergence flag detected this immediately. Setting `composite_role: "cv_source"` (with a note) resolves it and the chip / facet / snapshot all update on the next regen. (2) **JL_Work_Tree access — chose `git show`** over `git worktree add` because it's atomic, leaves no cleanup, and doesn't require a fresh branch switch; the tool tries all three files independently and skips any that fail to parse rather than aborting. All three parsed cleanly today; if a WIP push breaks one, that file's card gets a "(parse error — try regenerating after next JL_Work_Tree update)" note and the others still surface. (3) **SHA-differs affordance interpretation:** the FOCUS/SHA rule fires when the previous snapshot's captured HEAD SHA differs from the current HEAD SHA. It effectively says "code has moved since the last time this report was generated" and encourages a rerun. Silent on the very first run (no snapshot). (4) **Family bucket truncation:** the Family facet caps at 12 named values per panel + an "Other" bucket, computed per-tab from that tab's product distribution — keeps the sidebar scannable on the 244-family Aggregate-tables tab without dropping information (Other still filters). (5) **Backward compatibility preserved:** `--repo`, `--probe`, `--probe-queue`, `--export csv/xlsx`, `--online`, and `--out` all behave identically; `product_review.json`'s existing entries are never overwritten (new fields default to empty on newly created entries only). (6) **CSV export** routes evidence through the new `receipt_label()` helper so its column format is byte-identical to before. (7) **Regen timing:** end-to-end `python tools/product_scope.py --repo .` runs in ~0.5s on the cached-catalog path — well under the 30-second target — because the AST parse of three ~200-line files and the git subprocess calls are trivial next to loading the catalog cache. (8) **Filesystem quirk from the previous session persisted:** `.git/*.lock` files can't be unlinked from the container's Linux mount; worked around by cloning fresh to `/tmp/csrepo`, doing all commits there, and swapping `.git/` back at the end (per the handoff protocol in the redesign spec).
- **Files:** tools/product_scope.py (~+800 LOC net), product_report.html (regenerated), .gitignore (adds `.product_scope_last_run.json`), WORKLOG.md. Commit hashes: `25c5450` (#1 freshness pill), `eb1012c` (#2 faceted sidebar), `55d85dd` (#3 evidence links), `396ec58` (#4 affordances), `f0eaed0` (#5 diff view), `13830b7` (#6 composite refs), `0666a54` (#7 composite_role), `91be821` (#8 divergence flag).

### 2026-07-25 — Garrett Spangler — Product scope tracker: PRODUCT_MATCH coverage + `--export csv/xlsx`
- **Area:** tools
- **What was done:** Two enhancements to `tools/product_scope.py`. (1) Expanded the `PRODUCT_MATCH` dict so the evidence engine actually credits the work we already have on DHC and on our own analysis modules: the DHC matcher now catches `analysis/dhc.py`, `import dhc`, and `analysis.dhc` module references (previously the only pattern was `dec/dhc`, a string that appears nowhere in the code — the tool's own README flagged this as a known limit); and three new tracked products — `Composite prototype`, `Allocation analysis`, `CV driver model` — pick up the composite/alloc/cv_model imports in notebooks 05, 06, 07 and the `pull_acs_alloc_nj.py` ingestion script. (2) Added a `--export csv|xlsx` CLI flag that writes a one-row-per-product review table (stdlib `csv` / `openpyxl`) reusing the exact catalog + review + evidence + probe structures the HTML report already builds, so the export cannot drift from the tabs. `--export` skips HTML; the default HTML run is unchanged. Regenerated `product_report.html` and updated `tools/README.md`, `.gitignore` (adds `product_review.csv/xlsx`), and `requirements.txt` (adds `openpyxl`).
- **Findings / decisions:** (1) **Evidence coverage jump:** 2020 DHC went from **0 → 3 receipts** (notebook 04 cell 3, `analysis/dhc.py` line 36, data-dictionary); Composite prototype **0 → 2** (notebooks 06/07); Allocation analysis **0 → 5** (notebooks 05/06/07, `pull_acs_alloc_nj.py`, data-dictionary); CV driver model **0 → 1** (notebook 07). All four now register as Validated on the Home tab. (2) **New tracked products are our own methodology, not Census products** — they surface on the Home tab's "What is in the repo" table but do not attach to any catalog family card, because no family maps to them (FAMILY_TO_PRODUCT untouched). This is deliberate: `Composite prototype` etc. are our work products, and the Census catalog has no equivalent path. (3) **Judgment call, "allocation" as a matcher:** the plain word appears in Bureau documentation and product descriptions across the catalog — matching it would over-count. Chose `alloc.py`, `analysis.alloc`, `from analysis import ... alloc`, and `pull_acs_alloc` instead, which hit only real code references. (4) **Export field coverage:** `Product ID / Name / Family / Status / Repo Evidence / Geography Levels` come from real data; `MOE Var Count / Allocation Groups` are populated only for probed products (currently none — no `product_probes.json` on the repo yet); `Notes` falls back to `uncertainty_metrics` when the free-text `note` is blank; `Agency`, `Last Reviewed By`, `Last Reviewed Date` are not in the tool's data model and export as empty. (5) The tool auto-creates `product_review.json` (573 default `cataloged` entries) on first run — per tool README this file IS committed as team-owned state, but it is not part of these two enhancement commits and can be committed separately at the team's discretion.
- **Files:** tools/product_scope.py, tools/README.md, product_report.html (regenerated, 1.18 MB), .gitignore, requirements.txt, WORKLOG.md

### 2026-07-20 — Andrew Swiniarski — Housekeeping: progress docs synced for Justus's EDA 06–07
- **Area:** project management / docs
- **What was done:** Pulled Justus's Phase 2 bridge work into local (fast-forward, no conflicts) and reviewed both notebooks end-to-end against EDA 01–05. Updated the progress docs to account for it: HANDOFF (state, provisional decision #12 for the composite prototype's working boundaries, new landmine, reordered next-work), README (adopted the "Bridge toward composite score" checklist section and four new mentor questions **verbatim from `JL_Work_Tree`**, so the pending branch merge lands those hunks without conflict), and CLAUDE.md (new "Working With Teammates" section — fetch-first, WORKLOG as sync point, branch-merge etiquette, reproducibility contract on `main`).
- **Findings / decisions:** EDA 06/07 confirm and extend Phase 1 with independent methods: the 22.8% low-CV/high-allocation blind spot quantifies EDA 05's independence result; EDA 07's R² decomposition (estimate size ≈ 0.67 of pooled CV variance, place population ≈ 0.005) re-derives EDA 02's mechanism. **Repo-state flag: notebooks 06/07 on `main` import `analysis/alloc.py`/`composite.py`/`cv_model.py`, which exist only on `origin/JL_Work_Tree`** — they cannot run from `main` until that branch merges; merge + local re-execution + unit tests queued as next-work #1. No changes were made to Justus's work itself.
- **Files:** HANDOFF.md, README.md, CLAUDE.md, WORKLOG.md

### 2026-07-19 — Justus Long — EDA 07: stronger CV driver model + composite V2 residual flag
- **Area:** analysis / notebooks / docs
- **What was done:** Implemented the JL Final Takeaway multivariate model. New [`analysis/cv_model.py`](analysis/cv_model.py) builds a long CV frame separating place population from estimate size (income uses household universe `B99192_001E`, never dollars), fits nested numpy OLS models, and supports residual flags. [`notebooks/07-cv-driver-model.ipynb`](notebooks/07-cv-driver-model.ipynb) runs the nested models, a matched estimate-size panel (population / poverty / Black 65+), and wires `attach_cv_residual_flag` into the EDA 06 income matrix path. Charts: `eda07_nested_r2_and_residuals.png`, `eda07_cumulative_r2.png`, `eda07_matched_estimate_size.png`.
- **Findings / decisions:** (1) Place-pop-only R² ≈ **0.005** (JL failure reproduced). (2) Estimate-size alone R² ≈ **0.666**; full model ≈ **0.706**; interaction ≈ **0.726** (n = 24,947). (3) Matched mid-size bins: Black 65+ median CV still higher than population (~0.28 vs ~0.17). (4) Income residual model R² ≈ **0.01** — HH size does not explain income CV; residual flags are kept as a composite V2 seed but are more meaningful for count variables. Decision: keep matrix headline; do not lock weights; no DHC privacy join.
- **Files:** analysis/cv_model.py, analysis/test_cv_model.py, analysis/composite.py (`attach_cv_residual_flag`), notebooks/07-cv-driver-model.ipynb, docs/glossary.md, docs/phase1-findings-report.md, docs/uncertainty-sources.md, README.md, WORKLOG.md; charts in `data/processed/eda07_*.png` (local-only, regenerable)

### 2026-07-18 — Justus Long — EDA 06: composite reliability prototype (ACS CV × allocation matrix)
- **Area:** analysis / notebooks / docs
- **What was done:** Bridged Phase 1 EDA into a transparent ACS composite prototype. Graduated EDA 05 rate formulas into [`analysis/alloc.py`](analysis/alloc.py); added [`analysis/composite.py`](analysis/composite.py) for quadrant classification plus equal-weight / worst-component sensitivity helpers (with unit tests). Built and executed [`notebooks/06-composite-reliability-prototype.ipynb`](notebooks/06-composite-reliability-prototype.ipynb) at NJ tract level with income as the exact headline pairing, matched-size validation, and labeled poverty / Black 65+ robustness cases. Charts: `eda06_income_reliability_matrix.png`, `eda06_income_quadrant_map.png`, `eda06_matched_size_validation.png`.
- **Findings / decisions:** (1) **Blind spot is large for income:** among 2,109 classified tracts, **22.8%** are low CV (≤ 0.30) but high income allocation (above the exploratory NJ 75th percentile ≈ 0.46) — CV alone would treat these as fine. (2) **Matrix stays the headline:** equal-weight vs worst-component agree on only **84.6%** of top-risk-quartile membership (162 tracts each exclusive) — combination rules matter at the margin; do not lock weights yet. (3) **Matched-size check:** within household-count quartiles, income-allocation IQR stays > 0.05 and quadrant mixes still differ — size does not collapse the imputation axis (consistent with EDA 05). (4) **Product boundary held:** no row-join of 2010 DHC privacy noise into the ACS score. Poverty uses the labeled family-allocation proxy; Black 65+ uses age/race allocation only as a diagnostic axis. Mentor decisions logged in README.
- **Files:** analysis/alloc.py, analysis/composite.py, analysis/test_alloc.py, analysis/test_composite.py, notebooks/06-composite-reliability-prototype.ipynb, docs/glossary.md, docs/uncertainty-sources.md, README.md, WORKLOG.md; charts in `data/processed/eda06_*.png` (local-only, regenerable)

### 2026-07-17 — Andrew Swiniarski — Phase 1 findings report + first Bureau biweekly deck
- **Area:** docs
- **What was done:** Two communication deliverables closing out Phase 1. [docs/phase1-findings-report.md](docs/phase1-findings-report.md) — the complete EDA 01–05 analysis written in plain language for the lead, the team, and Census readers: methods explained non-technically but honestly, all findings with their numbers, the six-component composite-score justification (§6), the open-questions list, and a repo map; every figure in it traces to an assert-guarded notebook. `docs/biweekly-2026-07-22.pptx` — 9-slide check-in deck for the July 22 mentor meeting, matched to the team-recap design system (navy/ice/coral, Calibri Light, numbered-circle motif): title, one status slide (flagship EDA 04 chart + three findings), three priority questions, five chart backups with "How to read it" cards, full open-questions backup; speaker notes on every slide, written so the lead can present without support. Visual QA rendered through desktop PowerPoint itself (COM automation), so verified output is pixel-identical to what mentors will see.
- **Findings / decisions:** Decision: presentation/report binaries in `/docs` ARE committed (lead call, 2026-07-18) — the deck and report ship with the repo; regenerating charts stays a notebook job. The findings report is the plain-English record of Phase 1 and should be kept current as later phases land findings (noted in CLAUDE.md).
- **Files:** docs/phase1-findings-report.md, docs/biweekly-2026-07-22.pptx, README.md (front-door pointer), CLAUDE.md (living-report standard), HANDOFF.md

### 2026-07-17 — Andrew Swiniarski — EDA 05: allocation rates — imputation prevalence + independence from sampling noise (Phase 1, Step 5 COMPLETE)
- **Area:** ingestion / notebooks
- **What was done:** Final Step 5 EDA. [ingestion/pull_acs_alloc_nj.py](ingestion/pull_acs_alloc_nj.py) pulls the ACS allocation (imputation) tables pairing with our Phase 1 variables — B98031/B98032 overall rates, B99011/B99012/B99021 (sex/age/race), B99192 (household income), B99172 (family poverty, labeled proxy) — at county/tract/block group, **estimates only** (allocation tables publish no MOE variables; requesting a `_M` errors the query). [notebooks/05-allocation-rates-vs-cv.ipynb](notebooks/05-allocation-rates-vs-cv.ipynb) derives item allocation rates and tests whether they correlate with CVs, using **raw + size-controlled Spearman** (CV residualized on log universe count first, since EDA 02's size law would otherwise confound; controls are universe *counts* — households for income — never dollar values). Chart saved: `eda05_allocation_vs_cv.png`.
- **Findings / decisions:** (1) **Imputation is prevalent and variable-driven:** at the median NJ tract ~39% of households had some income imputed (family-poverty proxy ~41%) vs. <1% for age/race and ~0.05% for sex — three orders of magnitude across items; county overall rates 11.5–16.4%. (2) **Independence confirmed — the multi-component composite score is empirically justified (Q2):** every size-controlled allocation-vs-CV correlation at tract level sits in [−0.00, +0.19]; block group agrees. (3) **The size control mattered:** the poverty pairing's raw ρ = −0.18 collapsed to +0.02 once size was removed — the raw association was pure tract-size confound; future correlation-with-CV analyses should default to the controlled version. (4) **Surprise/landmine:** B98031/B98032 (the Bureau's ready-made overall allocation rates) are **county-only** — the API returns tract/BG rows with all-null values (probe lesson: returned rows ≠ returned values). (5) Weak income association (controlled ρ +0.19) logged as a mentor question. Decisions: E-only pull; family-universe poverty proxy labeled everywhere; top-coded-income exclusion carried over from EDA 01/02; rate derivation kept notebook-local (graduates to `analysis/alloc.py` if the composite score adopts it, per the `dhc.py` precedent).
- **Files:** ingestion/pull_acs_alloc_nj.py, notebooks/05-allocation-rates-vs-cv.ipynb, docs/data-dictionary.md (allocation sub-entry), docs/glossary.md (entry extended), README.md (Step 5 complete, new mentor question), HANDOFF.md; outputs `data/raw/acs5_2024_nj_alloc_*.parquet` + `data/processed/eda05_allocation_vs_cv.png` (local-only, regenerable)

### 2026-07-16 — Andrew Swiniarski — EDA 04: privacy noise measured — DAS demonstration vs. published 2010 SF1 (Phase 1, Step 5)
- **Area:** notebooks / analysis
- **What was done:** Fourth EDA notebook, [notebooks/04-privacy-noise-das-demo.ipynb](notebooks/04-privacy-noise-das-demo.ipynb): joins the 2010 DHC demonstration counts (parsed by the new `analysis/dhc.py`) 1:1 against published 2010 SF1 at five levels (state → block, 177,940 geographies), and measures privacy noise = demonstration − published for total population and the Black 65+ subgroup. Headline method (lead-approved): **binned relative RMSE** — RMSE of errors per size bin ÷ mean published count, the empirical analog of a CV — with per-unit scatter as a supporting view (per-unit log-log fits would drop the many exactly-zero errors and bias the slope). Three mentor-ready charts saved to `data/processed/` (flagship: `eda04_noise_rmse_by_size.png`).
- **Findings / decisions:** (1) **The slope hypothesis is directionally confirmed**: within-level slopes for total population are −0.78 (block) / −0.85 (block group) / −0.90 (tract) — far steeper than the −0.5 sampling law (EDA 02) and near the −1 constant-absolute-noise ideal, so **the two dominant noise mechanisms follow measurably different laws** (honest caveat: absolute noise grows several-fold within a level, so exactly −1 it is not). (2) **Surprise — the block-group anomaly**: absolute noise by level is non-monotonic; block groups (RMSE 23.2 people) run ~9× tracts (2.5) and ~3.4× blocks (6.9); a same-size block group is ~12× noisier than a tract; for the P12B subgroup table, *county* is noisiest in absolute terms (34.6). Level — i.e., where the privacy-loss budget is spent — is a first-class reliability factor; mechanism hypothesis (off-spine block groups) logged as a mentor question, not asserted. (3) Practical: total-population privacy noise crosses the 0.30 caution convention only below ~21-person blocks and is negligible (≤0.2%) at tract/county scale; the Black 65+ subgroup sits at ~0.3–0.4 relative noise at the *median* tract (18 people) — same order as, but ~2–3× smaller than, its ACS sampling CV (0.886, EDA 02). (4) **Ghost/vanished blocks are a categorical artifact class**: 807 blocks gain 4,695 phantom residents, 427 lose all 1,154 of theirs. (5) Errors sum to exactly 0 at every level for total population (state invariant propagating down); the subgroup's statewide +16 error propagates identically — hierarchical consistency observed live.
- **Files:** notebooks/04-privacy-noise-das-demo.ipynb, docs/glossary.md (7 new entries), docs/data-dictionary.md (SF1 entry, demo-entry updates), README.md (Step 5 checkbox, two new mentor questions); charts at data/processed/eda04_*.png (local-only, regenerable)

### 2026-07-16 — Andrew Swiniarski — 2010 SF1 baseline pull + DHC demonstration-file parser (EDA 04 groundwork)
- **Area:** ingestion / analysis
- **What was done:** Two new reusable pieces. [ingestion/pull_sf1_2010_nj.py](ingestion/pull_sf1_2010_nj.py) pulls the published 2010 SF1 counts (total population P1 + the twelve P12B Black 65+ cells, mirroring the ACS pull) for NJ at state/county/tract/block group/block — block-level queries run county-by-county — with variable labels verified live against the API and 10 sanity checks. [analysis/dhc.py](analysis/dhc.py) parses the DHC demonstration file **directly from its zip** (raw download never extracted): geo header + segment readers, LOGRECNO join, and a quality panel that *proves* the parse — the state-population invariant (demo NJ total must equal 8,791,894 exactly), P1 vs. geo-header POP100 on every record, and P12B internal additivity on all 219,847 records. Layout constants (P1 = segment 5 first cell; P12B = segment 8 fields 153–201) were derived from the release's Table Matrix and are re-proven at every run.
- **Findings / decisions:** All sanity checks pass on both sides; record counts match 1:1 at every level (1/21/2,010/6,320/169,588). Full-count additivity confirmed on SF1 (every level sums to exactly 8,791,894). SF1 API variable naming gotcha documented: `P001001`/`P012B020` convention (not `P0010001`), and `PCT012B020` exists as a *different* table. Decision (lead-approved 2026-07-16): EDA 04 scope = total population + Black 65+ parallel, down to block level, binned-relative-RMSE headline metric.
- **Files:** ingestion/pull_sf1_2010_nj.py, analysis/dhc.py; outputs `data/raw/sf1_2010_nj_*.parquet` (local only, regenerable)

### 2026-07-15 — Andrew Swiniarski — Housekeeping: progress-tracking docs synced to end of first EDA session
- **Area:** docs / project management
- **What was done:** Rewrote [HANDOFF.md](HANDOFF.md) to the current state (runbook complete, EDA 01–03 done, DAS file on disk, decisions 4–9 and new landmines recorded, next-work list refreshed — the stale "project is PAUSED" language is gone). Ticked README's Step 5 "done when" and noted Step 4 as started; refreshed the docs/README status table; extended CLAUDE.md's session reading list to include WORKLOG.md and HANDOFF.md.
- **Findings / decisions:** None — bookkeeping.
- **Files:** HANDOFF.md, README.md, docs/README.md, CLAUDE.md, WORKLOG.md

### 2026-07-15 — Andrew Swiniarski — DAS demonstration data located + downloaded; data dictionary started (runbook Step 6)
- **Area:** ingestion / docs
- **What was done:** Surveyed the Census Bureau's 2010 Demonstration Data Products archive (all five product folders), picked a provisional release, and scripted the download: [ingestion/pull_das_demo_nj.py](ingestion/pull_das_demo_nj.py) pulls the New Jersey summary file from the **2022-08-25 DHC demonstration release** (nj2010.dhc.zip, 250 MB, integrity-verified: 44 table segments + geo header, 2.0 GB uncompressed) plus the four companion docs needed to parse it (README, technical document, state geoheader layout, table matrix) into `data/raw/das_demo/`. Started [docs/data-dictionary.md](docs/data-dictionary.md) (README Step 4) with ACS, boundary, and DAS-demo entries. This closes the NEXT_ACTIONS runbook (Steps 1–6 all done).
- **Findings / decisions:** Provisional vintage choice (mentor question updated in README): **2022-08-25** is the newest *tabulated* demonstration release and nearest to DHC production settings — the 2023-04-03 production suite ships only national microdata (15 GB + 9.4 GB CSVs, impractical) and the 2022-03-16 tabulated release has an April 2022 technical-issues ALERT in its folder, so it was skipped. Gotcha for teammates: the archive's directory-index pages frequently time out (Cloudflare 524) — deep-link to files directly, as the script does. Baseline for the noise comparison (EDA #4) will be published 2010 SF1 tables via the API.
- **Files:** ingestion/pull_das_demo_nj.py, docs/data-dictionary.md, README.md (Step 3 checkboxes, vintage mentor question), NEXT_ACTIONS.md (Step 6 closed); data in data/raw/das_demo/ (local-only, regenerable)

### 2026-07-15 — Andrew Swiniarski — EDA 03: tract-level reliability choropleth, dashboard prototype (Phase 1, Step 5)
- **Area:** notebooks
- **What was done:** Third EDA notebook, [notebooks/03-cv-choropleth-nj-tracts.ipynb](notebooks/03-cv-choropleth-nj-tracts.ipynb): joins tract CVs to the vintage-matched boundaries (1:1 join re-asserted in-notebook), bins them into reference reliability tiers (ESRI/NCHS conventions, shown descriptively), and renders a side-by-side choropleth of median household income vs. poverty — a crude prototype of the executive-dashboard map (deliverable #3). Gray "no usable estimate" rendered as its own first-class category. Verified top-to-bottom with `jupyter nbconvert --execute`.
- **Findings / decisions:** (1) **Not one of NJ's 2,181 tracts reaches the high-reliability tier for poverty** (income: 44% do; 92% of tracts are below CV 0.30 for income vs. 21% for poverty); (2) **poverty-data reliability maps as the inverse of poverty itself** — Spearman −0.58 between poverty rate and poverty CV; median CV 0.31 in the poorest quartile of tracts vs. 0.49 in the least-poor quartile. Urban cores (Newark, Camden, Trenton, Atlantic City) are the *most* reliable places for poverty data; affluent/suburban tracts the least — an equity-relevant blind spot for anyone tracking suburbanizing poverty. (3) Classed tiers + explicit gray class communicate at a glance; carried forward as dashboard design seeds. Phase 1 Step 5's "done when" (notebooks committed, 2–3 mentor-ready charts) is now satisfied.
- **Files:** notebooks/03-cv-choropleth-nj-tracts.ipynb, docs/glossary.md (choropleth entry), README.md (Step 5 checkbox); map at data/processed/eda03_cv_choropleth_income_poverty.png (local-only, regenerable)

### 2026-07-15 — Andrew Swiniarski — EDA 02: CV by variable type + first derived estimate (Phase 1, Step 5)
- **Area:** notebooks / analysis
- **What was done:** Second EDA notebook, [notebooks/02-cv-by-variable-type.ipynb](notebooks/02-cv-by-variable-type.ipynb): at tract level, compares CV distributions across variable types, builds the project's first **derived estimate** (Black 65+ = six sex×age cells summed, MOE via the Census handbook's root-sum-of-squares formula with the zero-cell rule, sensitivity vs. plain RSS shown), and tests the mechanism with a CV-vs-estimate-size log-log scatter against the theoretical −½ sampling-noise slope. Extended [analysis/acs.py](analysis/acs.py) with `aggregate_estimate` and a zero-rule `aggregate_moe`. Verified top-to-bottom with `jupyter nbconvert --execute`.
- **Findings / decisions:** (1) **The sampling-noise law holds for size-driven counts** — fitted log-log slopes: population −0.47, Black 65+ aggregate −0.51, vs. theoretical −0.50; (2) **poverty defies it (slope −0.18)** — bigger poverty counts mean poorer tracts, not bigger samples, so its reliability is capped by prevalence (explains EDA 01's "80% of tracts unreliable" shocker); (3) **aggregation helps but doesn't rescue subgroups**: median CV 0.886 → 0.631 (1.4×) and coverage 11–50% → 73% of tracts, yet 87% of nonzero tracts stay above CV 0.30; (4) the handbook zero-cell rule trims a median 10% (max 55%) off plain-RSS MOEs — adopted as default, documented in glossary; (5) **surprise, unexplained: 131 tracts (all 21 counties) publish population MOEs of ±14–143 vs. median ±546** — near-controlled precision with a published MOE; cause unknown, logged as a mentor question rather than guessed at. Composite-score implications: estimate magnitude is a strong free predictor for counts, but variable type must be its own component.
- **Files:** analysis/acs.py, notebooks/02-cv-by-variable-type.ipynb, docs/glossary.md (derived estimate, MOE aggregation entries), README.md (Step 5 checkbox, new mentor question); charts at data/processed/eda02_*.png (local-only, regenerable)

### 2026-07-15 — Andrew Swiniarski — EDA 01: CV by geography size (Phase 1, Step 5)
- **Area:** notebooks / analysis
- **What was done:** First EDA notebook, [notebooks/01-cv-by-geography-size.ipynb](notebooks/01-cv-by-geography-size.ipynb): runs a data-quality panel on the NJ pull, computes coefficients of variation for every variable at county / tract / block group, and produces the headline mentor chart (income CV by geography level, log scale, with ESRI/NCHS reference lines shown descriptively). Shared formulas graduated to a new reusable module, [analysis/acs.py](analysis/acs.py) (CV, top-code flag, root-sum-of-squares MOE aggregation — each with its Census citation), so every later notebook computes them identically. Verified top-to-bottom with `jupyter nbconvert --execute`.
- **Findings / decisions:** Central empirical fact confirmed: median income CV **0.014 (county) → 0.128 (tract) → 0.204 (block group)**; worst-case income MOE ±$3,982 → ±$247,531. Surprises worth flagging: (1) **tract-level poverty exceeds CV 0.30 in 80% of NJ tracts** — a policy-critical variable is low-reliability almost everywhere planners would use it; (2) Black 65+ single cells are effectively noise at tract level (median CV ≈ 0.9) and are zero in 50–89% of tracts — supports aggregating the six cells in EDA 02; (3) every top-coded income (41 tracts, 270 block groups) ships with **no MOE at all**, and 2 non-top-coded block groups also lack income MOEs (unexplained annotation — logged for the data dictionary); (4) county population MOEs are all blank because the estimates are *controlled* (very reliable) — the "two opposite meanings of a missing MOE" landmine seen live. Decisions per the approved EDA plan: zero estimates → CV undefined; top-coded incomes excluded from CV distributions; thresholds 0.12/0.30/0.40 cited descriptively only (our tiers = later mentor decision).
- **Files:** analysis/acs.py, notebooks/01-cv-by-geography-size.ipynb, docs/glossary.md (reliability-threshold conventions entry), README.md (Step 5 checkbox); chart at data/processed/eda01_income_cv_by_geography.png (local-only, regenerable)
### 2026-07-14 — Katie Christiansen — US boundary pull & exploration
- **Area:** ingestion
- **What was done:** Wrote and ran [ingestion/pull_acs_us.py](ingestion/pull_acs_us.py) — pulls 2024 ACS 5-year estimates and matching cartographic boundaries for all US states and counties. The script saves separate ACS Parquet and GeoParquet files, verifies a 1:1 join on Census geography IDs, and renders a nationwide county verification map. Added [ingestion/Explore.py](ingestion/Explore.py) to join the national county data to its geometry and save a median-household-income map. This is very similar to Andrew's NJ analysis, but at a US level. 
- **Findings / decisions:** National analysis is limited to state and county levels for now because nationwide tract and block-group geometry is substantially larger and slower to manage. Estimates include 90% margin-of-error columns.
- **Files:** ingestion/pull_acs_us.py; Explore.py; outputs `data/raw/acs5_2024_us_*.parquet`, `data/raw/geo_2024_us_*.parquet`, `data/raw/verification_map_us_counties.png`, and `data/raw/us_county_median_household_income.png` (local only, regenerable). I uploaded these files manually for now.

### 2026-07-12 — Andrew Swiniarski — Team sync recap deck; work log started
- **Area:** project management / docs
- **What was done:** Built the recap deck for the July 12 team sync ([docs/team-recap-2026-07-12.pptx](docs/team-recap-2026-07-12.pptx)) covering setup status, the NJ pilot dataset, the headline finding (uncertainty explodes at small geographies), documented data landmines, and the 10-minute teammate onboarding path. Created this work log; teammates begin working in the repo from this point.
- **Findings / decisions:** First mentor meeting set for **July 22, 2026** — we bring a one-slide status and the CV-by-geography chart. Open items from the sync: workstream assignments (analysis / dashboard / methodology write-up / data hunting), repo-review feedback, confirming the recurring biweekly slot.
- **Files:** docs/team-recap-2026-07-12.pptx, WORKLOG.md

### 2026-07-09 — Andrew Swiniarski — NJ boundary pull + 1:1 join verification (Phase 1, Step 5)
- **Area:** ingestion
- **What was done:** Wrote and ran [ingestion/pull_nj_geometry.py](ingestion/pull_nj_geometry.py) — pulls NJ county/tract/block group cartographic boundaries (vintage-matched to the 2024 ACS data), saves GeoParquet to `data/raw/`, verifies every data row joins to exactly one shape at all three levels, and renders a verification map of all 2,181 tracts.
- **Findings / decisions:** Join checks passed 1:1 at every level. **6 invalid geometries at tract level and 6 at block group** (self-intersection quirks) — harmless for choropleths, but `make_valid` must run in the future processing layer before any area/overlay math.
- **Files:** ingestion/pull_nj_geometry.py (commit e7895a5); outputs `data/raw/geo_2024_nj_*.parquet` + `verification_map_nj_tracts.png` (local only, regenerable)

### 2026-07-09 — Andrew Swiniarski — ACS 5-year NJ data pull with MOEs (Phase 1, Step 4)
- **Area:** ingestion
- **What was done:** Wrote and ran [ingestion/pull_acs_nj.py](ingestion/pull_acs_nj.py) — ACS 5-year vintage 2024, New Jersey, at county / tract / block group: total population, median household income, poverty count, and six Black 65+ subgroup cells, each with its margin of error. Verifies official variable labels at runtime, hard-checks the 21-county count, and prints a per-column sanity report. Row counts: 21 counties / 2,181 tracts / 6,599 block groups.
- **Findings / decisions:** Three data landmines found and documented: (1) poverty (B17001) and race×age (B01001B) tables are **not published below tract level** — tract confirmed as the floor, mentor question logged; (2) **censusdis converts ACS annotation codes to NaN**, collapsing "controlled/very reliable" and "insufficient sample/unreliable" into identical blanks; (3) **median income is top-coded at $250,001**. Decision: `data/raw/` stays exactly as the API returned it; all cleaning belongs in a future processing layer.
- **Files:** ingestion/pull_acs_nj.py (commit 3e0cb91); outputs `data/raw/acs5_2024_nj_*.parquet` (local only, regenerable); landmines detailed in [HANDOFF.md](HANDOFF.md)

### 2026-07-09 — Andrew Swiniarski — Repo, environment, and secrets setup (Phase 1, Steps 1–3)
- **Area:** infrastructure
- **What was done:** Initialized the repo and pushed to GitHub; scaffolded the folder structure (`/ingestion`, `/notebooks`, `/analysis`, `/docs`, `/data`) with per-folder READMEs; set up `.gitignore` (raw data + secrets excluded), the `.env` API-key pattern with a committed `.env.example`, `requirements.txt`, and a Python 3.12 virtual environment. Census API key verified with a live call. Added the "Getting Started" quickstart to the README and seeded [docs/glossary.md](docs/glossary.md) (18 terms).
- **Findings / decisions:** Git history was **rewritten on 2026-07-09** to purge sponsor contact details (the repo is public) — commit SHAs from before that date are invalid, and stale clones must not be pushed from. Never re-add sponsor contact info to committed files. Session handoff notes written to [HANDOFF.md](HANDOFF.md).
- **Files:** commits 9d11afb → bcc1c5c; requirements.txt, .env.example, .gitignore, HANDOFF.md, docs/glossary.md
