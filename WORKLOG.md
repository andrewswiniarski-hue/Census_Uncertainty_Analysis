# WORKLOG.md — Team Work Log

A running record of the work done on this project: **who did it, when, and what came of it.** Newest entries go at the top.

**Why we keep this:** with the whole team working in one repo, this file is how we stay aware of each other's progress without reading every commit. It is also part of our defensibility story — when we assemble the final report and present to Census mentors, this is the paper trail of what was done, by whom, and what was found along the way.

---

## How to add an entry

When you finish a piece of work — a script, an analysis, a notebook, a document, a meeting deliverable — copy the template below to the **top of the Log section** (newest first) and fill it in. One entry per completed chunk of work; tiny fixes (typos, formatting) don't need one.

```markdown
### YYYY-MM-DD — Your Name — Short title of the work
- **Area:** ingestion | analysis | notebooks | docs | dashboard | project management
- **What was done:** 2–4 plain-English sentences a teammate on another workstream can follow.
- **Findings / decisions:** anything learned, decided, or surprising — data quirks especially. ("None" is fine.)
- **Files:** paths, notebook names, and/or commit hashes.
```

Ground rules:

- **Plain English first.** Write the summary so a teammate who wasn't there understands it without opening the code.
- **Surprises go here.** A weird distribution, an impossible value, a data quirk — log it even if it feels minor. (Also raise it with the team; see the "Data landmines" list in [HANDOFF.md](HANDOFF.md).)
- **Update the log in the same commit as the work** whenever possible, so the log and the repo never drift apart.
- New statistical terms still get defined in [docs/glossary.md](docs/glossary.md) — the log links to work, the glossary explains terms.

---

## Log

### 2026-07-26 — Garrett Spangler — Product Scope Tracker: counter consolidation + Bureau feed + glossary tooltips (3 atomic commits)
- **Area:** tools
- **What was done:** Shipped three atomic follow-through commits on `tools/product_scope.py` from the head-to-head audit against census.gov. **(#1) Consolidated tier counters to single source of truth.** Three Home-tab progress counters were rendering different numbers on the same page: the named-tier rail said `551/0/0/22`, the Sankey said `573→22→22→22` (cumulative view of the same data), and the landscape treemap legend said `567/6/0/0` under a completely different "repo evidence" tier definition. Fixed by (a) renaming `_pipeline_counts()` to `_tier_counts()` and adding a `_has_review_signal()` helper so the reviewed condition matches the documented spec (`stage != "cataloged"` OR non-empty `uncertainty_metrics` OR non-empty `note` OR any `insights` entry — future-proofs against stages other than `focus`); (b) making the landscape treemap legend + `_prog_tier4()` read from the same helper and align labels to the pipeline vocabulary (`cataloged / probed / sampled / reviewed` instead of the prior `untouched / repo evidence / metadata fetched / data peeked`); (c) keeping `_pipeline_counts` as a thin alias so external callers don't break. Post-consolidation numbers (all three renderers now identical): cataloged 551, probed 0, sampled 0, reviewed 22, total 573. Program box coloring in the treemap now reflects the deepest pipeline stage any of the program's families reached — programs with a reviewed family light up green (SIPP, Decennial, ACS, PEP, PopProj), the 6 programs previously blue-for-repo-evidence go back to grey unless they have a reviewed family. **(#2) Bureau press-release mini-feed on Home.** New "This week from Census" compact section directly above the What-we've-learned feed. Fetches top 5 items from `https://www.census.gov/newsroom/press-releases.xml` at regen time (`urllib.request` + `xml.etree.ElementTree` — zero new deps) and caches for 24h in gitignored `.census_press_cache.json`. Handles both RSS 2.0 (`<item>`) and Atom (`<entry>`) feed shapes since census.gov has shipped both engines historically; parses `pubDate` as RFC-822 via `email.utils.parsedate_to_datetime` and Atom `published`/`updated` as ISO-8601. Four-tier graceful fallback: fresh cache → cache silently, network fetch (5s timeout) → parse+cache+use, fetch fails but stale cache exists → show cache with amber "may be outdated" tag, no cache + fetch fails → empty state with direct `census.gov/newsroom` link. The sandbox this was authored in blocks `census.gov` (403 tunnel-forbidden via the outbound allowlist) so ONLY the empty-state branch was seen in the local regen; the other three branches were exercised manually by seeding a mock cache and re-running — the render + parse paths worked cleanly. Closes the "recency awareness" gap the tool scored 5/10 on in the prior audit (vs census.gov's 9/10). **(#3) Glossary tooltips extended to jargon in body copy.** The pre-existing `.gloss` CSS + `data-tip` machinery was only wired to ~3 chips (`kind`, `research pipeline`, `reviewed`). This commit adds an `_gloss_body(html)` post-render helper that walks any rendered HTML string, splits on `<...>` tag boundaries, skips `<script>` / `<style>` / `<pre>` / `<code>` / `<textarea>` blocks entirely, and wraps the FIRST word-boundary occurrence of each of 9 new glossary terms — MOE, CV, DP, PUMS, allocation, swapping, imputation, variance replicate, differential privacy — in a new `.gloss-inline` span with a dotted underline + tiny superscript `?` mark. Uppercase-acronym terms require an uppercase match to avoid false positives (`MOE` matches "MOE" but not "moe" in "smoe"); multi-word terms are matched case-insensitively. Applied at render time to the Home panel and every kind/AM panel; first-occurrence per glossary term is shared with the manual `gloss()` calls via the pre-existing `_GLOSS_SEEN` set so a term auto-tooltipped in body copy won't get re-tooltipped in a header the manual call also produces.
- **Findings / decisions:** (1) **Before → after measurements.** LOC on `tools/product_scope.py`: **8,026 → 8,558** (+532 net across 3 commits: +91 for the counter helper + landscape rewiring, +268 for the Bureau feed section + fetcher + parser + CSS, +173 for the body-gloss helper + inline CSS + expanded GLOSSARY). HTML output size: **5,150,932 → 5,160,038 bytes** (+9,106 bytes, +0.18% — modest since none of the three commits add per-card DOM, they add small always-visible sections + a few sparse tooltip spans). Regen wall time: **~1.4s → ~1.5s** (fetch attempt for the RSS costs ~1s on cold-cache when the network blocks it; drops to ~1.4s on cached-fresh path). (2) **Counter reconciliation numbers post-Phase A #1.** Grep-verified all three counters now show identical numbers: tier rail `pipeline-label-n` chips = `551/0/0/22`, Sankey `sankey-node-count` = `573→22→22→22` (cumulative form of the same 4-vec), landscape legend `lscape-legend-cnt` = `551/0/0/22`. Landscape summary line reads `Pipeline reach: 22 reviewed · 0 sampled · 22 advanced past cataloged (573 total)`. (3) **Canonical tier definition landed.** cataloged = default; probed = `path in product_probes and ok=True`; sampled = `path in scope_data_cache`; reviewed = `stage != "cataloged"` OR non-empty `uncertainty_metrics` OR non-empty `note` OR any entry in `insights`. Under the current review file this counts exactly the 22 backfilled uncertainty_metrics entries — the 5 FOCUS entries and 7 insight-carrying paths are all a subset of that 22. (4) **Bureau feed sandbox observation.** `urllib.request.urlopen("https://www.census.gov/newsroom/press-releases.xml", timeout=5)` returns `URLError <urlopen error Tunnel connection failed: 403 Forbidden>`. Same allowlist behavior as the earlier font-bundling attempts. The empty-state branch renders cleanly; Garrett should see the live-fetch branch on Windows where the outbound allowlist doesn't apply. Cache file `.census_press_cache.json` gitignored (24h TTL — regenerable on demand, no benefit to sharing another teammate's cache). (5) **Glossary body-tooltip landing spots.** After regen: 9 unique `.gloss-inline` spans in the HTML, one per glossary body term. Sampled contexts verified in prose (not URLs / JS strings / attribute values): `MOE` lands inside a curriculum concept blurb, `CV` inside a curated finding detail, `PUMS` inside the ACS PUMS card description as the acronym is being introduced, `DP` inside the "learn: pre-DP baseline" curriculum text, `allocation` inside the acs/acs5/subject blurb, `swapping` inside a DP-vs-pre-DP comparison, `imputation` inside a curriculum concept line, `differential privacy` inside the sampling-and-privacy mechanism list, `variance replicate` inside a hedged general-methodology sentence. (6) **Filesystem lock quirk persists.** All 3 commits went through the `/tmp/csrepo_git` scratch-dir workaround (`.git/index.lock` on the Windows-mounted volume is unwritable from the Linux sandbox). CRLF/LF churn on ~35 unrelated files left alone per standing protocol. **Do NOT push.** All 3 commits are local. (7) **Judgment call — `.gloss-inline` vs the standalone `.gloss` ? badge.** Kept the two variants as distinct CSS classes because they serve different reading modes: the standalone `.gloss` badge is a small orb after a header word (users hover it deliberately); the `.gloss-inline` span underlines the term itself with a tiny superscript so a reader scanning body copy can spot definable jargon without breaking the sentence flow. Both share tooltip styling (navy background, 280 px max-width, ::after positioning) so hovering feels the same in both places.
- **Files:** `tools/product_scope.py` (+532 net LOC across 3 commits: `_tier_counts` + `_has_review_signal` + landscape rewiring, Bureau feed section + fetcher + parser + CSS, `_gloss_body` helper + inline CSS + expanded GLOSSARY dict); `product_report.html` (regenerated, +9 KB); `.gitignore` (+1 pattern for `.census_press_cache.json`); `WORKLOG.md` (this entry). Commit hashes: `99e22d9` (#1 counter consolidation), `5dae5ff` (#2 Bureau feed), plus this #3 (glossary tooltips + WORKLOG).

### 2026-07-26 — Garrett Spangler — Product Scope Tracker: learning-focused UX pass (6 atomic commits)
- **Area:** tools
- **What was done:** Executed a 6-commit UX pass on `tools/product_scope.py` sourced from a research pass over award-winning data-heavy tools + design systems, filtered to items that serve LEARNING (reduce text-crawling, speed comprehension) rather than pure competition aesthetics. The commits, in order: **(#1) Type-system rip-replace.** Collapsed 21 hardcoded font-size values into the Radix Themes 9-step scale via CSS custom properties `--fs-1` .. `--fs-9` and paired `--lh-1` .. `--lh-9` (12/14/16/18/20/24/28/36/60 px). Weight ladder normalized to 400 body / 500 emphasis / 600 headline (300 and 700 banned). Three font-family tokens: `--f-sans` (body/UI), `--f-mono` (numbers/paths/code), `--f-display` (H1 / section titles / hero moments only). Headlines get `-0.02em` tracking at 24 px+ and `-0.03em` at 36 px+ (Vercel Geist convention). Font bundling could NOT land — every external source we tried is blocked by the sandbox allowlist (Google Fonts, Fontsource, jsDelivr, unpkg, raw.githubusercontent.com); rather than commit a broken font reference, the family tokens resolve to tight system-font stacks matching the DNA of IBM Plex Sans, IBM Plex Mono, and Fraunces. **(#2) Named-tier progress rail** replaces the pre-existing 3-bar coverage list on Home ("Has repo evidence: 6/573 · 1.0%" style) with a single horizontal segmented bar over four mutually-exclusive pipeline stages: cataloged → probed → sampled → reviewed. Segment widths are proportional to stage count; palette matches the landscape tier chips (grey / blue / teal / green). Every raw sub-25% percentage is gone — segment area is the percentage. **(#3) Sankey research pipeline** — inline SVG (Bezier ribbons + stacked-bar nodes) rendered ABOVE the landscape treemap on Home. Same four stages as the rail, drawn as flows: ribbon width = count that advanced to next stage. ~180 px tall, ~150 LOC of pure Python-to-SVG, no libraries. Click on a stage node jumps to the Products tab. Treemap stays below unchanged (different question: shape-of-space vs. pipeline-progress). **(#4) 5-product curriculum on Home** — hardcoded `CURRICULUM` constant of the 5 seeded FOCUS products walked in a specific order (acs/acs5 → dec/dhc → dec/das-demo → acs/acs5/subject → dec/sf1) so each product illustrates one uncertainty mechanism and each mechanism builds on the previous. Renders below the Phase 1 hero card. Every blurb is grounded in either the phase-1 findings report or the seeded FOCUS entries' `uncertainty_metrics` text — sources are documented inline in the constant (no fabrication). Broadened the existing `data-jump-path` click handler so the curriculum's "Open card →" CTAs use the same jump-to-Products behavior as "Recently touched." **(#5) Empty states as teaching moments.** Rewrote four visible empty states: (a) per-card insights feed (566 cards) — was "No notes yet on this product," now three Notion-style click-to-copy `--review --insight "..."` scaffolds pre-filled with question prompts ("What did the team learn?" / "What's the reliability caveat?" / "Where is this used at Census?"), plus a keyboard-shortcut footer; (b) per-card uncertainty_metrics empty state (551 cards) — was "not yet documented," now a copyable `--review --notes` scaffold with a starter paragraph prompt; (c) Home "What we've learned" no-team-notes — was a bare CLI, now a Linear-style keyboard cheatsheet (/, j/k, Enter, E) framed as "while you wait, learn the shortcuts"; (d) Home "Recently touched" no-signals — copyable button + keyboard hint. Every empty state now either teaches a shortcut, offers a specific action, or provides a starter scaffold. **(#6) Guided tour mode.** "Take the tour (2 min)" button in the freshbar launches a 7-stop overlay walkthrough of Home in order (Start-here → Findings report → 5-product curriculum → Where the team is (rail) → What we've learned → Research pipeline (Sankey) → Landscape treemap). Semi-transparent dim panels + gold halo around the current target + floating callout box (280 px wide, 1-line title + 2-sentence body + Prev/Next/Skip). Arrow keys navigate, Escape exits, Enter = Next. localStorage `product_scope:tour_seen` hides the "Take the tour" button after one full pass but leaves a small `?` re-entry visible. Pure vanilla JS (~200 LOC) + fixed-position DOM (~50 LOC CSS), no dependencies.
- **Findings / decisions:** (1) **Before → after measurements.** LOC on `tools/product_scope.py`: **7,008 → 7,868** (+860 net across 6 commits: +78 for CSS tokens + `--fs-N` migration, +184 for the pipeline rail (helper + CSS + Home rewrite), +267 for the Sankey (CSS + `build_sankey_pipeline` + JS click handler), +212 for the curriculum (CSS + constant + `build_curriculum_card`), +185 for empty-state teaching moments across 4 sites, +268 for the tour overlay + JS controller + freshbar launcher). HTML output size: **3.85 MB → 5.03 MB** (+1.18 MB, +30%). The bulk of the growth is the empty-state scaffold expansion: 566 cards × 3 click-to-copy scaffolds each = 1,698 new copyable command blocks × ~600 characters each ≈ 1.0 MB. The tour DOM adds ~1 KB; the Sankey ~2 KB; the curriculum ~1.5 KB. Regen wall time (`time python3 tools/product_scope.py`): **0.83s → ~1.4s** (still under 2 s; shell startup dominates the runtime beyond the pipeline itself). (2) **Font-size collapse.** Grep-verified: 151 hardcoded `font-size:` uses across 21 distinct values in the pre-commit source → 0 hardcoded values post-commit; 152 `var(--fs-N)` references in the generated HTML. Every line-height paired to a 4 px multiple; every weight normalized to 400/500/600. (3) **Font bundling: fallback documented.** External fetches to Google Fonts, Fontsource, jsDelivr, unpkg, and raw.githubusercontent.com all returned 403 blocked-by-allowlist. `--f-sans` / `--f-mono` / `--f-display` currently resolve to system-font stacks tuned for Plex-Sans-DNA, Plex-Mono-DNA, and Fraunces-DNA respectively. The token layer is in place; when network access lands, one `@font-face` block at the top of the `<style>` is the only change needed to bundle real WOFF2 files inline. **This is a known limitation to flag with the mentors if a future review pushes back on the visual — the DESIGN SYSTEM is correct, the FONT FILES are not yet inline.** (4) **Curriculum sourcing discipline (CLAUDE.md "never bluff" rule).** All 5 curriculum entries trace to either the phase-1 findings report sections OR the seeded FOCUS product_review entries' uncertainty_metrics text. Documented inline: acs/acs5 → FOCUS entry + Section 3, dec/dhc → FOCUS entry + Section 4, dec/das-demo → uncertainty_metrics + Section 4, acs/acs5/subject → FOCUS entry (allocation_source role) + Section 5, dec/sf1 → FOCUS entry (benchmark role) + Section 4. No fabricated blurbs. (5) **Sankey flow-count semantics.** In the current tool state (probed=0, sampled=0, reviewed=22), the reviewed products bypassed sampling and probing entirely — the uncertainty_metrics was backfilled from the phase-1 report directly. For the Sankey to render as a PIPELINE rather than as three disconnected puddles, `_sankey_cumulative_counts()` treats deeper tiers as having conceptually passed through the shallower ones. This is documented in the helper's docstring; the treemap below still uses the honest three-way partition. (6) **Judgment call — tour click target.** Sankey nodes and curriculum CTAs jump to the Products tab but don't apply a hard pipeline-stage filter — adding a 5th facet just for pipeline stage would be more mechanism than the click needs. The click is a discoverability hook, not a full facet integration. (7) **Filesystem lock quirk persists.** All 6 commits went through the `/tmp/csrepo_git` scratch dir workaround (`.git/index.lock` on the Windows-mounted volume is unwritable from the Linux sandbox). CRLF/LF churn on 35 unrelated files left alone per standing protocol. **Do NOT push.** All 6 commits are local.
- **Files:** `tools/product_scope.py` (+860 net LOC across 6 commits); `product_report.html` (regenerated, +1.18 MB); `WORKLOG.md` (this entry). Commit hashes: `87bd90d` (#1 type system), `469c6ad` (#2 named-tier progress rail), `5e74065` (#3 Sankey), `fee6afd` (#4 curriculum), `f93d765` (#5 empty-state teaching moments), plus this #6 (guided tour + WORKLOG).

### 2026-07-26 — Garrett Spangler — Product Scope Tracker: differentiation audit follow-through (3 atomic commits)
- **Area:** tools
- **What was done:** Shipped the three concrete asks that fell out of the recent differentiation audit on `tools/product_scope.py`. **(#1) Surface `docs/phase1-findings-report.md` prominently.** Added a gold-accented hero card at the top of the Home tab (above "Where the team is") with a book emoji, a plain-English pitch for the report, and a navy pill-button CTA to open it. Persists regardless of the Start-here banner state (banner lives outside the panel and hides itself via localStorage after one dismissal). Also added a small gold-tinted "Phase 1 report - Section X ->" footer link on the drill-down of the 7 cards whose products the report explicitly covers (acs/acs5, acs/acs5/subject, acs/acs5/profile, acs/acs5/cprofile, dec/dhc, dec/sf1, dec/das-demo), deep-linking to the relevant GitHub slugified section anchor. New `PHASE1_REPORT_PATH` + `PHASE1_REPORT_SECTIONS` constants at the top of the file so future teammates can extend the cross-link set with a single dict edit. **(#2) Link every card to data.census.gov as the primary "explore data" CTA.** New `_ql_dcgov_html()` render pinned to the very top of every Quick Look block. Navy pill-button labeled "Explore data on data.census.gov ->" with a short plain-English tagline explaining what the Bureau's interactive query tool actually does. Two-tier lookup: explicit `DCGOV_URL_BY_PATH` override (geo/tiger -> census.gov geographies page, dec/das-demo -> census.gov DAS reference page), else program-name -> `d=<...>` filter value from `DCGOV_PROGRAM_D_PARAM` (covers ~30 programs), else keyword-search `?q=<program>` fallback so every card gets a working link even for unmapped programs. **(#3) Backfill `uncertainty_metrics` on top ~20 products from findings report + hedged general methodology.** Filled 22 previously-empty `uncertainty_metrics` fields in `product_review.json`: 6 grounded in the phase-1 findings report (acs/acs5, acs/acs5/subject, dec/dhc, dec/sf1, dec/das-demo, acs/acs1 - each cites the report section) and 16 hedged general-methodology entries clearly marked `[general methodology - needs mentor review]` (acs/acs5/profile, acs/acs5/cprofile, acs/acs5/pums, acs/acs1/subject, acs/acs1/pums, dec/dp, dec/pl, dec/pes, geo/tiger, pep/population, pep/housing, popproj/pop, cbp, cre, sipp, pdb/blockgroup). Direct edit of `product_review.json` (schema already carried the field for all 573 products); FOCUS stage + composite-role metadata preserved unchanged on the 5 seeded products; `sort_keys=True + indent=2` matches the tool's own serializer so the diff stays clean.
- **Findings / decisions:** (1) **Before -> after measurements.** LOC on `tools/product_scope.py`: **6,747 -> 6,868** (+121 net across the hero card + dcgov helpers + `PHASE1_REPORT_*` constants + 3 new CSS blocks). HTML output size: **3,612,503 -> 3,852,730 bytes** (+240,227, +6.6% - most of the growth is 582 "Explore data" CTAs across the panels + 7 phase-1 cross-links + the hero card copy). Regen wall time (single run, `time python3 tools/product_scope.py`): **0.821s -> 0.883s** (still well under 1 s). `product_review.json` uncertainty_metrics coverage: **0/573 -> 22/573** (3.8%). (2) **Sourcing discipline for the uncertainty backfill (CLAUDE.md "never bluff" rule).** Every entry carries an explicit provenance tag at the end: `[Source: findings report Section X]` for the 6 report-grounded entries, `[general methodology - needs mentor review]` for the 16 hedged entries. Two entries (acs/acs1 and acs/acs5/profile) blend both: the primary sentence is grounded in the report, a secondary sentence adds general methodology hedged as such. Nothing was written from confident recall alone. Products where I was uncertain about the exact uncertainty surface (e.g., the Economic Census's noise-infusion parameters, SIPP's precise replicate-weight count in current vintages, the ABS's disclosure limitation approach) were skipped or hedged rather than backfilled with a false-precision sentence. The backfill list favors flagship products (all 5 FOCUS entries + the ACS + Decennial + PEP + PopProj families) that mentors are most likely to ask about; low-hanging-fruit long-tail products (ecn/*, cps/*, sipp waves, timeseries variants) were deliberately deferred - a mentor review pass can populate those with the correct discipline. (3) **Verification.** Regenerated the report after each commit; confirmed the 22 backfilled products now render their plain-English uncertainty sentence in the More-details drill-down (previously showed "not yet documented"); 551 unfilled products still correctly show "not yet documented" (573 - 22 = 551 exactly); the 5 FOCUS products retain `stage=focus` and their composite-role metadata; the hero card renders once on Home; the phase-1 cross-link renders on 7 cards (each x2 across the AM tab + Aggregate tab, so 14 occurrences in the HTML). (4) **URL slug rules for the phase-1 cross-links.** GitHub renders `.md` files with slugified header anchors: lowercase, punctuation stripped, spaces and em-dashes collapsed to `-`. Confirmed the target anchors (`#3-sampling-noise-the-law-of-shrinking-places-eda-0103`, `#4-privacy-noise-a-fixed-cost-that-small-places-pay-eda-04`) match what GitHub actually generates for the phase-1 report. If a reviewer opens `product_report.html` locally (no GitHub), the browser resolves the `#<anchor>` against the raw `.md` and simply lands at the top - graceful degradation. (5) **Filesystem lock quirk persists.** All 3 commits went through the `/tmp/csrepo_git` scratch dir workaround (`.git/index.lock` on the mount is unwritable). CRLF/LF churn on 35 unrelated files left alone per standing protocol. **Do NOT push.** All 3 commits are local.
- **Files:** tools/product_scope.py (+121 net LOC: `_ql_dcgov_html`, `_ql_catalog_detail_html` cross-link block, `build_phase1_hero_card`, `PHASE1_REPORT_PATH`/`PHASE1_REPORT_SECTIONS` + `DCGOV_*` constants, CSS blocks for `.home-hero-p1` / `.ql-d-p1link` / `.ql-dcgov`); product_review.json (22 `uncertainty_metrics` fields backfilled); product_report.html (regenerated, +240 KB); WORKLOG.md (this entry). Commit hashes: `52097d3` (#1 phase-1 report hero + cross-links), `151faee` (#2 data.census.gov CTA), plus this #3 (uncertainty_metrics backfill + WORKLOG).

### 2026-07-26 — Garrett Spangler — Product Scope Tracker: landscape treemap redesign (single commit)
- **Area:** tools
- **What was done:** Focused viz polish on Garrett's Product Scope Tracker landscape section. Baseline complaint was two-fold: (1) 20+ program boxes rendered as unclickable slivers with hover-only labels; (2) the 2x2 grid of mostly-grey kind panels read as a dry/flat "abandoned dashboard." This pass rebuilds `build_landscape_viz()` around a stacked full-width row per dataset kind. Each kind row is its own SVG with a header strip (kind name + product count + inline "top:" line naming the three largest programs) sitting above a squarified program treemap; below the SVG, an optional native `<details>` disclosure holds any programs that squarify + min-box floor squeezed out ("+ N more programs" chip that expands to a chip list). Row height is share-proportional (Aggregate 244 tallest, Uncategorized 1 slimmest) with an auto-grow loop that extends the row (up to a 340 px inner-h cap) until at most `LSCAPE_MAX_SPILL=2` programs would spill; a mild layout-only size compression (`sizes ** 0.65`) prevents SIPP (176/235 = 75% of Microdata) from squeezing singletons to invisible slivers while still visibly dominating its row. Every program box now renders a permanent SVG `<text>` label (program name + count) at an adaptive font size (12 -> 11 -> 10 -> 9 px based on box dimensions) with ellipsis truncation to fit box width — the min-box floor (76x30 px) guarantees the label fits legibly. Palette: kept grey/blue/teal/green tier scheme but bumped the three touched-tier saturations so they pop off the mostly-grey wall; added a subtle per-kind row background tint (warm beige for Aggregate, cool blue-grey for Microdata, soft green for Time series, neutral for Uncategorized) with a matching border and a 34x2-px accent underline beneath the kind name in the tint's darker step. Program boxes now round at 3 px, non-grey tiers get a slightly heavier stroke (1.25 vs 0.75) for extra pop, and a hover state deepens the stroke to 2 px. Legend refreshed with a boxed ice-blue background, filled swatches, and a bolded tier label; caption spells out the mild-compression choice and links to Bruls et al. 2000 for the layout algorithm. All existing click-to-drill and keyboard-nav (tabindex, role="button", Enter/Space) preserved; SVG stays self-contained (no D3, no external CSS/JS); delegated JS click handler unchanged.
- **Findings / decisions:** (1) **Before -> after measurements.** LOC on `tools/product_scope.py`: **6,441 -> 6,782** (+341, absorbed into two rewritten helpers + one new spill helper + the auto-grow loop; CSS block grew by ~30 lines). HTML output size: **3,600,836 -> 3,609,351** bytes (+8,515 bytes, +0.24% — the SVG grew per Garrett's expectation, but the redesign spends most of its budget on the always-on `<text>` elements it adds, not on structural bloat). Regen wall time (3 runs, `time python3 tools/product_scope.py`): **0.79s -> 0.83s** (still under 1 s; the auto-grow loop adds up to 6 squarify passes per kind row but each is O(N log N) on ~15-25 programs so total overhead is <30 ms). (2) **Label coverage.** Of 44 unique program boxes in the catalog, **39 render as always-on labeled SVG boxes** (88.6%) and **5 sit in the spill disclosures** (11.4%) whose "+N more" summary line still shows the first 3 spilled program names inline and whose click-to-expand reveals full clickable chips; nothing is hover-only anymore. Per-kind breakdown: Aggregate 21 visible + 4 spill (25 total), Microdata 5 visible + 0 spill, Time series 12 visible + 1 spill (13 total), Uncategorized 1 visible + 0 spill. **Smallest visible box: 72 x 50 px** (well above the 30-px legibility floor; the size-compression exponent + auto-grow combo means even the smallest program boxes are ~2.5x the min-box area). (3) **Total viz height.** Sum of the four kind-row SVG heights = **1,143 px** (was ~380 for the old flat treemap). This IS the "expand out the tree maps" Garrett asked for; row heights are 400 (Aggregate, capped), 322 (Microdata), 317 (Time series), 104 (Uncategorized). Adds ~2 monitor viewports of scroll on a laptop screen — acceptable given the tradeoff for legibility, but if it's too much we can lower `LSCAPE_ROW_MAX_H` or tighten `LSCAPE_ROW_BASE` in one commit. (4) **Visual sketch.** Reading top to bottom on the Home tab landscape section:
    ```
    +-----------------------------------------------------------------------+
    |  The Census data landscape                                            |
    |  <caption>                                                            |
    +-----------------------------------------------------------------------+
    | AGGREGATE TABLES              244 products (row inner_h 348, warm    |
    |  --  <tint underline>         · 25 programs   beige tint bg)         |
    |  top: Economic Census (80) · Decennial Census (55) · Pop Est (38)    |
    |  [Economic Census][ Decennial (blue)  ][ ACS ][Pop Est][Pop Proj]... |
    |  [ Curr Pop Survey ][ CBP ][ NBP ][ Ann Retail ]... more boxes ...   |
    |  <details>+4 more programs: acs5 . geoinfo . intltrade . ...</details>|
    +-----------------------------------------------------------------------+
    | MICRODATA                     235 products · 5 programs (cool blue-  |
    |  --                                          grey tint bg, inner 270)|
    |  top: SIPP (176) · CPS (32) · ACS PUMS (15)                          |
    |  [       SIPP (blue: has repo evidence)       ][ CPS ][ PUMS ][DHC]  |
    |  [ small progs get proper labels now, no slivers ]                   |
    +-----------------------------------------------------------------------+
    | TIME SERIES                   93 products (soft green tint, inner    |
    |  --                           · 13 programs  265)                    |
    |  top: International Trade (54) · ACS (12) · CBP time series (5)      |
    |  [ Intl Trade (blue) ][ ACS ][ CBP ts ]... 12 boxes total ...        |
    |  <details>+1 more program: ...</details>                             |
    +-----------------------------------------------------------------------+
    | UNCATEGORIZED                 1 product · 1 program (neutral, 52)    |
    |  --                                                                  |
    |  all: Geography boundary files (1)                                   |
    |  [                     Geography boundary files                    ] |
    +-----------------------------------------------------------------------+
    | [grey] untouched (554)  [blue] repo evidence (19)  [teal] fetched(0) |
    | [green] peeked (0)                Team reach: 19 of 573 · ...        |
    +-----------------------------------------------------------------------+
    ```
    (5) **Aesthetic notes.** Palette choices: kept the existing project design system (navy `#1F2A5C`, ice `#EEF2FA`, gold `#C9A227`, muted `#5A6072`); per-kind row tints picked to feel like paper-stock swatches — warm beige for Aggregate (reads as "authoritative tables"), cool blue-grey for Microdata (reads as "engineered records"), soft green for Time series (reads as "temporal"), plain neutral for Uncategorized. Tier fills bumped from the timid `#DCE7FA / #B8E0DC / #B7DEBB` triad to a more assertive `#B7CDF6 / #8BD3CC / #8FCD97` triad — still low-contrast enough to read as "family" of colors but the touched tiers now stand out clearly against the untouched grey. Stroke widths (0.75 grey vs 1.25 touched) give an extra dimension of pop for the ~19 blue boxes in the catalog. Rounded 3-px corners on program boxes + 10-px corners on row shells + native details disclosure for spill + light `box-shadow: 0 1px 2px rgba(31,42,92,0.05)` on each row = subtle depth without gradients. Legend upgraded from bare comma-separated to a boxed ice-blue chip strip with count-in-mono for each tier and a right-aligned team-reach summary. (6) **Judgment calls.** (a) *Size compression exponent 0.65:* strict area-proportionality (exp=1.0) was the ideological cleanest choice but produces unreadable singletons when one program (SIPP) is 75% of its kind. Tried 0.5 (sqrt, too flat), 0.7 (still 4 spilled per kind), 0.65 landed at 5 total spill. Documented the compression in the caption so a defense-of-methodology answer is trivial: "true product count on every label + tooltip; visual size is a monotonic weighted transform for readability." (b) *Auto-grow loop over static row height:* originally sized rows purely by share, which meant Microdata's ~5 non-SIPP programs still spilled because SIPP hogged the row area. The auto-grow loop retries squarify at 1.35x row height up to `LSCAPE_ROW_MAX_H=340` (inner) until spill drops below 2. Adds ~30 ms total wall time and keeps the numbers-guy in the room happy. (c) *Spill disclosure as `<details>` below the SVG, not embedded IN the SVG:* originally tried a fixed "+N more" chip at the end of the last row inside the SVG, but expanding it inline via `<foreignObject>` was fragile across browsers. Native `<details>` is accessible-by-default, works with JS off, and the summary line's inline preview of the first 3 spilled program names covers the "reader can see what got hidden" case without needing to click. (d) *Row height cap 340 px inner:* let this go higher and Microdata (SIPP + 4 tiny programs) still spills 0 at 320 px; kept the cap conservative to avoid the total viz height creeping past ~1200 px on a monitor. Two-monitor-viewports of scroll is where I drew the line; if Garrett wants tighter, drop the cap and accept 1-2 more spills per kind. (e) *No SVG drop shadows / filters:* considered `<filter id="dropshadow">` for depth but SVG filter bloat is real (~1 KB per filter def) and CSS `box-shadow` on the row shell gives the same visual affordance for free. (7) **Preserved without touching:** click-to-drill delegated JS handler (`data-landscape-prog` unchanged), keyboard-nav via `tabindex="0"` + `role="button"` on every program box AND every spill chip, native `<title>` tooltips with the exact same content, per-tier CSS classes (`.lscape-tier-{0,1,2,3}`), and the `_all_flex_fallback` + `_lscape_prog_fallback_html` safety nets for the pathological data-shape case. (8) **Filesystem lock quirk:** commit went through `/tmp/csrepo_git` scratch-dir workaround per the standing protocol (`.git/index.lock` on the mount blocks direct commits). CRLF/LF churn on 35 unrelated files left alone per instructions. Leftover `.git-backup-*` dirs cleaned. (9) **Do NOT push.** Local commit only.
- **Files:** tools/product_scope.py (+341 net LOC — new stacked-row `build_landscape_viz()`, rewritten `_lscape_prog_text_svg()` and `_lscape_kind_head_svg()`, new `_squarify_with_floor()` and `_lscape_spill_details_html()` and `_lscape_prog_box_svg()` and `_kind_slug()` and `_lscape_row_inner_h()` helpers, refreshed tier + tint palettes, refreshed CSS block); product_report.html (+8,515 bytes, regenerated); WORKLOG.md (this entry). Commit hash: this commit `<TBD>`.

### 2026-07-26 — Garrett Spangler — Product Scope Tracker: beginner-UX pass (5 atomic commits)
- **Area:** tools
- **What was done:** Ran a beginner-usability audit on `tools/product_scope.py` in the persona of a non-technical teammate (Andrew / Katie) opening the tool cold. Audit scored 4/10 — the primary loop of "look at a product, learn about it, record a thought" required the terminal at every step and used Census jargon (probe / sample / cataloged / actively managed) without ever defining it. Shipped 5 atomic commits to close the gap. **(#1)** Deleted the per-card `probe` checkbox + bottom-right `#qbar` queue slideout + the `--probe-queue`/`probe_queue.json` round-trip: click-a-box-then-download-a-file-then-run-a-different-command was two-and-a-half steps too many; the single-shot `python tools/product_scope.py --probe <path>` copy-button on each card is the one true path now. **(#2)** Added a first-visit **"Start here" banner** with the three things a cold reader needs to do (scroll to the landscape treemap, open a card's More Details, use a card's copy button), dismissible via `localStorage['product_scope:start_here_dismissed']` — writes once, never shows again on that machine, degrades gracefully in private mode. Same commit added inline `?` glossary tooltips on the first visible occurrence of each of six jargon terms (probe / sample / cataloged / actively managed / vintages / kind); pure-CSS hover popover, first-occurrence-only via a per-render tracker set. **(#3)** Renamed the visible surface from jargon to plain English while keeping internal enum values, JSON schema keys, and CLI flag names untouched (schema preserved, backward-compatible): "Cataloged" → "Listed only", "Actively managed" tab → "Team is working on", `probe`-flavored UI copy → "metadata fetched", `sample` → "data peek", "Vintages" → "Years published", "Kind" → "Dataset type", "Reviewer mode: only actively-managed products" → "Filter to only products the team is working on", "What the API reports" → "What the Census API says is in this product". **(#4)** Added a single **"Learn more about this product (1 command)"** button pinned to the top of every card's drill-down — one gold button that copies `python tools/product_scope.py --probe <id> --sample <id> --open` (fetches metadata + peeks at data + auto-opens the report in one invocation, no sequencing confusion). The old two-affordance empty state (separate probe + sample copy buttons) collapsed to just this one; individual probe / sample commands moved into a "More options for advanced users" collapsible. For non-API products, the button gets a subtle "(data peek not available — this product isn't available via the Census API)" note next to it. **(#5)** Added a **browser-only insight recording flow** via a "+ Add a note" button on every card. Click → JS builds a tiny plain-text stub in memory (with `product_id` / `who` header keys, comments, and a body prompt) → downloads as `note_<slug>_<timestamp>.txt` via `Blob` + programmatic anchor click. User edits the file, drops into `notes/` in the repo, next `python tools/product_scope.py` regen scans the folder, parses each note (comment-tolerant `Key: value` header with a blank line before the body — no PyYAML dependency, stdlib-only per the constraint), appends as a `source=human` insight, and moves the file into `notes/ingested/` so it doesn't re-ingest. Content-hash dedup on `(who, text)` blocks accidental re-inserts if the same file gets re-dropped. Added `notes/README.md` explaining the flow. This WORKLOG entry folds into commit #5 per the audit spec.
- **Findings / decisions:** (1) **Before → after measurements.** LOC on `tools/product_scope.py`: **5,982 → 6,440** (net +458 across 5 commits; #1 removed 79 lines of qbar/probe-queue plumbing, #2/#4/#5 added the banner + glossary + Learn-more + notes ingestion + CSS + JS). HTML output size: **3,050,725 → 3,600,602** bytes (+549,877 bytes, +18.0% — Learn-more block + Add-a-note button now render on every one of ~578 card renders, dominating the diff). Regen median wall time (3 runs, `python3 tools/product_scope.py`): **0.70s → 0.75s** (still under 1 s; notes/ ingestion adds one lock cycle + one directory scan, both < 5 ms in practice). `--help` flag count: **16 → 15** (`--probe-queue` gone; #4 and #5 both stayed within existing surface). (2) **Grep audit clean.** In `product_scope.py`: `grep 'qbar\|probe-queue\|probe_queue\|class="qbox"'` returns only 3 hits — one CSS comment (line 1966), one function name (`run_probe_queue` still used for `--probe`), and its call site — zero active DOM elements. In `product_report.html`: `<div id="qbar">` = 0, `class="qbox"` = 0, `Download queue` = 0. Post-#3 rename audit: "cataloged" as visible label = 0 (only `data-*` attribute values remain, which are internal enum keys); "Actively managed" visible = 0 (renamed to "Team is working on"); "Vintages" visible = 0 (renamed to "Years published"); "Kind" visible = 0 outside CSS class names / SVG group names / Census-authored catalog description text. (3) **Beginner-usability rescore.** Same 5-signal rubric as the audit baseline: (a) opens without error — YES (pre & post); (b) understand in 30 s — pre WEAK (no banner, no term definitions, tier chip jargon dominates), post STRONG (Start-here banner names the 3 first things; `?` tooltips on every jargon term; renamed labels read as sentences); (c) find something interesting in 3 clicks — pre WEAK (treemap + card + drill-down + a lot of jargon), post STRONG (treemap + card + Learn-more button now takes you all the way); (d) take one meaningful action without a terminal — pre FAIL (every action was a copy-command), post PASS (the "+ Add a note" flow lets a beginner record a thought browser-only, no terminal); (e) record own thought without help — pre FAIL (needed to know `python tools/product_scope.py --review <id> --insight "..."`; hostile to a non-programmer), post PASS (click, edit, drop, next regen). Composite: **4/10 → ~8/10** post-pass. Terminal is still required for actually running the regen (unavoidable — we deliberately don't want a browser writing JSON directly), but the loop of "learn + record" is now browser-first. (4) **YAML/TOML/JSON decision for note format.** Rejected PyYAML per the constraint (not stdlib). Rejected TOML (stdlib in 3.11+ but strict quoting rules would trip up a non-programmer editing by hand). Rejected JSON (comma / quote errors are punishing). Landed on **plain text** with a forgiving `Key: value` header + blank line + body, comment-tolerant (`#` anywhere). Documented at the top of every downloaded stub AND in `notes/README.md`. Parser resilient to leading comment blocks (a beginner following the stub literally has 5 comment lines before the first `product_id:`, which broke my first parser attempt — smoke-tested and fixed). (5) **Smoke-test evidence for the notes flow.** Dropped `notes/test_beginner_ux_flow.txt` with `product_id: acs/acs5`; regen ingested it (`[notes] ingested 1 note(s) from notes/ (acs/acs5)`), the insight appeared 5x in the rendered HTML (drill-down + AM panel + Aggregate panel), and the file moved to `notes/ingested/`. Re-dropped the same file to test dedup: parser saw the content match, printed `[notes] test_beginner_ux_flow.txt: duplicate (who + text already on acs/acs5); moving to notes/ingested/ without re-inserting`, and no duplicate insight was created. Test-generated file + insight cleaned up before the commit (insight removed from `product_review.json`; dev-only test files in `notes/ingested/` gitignored via `notes/ingested/test_beginner_ux_flow*` since the container's filesystem couldn't unlink them). (6) **Judgment call: keep `run_probe_queue()` alive after #1.** The function is still called for the `--probe <path>` CLI single-shot path (renamed callsite still passes a list of one). Deleting the function would have required inlining or renaming, both wider-scoped than a clean cut. Kept as-is with a comment; only the `--probe-queue` argparse + JSON round-trip disappeared. (7) **Judgment call: glossary tooltip placement.** Spec says "first occurrence" of six terms. Since "probe" first appears in the "Where the team is" coverage bar label ("Probed (API metadata)"), that's where the `?` sits. Same story for "sample" (Sampled bar), "actively managed" (AM tab button), "vintages" and "kind" (card TL;DR line 1), "cataloged" (STAGE facet value chip — which lives in the collapsed Reviewer facets group, so the tooltip is present but only visible once the reader expands that group). Per-render tracker `_GLOSS_SEEN` prevents 500 tooltips on one page. (8) **Judgment call: notes flow ingestion runs inside the review lock.** `ingest_notes()` is called inside `with_review_locked()` so a concurrent `--review` CLI invocation can't clobber the batch. Same pattern as `emit_auto_insights()`. (9) **Judgment call: `notes/` folder tracked, `notes/ingested/` tracked (via `.gitkeep`)**. Per spec, notes are a shared teammate contribution mechanism — the paper trail belongs in git. Added a placeholder `notes/ingested/.gitkeep` so the folder survives fresh clones. (10) **Filesystem lock quirk persists.** All 5 commits went through the `/tmp/csrepo_git` scratch dir + `rsync --inplace --whole-file` back to `.git` workaround. Leftover `.git-backup-*` dirs on the mount left alone per standing protocol. (11) **Do NOT push.** All 5 commits are local.
- **Files:** tools/product_scope.py (+458 net LOC across 5 commits — 5,982 → 6,440); tools/README.md (drop the `--probe-queue` clickable-box section, keep the `--probe <path>` single-shot); product_report.html (regenerated after every commit, +550 KB — Learn-more block on every card drives most of the growth); notes/README.md (new, explains the browser download-a-note flow); notes/ingested/.gitkeep (new); .gitignore (+3 lines for the dev-only smoke-test artifacts); WORKLOG.md (this entry). Commit hashes: `bc73e94` (#1 delete probe checkbox + probe_queue.json roundtrip), `95fb283` (#2 Start-here banner + glossary tooltips), `2cb31ba` (#3 rename jargon on visible surface), `ad24d67` (#4 single Learn-more one-command button), plus this #5 (notes/ flow + WORKLOG).

### 2026-07-26 — Garrett Spangler — Product Scope Tracker: discovery-first reframe + team onboarding walkthrough
- **Area:** tools
- **What was done:** Session wrap. Shipped 30+ atomic commits over the day reshaping `tools/product_scope.py` from a "which products belong in the composite score?" decision tool into a discovery-first browser for the Census data landscape. Discovery-goal audit closed at **8.80/10** (up from 3.6/10 baseline). Composite-role CLI removed (`--role` / `--note` / `--author` gone; schema preserved on read so the 5 seeded FOCUS entries still round-trip); three new Home sections landed (**Where the team is** coverage bars + recently-touched, **What we've learned** aggregated findings feed, and an SVG squarified treemap of all 573 products); keyboard-first triage added (`/` `j`/`k` `Enter` `E`); URL hash filter state for shareable views; `--open` browser autoload after any regen. Day closed with a hygiene pass (`.env.example` template, `Unflagged`→`Uncategorized` rename, "Do I need a Census API key?" README section) and this packaging pass (audit `tools/README.md`, add pointer to the tool from the repo `README.md`, land this entry). Ready for the team to actually use.

  **Onboarding walkthrough — pin this if you're opening the tool for the first time.**

    1. `git pull` on this branch.
    2. `pip install -r requirements.txt` (or add `--break-system-packages` if you'd rather skip a venv — `product_scope.py` itself is stdlib-only, so the deps only matter for the notebooks).
    3. First run: `python tools/product_scope.py`. Auto-detects the repo root, crawls the Census catalog if `scope_field_cache.json` isn't already cached, regenerates `product_report.html` in under a second, prints the path.
    4. Open the report: `start product_report.html` on Windows, `open product_report.html` on macOS, or add `--open` to step 3 and it launches your browser as soon as the write finishes.
    5. What to look at first — Home tab has three sections stacked top to bottom: **Where the team is** (coverage stats + recently touched products), **What we've learned** (curated findings + WORKLOG mining + human insights), **The Census data landscape** (SVG treemap of all 573 products organized by kind; box size = product count, color = team reach). Click any treemap box to jump straight to that program's cards.
    6. Browsing — Products tab has 4 kind panels (Aggregate / Microdata / Time series / Uncategorized). `/` focuses the filter, `j`/`k` steps between visible cards with wrap-around, `Enter` opens the focused card's Quick Look drill-down, `E` toggles that drill-down's More Details. Sidebar facets filter live.
    7. Going deep on one product — `python tools/product_scope.py --probe acs/acs5 --open` fetches its variable + geography metadata and regens with the fresh probe. `--sample acs/acs5 --open` fetches actual data rows and runs canonical EDA (dtype, missingness, geography breakdown, sparklines).
    8. Recording insights — `python tools/product_scope.py --review acs/acs5 --insight "This looks like it covers X well" --open`. Auto-regens + opens. `who` attribution comes from `git config user.name`, no flag needed.
    9. API key — **not required** for the basic loop (regen, probe, review, one-off `--sample`). Only needed for heavy `--sample` batches (>500 req/day/IP). See `.env.example` + `tools/README.md`'s "Do I need a Census API key?" section if you want to configure one.
- **Findings / decisions:** **Known limitations.** (1) Composite work demoted, not deleted — schema (`composite_role`, `composite_role_note`) still round-trips in `product_review.json`, `--review --status` still moves stage, role chips still render on FOCUS drill-downs; only the `--role` / `--note` / `--author` CLI flags and the composite-role sidebar facet are gone. Re-elevate in one commit if the team returns to composite framing. (2) All 573 catalog products live under the "Show all products" toggle (Reviewer mode) — default view opens on the 4 kind panels, not the 5-card actively-managed slice. (3) WORKLOG mining occasionally over-includes process notes; a stat-carrying heuristic drops the worst offenders but not all. (4) Non-API products (DAS demo files, TIGER shapefiles) display in the catalog but `--sample` skips them cleanly with a `not sample-able via API` message rather than erroring out. **Follow-up asks — teammates, this is where you come in.** The two audit ceilings still parked at 7/10 (Discoverability and Shared Understanding) unlock the moment teammates start probing / sampling products and logging their own insights. Try `--probe <your-favorite-dataset>` on something you're curious about. Try `--sample --open` on any ACS or DHC variant. Log a `--review --insight` for anything you notice. And if a product family shows `Kind: Uncategorized`, that's a tool fallback because Census's `data.json` doesn't carry `c_isAggregate` / `c_isMicrodata` / `c_isTimeseries` for that record — flag it and I'll add a manual override in `PROGRAM_PREFIXES`.
- **Files:** tools/product_scope.py (5,982 LOC after the session), tools/README.md (audited + filled gaps this pass), README.md (added the Product Scope Tracker pointer), product_report.html (3.05 MB, regenerated per commit), product_review.json (5 seeded FOCUS entries untouched), .env.example (added earlier today), WORKLOG.md (this entry). Session commits: 30+ atomic. Today's packaging commits: `4aaeac6` (tools/README audit), `34d55a3` (root README pointer), plus this WORKLOG commit.

### 2026-07-26 — Garrett Spangler — Repo hygiene pass: key-exposure audit + Uncategorized rename + API key story (3 atomic commits)
- **Area:** tools
- **What was done:** Small hygiene pass on Garrett's Product Scope Tracker. **Phase 0** ran a key-exposure audit across every tracked file: grepped for 40-char hex, `CENSUS_API_KEY=<value>` assignments, `?key=` / `&key=` URL fragments, and Python-style `api_key=<value>` literals. Verdict CLEAN — the only 40-char hex hits in tracked files were git commit SHAs (`data-head-sha` in `product_report.html`, `head_sha` in `.product_scope_last_run.json`, which is untracked anyway); `.env` is correctly gitignored and untracked (`git check-ignore .env` matches, `git ls-files | grep '\.env$'` returns only `.env.example`); the real key lives only in the local `.env`. **Commit 1** refreshed `.env.example` to give teammates a clearer story about when the key is optional vs. needed. **Commit 2** renamed the fourth kind bucket `Unflagged` → `Uncategorized` across `tools/product_scope.py` (KINDS list, KIND_BLURB text, `_kind_of()` fallback, geo/tiger FAMILIES stub, landscape-viz header + fallback comments) and `tools/README.md`; regenerated `product_report.html` (SVG treemap label, fallback chip, kind panel). No `product_review.json` migration needed — `kind` isn't stored in the review schema. **Commit 3** added a "Do I need a Census API key?" section to `tools/README.md` (right after the two-command onramp) explaining that anonymous access covers the basic workflow and the key is only needed for heavy `--sample` batches, plus a `No API key required` one-liner at the top of the Try-it section, plus a friendlier two-line stdout note in the `--sample` path when no key is set (only fires on `--sample`, not on every regen).
- **Findings / decisions:** (1) **Audit clean.** Zero secret-shaped values in any tracked file. Real key present only in `.env`, correctly gitignored, correctly untracked. `.env.example` already existed as a placeholder (refreshed to new content this pass, no leak risk in the diff since both old + new are placeholders). (2) **Rename verification.** Grep `Unflagged` in `tools/product_scope.py` + `tools/README.md` = 0 hits post-rename. Grep in regen'd `product_report.html` = 0 hits (previously 4 hits in SVG label + fallback chip + 2 kind-panel `Kind Unflagged` chips). Historical WORKLOG.md narrative text mentioning "Unflagged" left as-is per instructions (mined feed treats it as historical). (3) **Missing-key message scoped correctly.** The new two-line stdout note lives inside `run_sample()` next to the existing `[sample]` header, so it fires only when `--sample` is invoked without a key. Every-regen output stays clean. (4) **HTML size delta.** `product_report.html`: 3,045,787 → 3,049,668 bytes (+3,881 bytes, +0.13% — trivial). Regen wall time flat (single run, ~0.7s). LOC on `tools/product_scope.py`: 5,977 → 5,983 (+6 for the two-line print block). (5) **Filesystem lock quirk persists.** All 3 commits went through the `/tmp/csrepo_git` scratch-dir workaround (`GIT_DIR=/tmp/csrepo_git`, then `rsync -a --inplace --whole-file` for objects/refs/logs, `cp` for HEAD/index/COMMIT_EDITMSG back to the mount `.git`). Leftover `.git/index.lock` on the mount is harmless and left alone. (6) **Do NOT push.** All 3 commits are local.
- **Files:** .env.example (4-line placeholder refresh), tools/product_scope.py (+6 LOC net — 8 rename edits + 6 for the missing-key note), tools/README.md (Uncategorized rename in tabs paragraph + new "Do I need a Census API key?" section + one-liner in Try-it section), product_report.html (regenerated), WORKLOG.md (this entry). Commit hashes: `da8c804` (env: .env.example refresh), `90f2738` (product_scope: Unflagged → Uncategorized), plus this commit `<TBD>` (README key story + friendlier missing-key note).

### 2026-07-26 — Garrett Spangler — Product scope tracker: ceiling-push pass (3 atomic commits)
- **Area:** tools
- **What was done:** Same-day follow-on to the discovery-mode cuts. The post-cuts reaudit came in at composite 7.95/10; three dimensions had clear upside if we shipped the deferred features they were waiting on: **Landscape awareness** wanted a real area-accurate treemap (not the flex-basis approximation); **Triage speed** wanted keyboard-first navigation + shareable URLs; **Signal-to-noise** wanted a tighter "What we've learned" feed. This pass ships all three as atomic commits. **(#1)** Replace `build_landscape_viz()`'s flex-basis nested-boxes layout with a proper SVG squarified treemap (Bruls, Huijsen & van Wijk 2000). Two nested partitions: outer 4 kind rectangles sized to product-count share (Aggregate 244/573 = 42.6% area, Microdata 235 = 41.0%, Time series 93 = 16.2%, Unflagged 1 = 0.17%), inner program rectangles area-proportional inside their kind. Color = 4-tier reach palette (grey untouched, blue repo-evidence-only, teal probed, green sampled). SVG `<g>` groups carry `tabindex="0"` + `role="button"` for keyboard drill-in; native `<title>` for tooltips; Enter/Space also triggers the drill. Graceful fallback to a flex row when a kind is too small to fit inner boxes (Unflagged singleton case) — no stderr warning for that expected case; stderr warn only on genuine data-shape failure. **(#2)** Triage keyboard-nav + URL hash filter state. `/` focuses the active panel's text filter; `j`/`k` cycles focus through visible `.prod` cards with wrap-around (each card now has `tabindex="0"`); `Enter` on a focused card toggles its Quick Look drill-down. State (active tab, text query, per-facet selections, AM toggle) serializes to `location.hash` as URLSearchParams (`#tab=k0&q=income&f_kind=Aggregate&am=1`); on load, hash overrides localStorage for any key it specifies. All three additions are progressive enhancement — JS off leaves native anchor scroll, native `<details>` toggle, and localStorage-only filter state intact. **(#3)** Tighten `mine_worklog()` with a stat-carrying heuristic (score = number-with-unit matches capped at +3 + unique stats vocab terms + finding-shaped leading verbs + process-note penalty; zero-or-negative rows drop entirely), and cap the "Show all" drawer at 30 mined+auto items. Curated FINDINGS (12) never drop from view; the cap applies only to mined+auto rows, which flood the drawer without it.
- **Findings / decisions:** (1) **Before/after measurements (mount HEAD `317e207` → this commit).** LOC on `tools/product_scope.py`: 5,285 → ~5,700 (+415 across 3 commits; #1 added ~300 for squarify + SVG render, #2 added ~120 for JS + facets restore, #3 added a few net after the heuristic). HTML output size: 3,088,126 → ~3,120,000 bytes (+30 KB; SVG treemap +12 KB, tabindex + JS bundle +21 KB, capped WWL saved ~2 KB). Regen median (3 runs): 0.71-0.73s (flat). `--help` unchanged at 16 flags / 54 lines. (2) **Treemap area verification.** Outer partition: Aggregate 161,804 px² (target 161,780), Microdata 155,838 (target 156,035), Time series 61,664 (target 61,675), Unflagged 656 (target 663) — all match within 0.2%. Inner top-3 boxes by area: SIPP (Microdata, 176 products) 107,104 px²; Economic Census (Aggregate, 80) 48,573 px²; Decennial Census (Aggregate, 55) 33,307 px². SIPP visibly the biggest single box, matching its 30.7% share of the catalog. 43 SVG program boxes + 1 fallback chip (Unflagged singleton) = 44 total, matches spec. (3) **WORKLOG mining reduction.** Pre-#3: 158 items across 29 entries fed to WWL feed indiscriminately, flooding the Show-all drawer. Post-#3: 60 mined items across 24 entries (zero-score rows dropped as pure process notes); combined with 12 curated + 8 auto (6 auto:divergence + 2 auto:cache_diff), total 80 entries. Rendered: top-of-fold 15 (12 curated + 3 highest-scoring non-curated); Show-all drawer 27 more non-curated (SHOWALL_CAP = 30 - 3 already visible); footer says "38 more not shown" with link to WORKLOG.md. Total visible in expanded state: 42 items, was 180. (4) **Filesystem lock quirk persists.** All 3 commits went through the `/tmp/csrepo_git` scratch-dir workaround (init once by copying original `.git`, run `git commit` with `GIT_DIR=/tmp/csrepo_git`, sync refs + index + logs + new objects back with `rsync -a --inplace --whole-file --ignore-existing`). Leftover `.git/index.lock` on the mount is harmless — git read ops work; git write ops all route through the scratch dir. (5) **Do NOT push.** All 3 commits are local.
- **Files:** tools/product_scope.py (net +415 LOC across 3 commits), product_report.html (regenerated), WORKLOG.md (this entry). Commit hashes: `8b2a430` (#1 SVG squarified treemap), `e9996df` (#2 keyboard nav + URL hash filter state), plus this #3 (WORKLOG mining heuristic + Show-all cap).

### 2026-07-26 — Garrett Spangler — Product scope tracker: discovery-mode cuts (6 atomic commits)
- **Area:** tools
- **What was done:** Follow-up to the same-day explore-first reframe. That pass built the new discovery surfaces (Where the team is, What we've learned, Census landscape) but left the reviewer-mode / composite-role scaffolding running underneath in case the framing had to snap back. It didn't — the reframe held — so this pass cuts the leftover baggage and folds in the two enhancements the reaudit called out. Six atomic commits, ordered demotions first / additions second so the tool renders cleanly at every step: **(#1)** Drop the `Agency U.S. Census Bureau` chip from every card's TL;DR — every one of the 573 catalog entries has the same agency, so the chip was 573× pure noise. **(#2)** Remove the composite-role CLI surface (`--role`, `--note`, `--author` argparse flags; the interactive TTY prompt for `--role` missing `--note`; the "Composite role" sidebar facet) and the associated write-time validators. Schema-read of `composite_role` / `composite_role_note` is preserved, so the 5 pre-existing FOCUS entries still round-trip cleanly and still surface their role chip in the drill-down. **(#3)** Delete `_build_reviewer_mode_details()` and its `<details>Reviewer mode data</details>` wrapper at the bottom of Home — funnel read `568 → 0 → 0 → 5 → 0` (depressing without being informative), and the notebook-health table duplicated signals already carried elsewhere. Also deleted the orphaned CSS (`.funnel`, `.fstep`, `.farrow`, `.f-cand`, `.f-focus`, `.rev-mode-details`, `.rev-mode-body`). **(#4)** Suppress the "Review status: Cataloged" chip row in card drill-downs when the product is at `stage=cataloged` AND has no composite role — was rendering on all 573 cards; now renders only when there's an actual review signal (5 FOCUS cards, or any product with a declared role). **(#5)** Render the 12 curated `FINDINGS` inline in each matching card's drill-down insights feed, not just in Home's "What we've learned". Ordering per spec: curated (pinned pushpin icon, editorial synthesis) → human insights (newest first) → auto-insights. **(#6)** Landscape treemap widths: swap the Python-clamped `flex-basis` for a CSS `clamp(60px, {pct * 10}px, 400px)`. Multiplier tuned so the biggest program in each kind hits or approaches the 400px ceiling while the smallest stays at the 60px legibility floor — visible ratio of ~5-6× between biggest and smallest programs.
- **Findings / decisions:** (1) **Before/after measurements (mount HEAD `f33b490` → `1c85e36` before this final commit).** LOC on `tools/product_scope.py`: 5,369 → 5,241 (-128, net cuts). HTML output size: 3,240,223 → 3,181,213 bytes (-59 KB from #3 dominating). Regen median (3 runs): 0.74s → 0.70s (flat). `--help` line count: 62 → 54 (-8). `--help` flag count: 19 → 16 (-3, dropped `--role` / `--note` / `--author`). (2) **Grep audit clean.** In regen'd `product_report.html`: `Agency U.S. Census Bureau` = 0 hits (was 573 in TL;DR before #1). "Reviewer mode data" text = 0 hits in rendered HTML (1 hit remains in the Python source as a historical CSS comment). `Composite role` = 0 DOM hits (2 hits remain in Python source: one in a stale docstring, one in a comment describing the reframe rationale). "Review status" = 10 hits (was 573; now 5 FOCUS × 2 renders in AM panel + main Aggregate panel). (3) **FOCUS preservation verified.** All 5 FOCUS entries (`acs/acs5`, `acs/acs5/subject`, `dec/dhc`, `dec/sf1`, `geo/tiger`) still render their stage chip (`>FOCUS<` × 13) and their role chip (`CV source`, `Allocation source`, `Privacy noise`, `Benchmark`, `Geometry` × 2 each) in drill-downs. `product_review.json` untouched — 5 focus entries with role + note intact. (4) **Judgment call on `auto:divergence` collector:** kept. The collector emits on `composite_role` state transitions, not on a CLI action, so removing the CLI setters didn't require touching the collector. Post-cut, no new divergence events can fire from CLI (`--role` is gone), but if a role is edited manually into `product_review.json` the collector still fires and the row lands in the "What we've learned" feed. Small, self-contained, safely inert — cost of removing it is higher than cost of leaving it dormant. (5) **Curated-findings inline count:** 22 curated rows now render on cards (10 `acs/acs5` findings × 2 panels + 2 `dec/das-demo` findings × 1 panel), plus the same 12 curated headlines still in Home's "What we've learned" — total 34 pushpin icons in the regen. (6) **Landscape treemap width spot-check:** biggest program per kind gets a weighted-px value that hits or exceeds 400 (Aggregate / Economic Census 327, Microdata / SIPP 748, Timeseries / International Trade 387, Unflagged / Geography-boundary-files 1000 all get clamped to 400 by CSS). Smallest programs get 4px weighted → clamped to 60px floor. Visible ratio ≈ 5.4-6.7×. (7) **Filesystem lock quirk persists.** All 6 commits went through the `/tmp/csrepo` scratch-clone workaround. Six new `.git-backup-*` dirs to clean per standing protocol; leftover `.git/index.lock` on the mount is harmless (git ops all run against the scratch clone). (8) **Do NOT push.** All 6 commits are local.
- **Files:** tools/product_scope.py (-128 net LOC), product_report.html (regenerated, -59 KB), WORKLOG.md (this entry). Commit hashes: `c71f6ae` (#1 Agency chip drop), `a6812c6` (#2 composite-role CLI + facet), `5f77dd5` (#3 delete `_build_reviewer_mode_details()`), `a086e8e` (#4 hide "Review status" row for cataloged), `1c85e36` (#5 curated FINDINGS inline on cards), plus this #6 landscape widths + WORKLOG commit.

### 2026-07-26 — Garrett Spangler — Product scope tracker: explore-first reframe (6 atomic commits)
- **Area:** tools
- **What was done:** Substantial reframe of `tools/product_scope.py` after the tool had drifted so far into "which products belong in the composite score?" framing that even the composite-role vocabulary was confusing to teammates. Primary UX is now **explore-first**: a cold teammate opens the report and immediately understands (a) how much of the ~573-product catalog the team has actually reached into, (b) what the team has already learned, and (c) how the whole landscape is organized. The review workflow (candidate/FOCUS stages, composite roles, --review CLI, faceted status filters) is preserved everywhere it existed — schema unchanged, CLI flags unchanged, cards still carry every chip — but demoted from the primary surface. Six atomic commits. **(#1 Home: "Where the team is")** Replaces the funnel + curated-findings-first hero with a two-part section: three coverage bars (Has repo evidence, Probed, Sampled) colored green/amber/red by percentage of catalog reached, plus a "Recently touched" list of the top ~10 products the team has actually engaged with (ranked by tier: sampled > probed > repo evidence, then by newest timestamp). Every recently-touched row is a jump link into the Products tab card. The pre-reframe funnel + inventory + work-depth + notebook-health + WORKLOG hero move into a collapsed `<details>` at the bottom labelled "Reviewer mode data". **(#2 Home: "What we've learned")** New section aggregates every insight source in one feed: 12 curated FINDINGS + 137 mined WORKLOG findings + 6 auto:divergence + 2 auto:cache_diff + 0 human insights (with a friendly empty-state pointing at `--review --insight`). Legacy `auto:repo` entries are excluded (they duplicate WORKLOG text — audit cut them from generation two passes ago, this stops surfacing them alongside real WORKLOG entries too). Sort priority: curated → human → divergence → cache_diff → worklog, then reverse-chronological within group. Visible cap of 15 items; the remaining 142 sit behind a "Show all (157)" toggle. **(#3 Home: Census data landscape viz)** Interactive hierarchical treemap-style boxes below the two feed sections: 4 kind panels (Aggregate / Microdata / Time series / Unflagged) each subdivided into program boxes sized proportionally to product count and colored by max team-reach tier (grey untouched / blue reached / green sampled). Each box carries `title=` hover tooltip with product/touched/sampled counts and clicks jump to the matching Products tab with the filter input pre-populated to the program name. Self-contained CSS grid + flex sizing — no D3, no CDN. Rendered 4 kind boxes with 44 program boxes total; today 41 are grey, 3 are blue (ACS, Decennial, Geography — the six tracked products), 0 are green (no samples cached). **(#4 Products: flip default + facet reorganize)** Inverted the actively-managed default: pre-reframe opened showing 5 AM cards on the AM tab and hid the 4 kind panels; post-reframe opens showing all 572 catalog cards across 4 kind panels and hides the AM tab. Renamed the CSS class from `.am-showall` to `.am-reviewer` and inverted its polarity so the new default state has no class set. localStorage key preserved (`product_scope:show_all_products`) with a one-time migration flag (`product_scope:show_all_products:migrated_v2`) that flips returning readers' stored value so their previous view is preserved across the reframe. Toggle label + hint text renamed to "Reviewer mode: only actively-managed products (5 of 573)" — the toggle now advertises entry INTO the smaller workflow, not out of it. Facet sidebar reorganized: primary block always visible carries Family / Agency / Frequency (new — bucketed from vintages: annual / decennial / multi-year / single vintage / time series); secondary block collapsed behind a "Reviewer mode facets" `<details>` carries Status / Composite role / Has repo evidence / Has API probe / Notebook validated. Geography-level facet skipped for now with a `# TODO` comment — the catalog's spatial field is mostly just "United States"; needs probe data to be trustworthy. **(#5 Cards: TL;DR reframe)** Product card Quick Look lines reordered around "what it is" / "what's inside" / "description" instead of "tier chip / stage chip / role chip / probe metadata". New line 1 ("What it is") shows Family · Agency · Kind · Vintages · Spatial (catalog-only, always visible, zero API cost). New line 2 ("What's inside") shows Geography (populated counts if sampled, declared levels if probed, "not yet probed" fallback otherwise) plus probe counts and sample shape when cached. Cache-tier chip (Cached: catalog/probe/sample) shrunk to a subtle trailing marker at the end of the "What's inside" line — kept for readers who've learned the color scheme, but no longer the visual anchor. New line 3 pulls the catalog `description` up from the drill-down (~200-char truncation) so a reader sees WHAT THE PRODUCT IS without expanding anything. Stage chip + Composite role chip demoted from the pcard mini + Quick Look header into the top of the drill-down as a "Review status" row. pcard mini now carries only workchip (Not started/Identified/Pulled/Analyzed/Validated) + insight count — the discovery-oriented signals. **(#6 README + WORKLOG)** README onramp rewritten from "Decide whether a Census product belongs in the composite" / "Try it in 3 commands" (which led with `--review acs/acs5 --status focus --role cv_source`) to "Explore Census data products" / "Try it in 2 commands" (leading with `python tools/product_scope.py` + `start product_report.html`). Review helper mentioned but as a follow-up affordance, not the primary verb. Reference material below the horizontal rule kept intact. This WORKLOG entry lands in the same commit.
- **Findings / decisions:** (1) **Baseline vs. after (across all 6 commits).** LOC on `tools/product_scope.py`: 4,381 → 5,369 (net +988 across 6 commits — this is a reframe pass, not a simplify pass; features added include the coverage-bar helpers, recency-signals compute, the aggregated-insights feed, the landscape treemap builder, the facet-primary/reviewer split, and the reframed TL;DR line helpers). HTML output size: 2,833,073 → 3,233,145 bytes (+400,072 bytes, +14.1%). The bulk of the size increase is Line 3 of the TL;DR (~300-char catalog description on each of 573 cards ≈ 172 KB) plus the aggregated-insights feed (157 rows × ~500 bytes ≈ 78 KB) plus the landscape treemap (44 program boxes × ~200 bytes ≈ 9 KB) plus the "Where the team is" section (~10 KB). Regen median wall time (3 runs each): 0.73s → 0.68s (essentially flat — no new subprocess calls, no new API traffic; the new mtime-based recency signals hit stat() on ~50 evidence files which is <10 ms total). Combined `--review + auto-regen` end-to-end: 0.65s. `--help` flag count: 19 → 19 (unchanged — schema and CLI preserved). `--help` line count: 62 → 62 (unchanged). **Composite delta**: +988 LOC / +400 KB HTML / flat regen time / 0 flag changes / 0 schema changes. (2) **Cold-teammate verification.** Regen'd HTML, opened panel-home, spot-checked structure: `Where the team is` section has 3 coverage bars (evidence 6/573 red, probed 0/573 red, sampled 0/573 red) followed by "Recently touched" list of 6 products (all tier-0 repo, since no probes/samples cached). `What we've learned` section follows with 15 visible + 142 collapsed (157 total = 12 curated + 137 WORKLOG + 6 divergence + 2 cache_diff + 0 human, with the "No team notes yet" empty-state prompt visible). `The Census data landscape` section follows with 4 kind boxes containing 44 program boxes total (3 blue, 41 grey — no green yet because no samples cached). `Reviewer mode data` collapsible sits at the very bottom containing the pre-reframe funnel + inventory + work depth + notebook health. (3) **Products tab default flip verification.** With a clean localStorage (`product_scope:show_all_products:migrated_v2` unset), page loads with body NO `.am-reviewer` class; 572 cards visible across 4 kind panels. Ticking the toggle flips to `.am-reviewer` on body + panels, kind tabs hide, AM tab appears with 5 cards. Ticking untick returns. (4) **Card TL;DR verification on acs/acs5.** Line 1 renders "Family: American Community Survey · Agency: U.S. Census Bureau · Kind: Aggregate tables · Vintages: 2009–2024 · Spatial: US". Line 2 renders "Geography: not yet probed · [catalog marker]". Line 3 renders the catalog description (truncated to ~200 chars). Insights TL;DR follows with the two auto:divergence signals. Drill-down carries the demoted "Review status: FOCUS + CV source" chip row at the top. Mini on pcard shows only "Validated + 10 insights" — no stagechip, no rolechip. (5) **Grep audit clean.** In `product_report.html`: `am-showall` remains at 4 hits, ALL in WORKLOG-mined text or in the code's own migration comments (0 actual DOM references — everything active uses `.am-reviewer` now). `recipe|composite reliability|FOCUS pipeline` counts in the Home tab render are: recipe=0, composite reliability=0 (present only in the demoted "Reviewer mode data" section and inside individual cards' drill-downs, per spec), FOCUS pipeline=0. (6) **Backward-compat verification.** `python tools/product_scope.py --review acs/acs5 --insight "reframe smoke test"` writes correctly (stamped `who="Garrett Spangler"` from git config, `last_reviewed_date=2026-07-26`); insight was reverted after test. `product_review.json` schema unchanged: 573 entries, 5 FOCUS entries preserved, 44 total insights before/after. `--probe X` and `--sample X` code paths untouched. FOCUS entries render in both the demoted "Reviewer mode data" section on Home (via the funnel counts) and in their cards' drill-down "Review status" row. (7) **Judgment call: keep FOCUS/composite machinery visible in card drill-downs.** Spec said "Composite role chip → moves to More Details" — I put it in a small "Review status" row at the TOP of the drill-down body (with the demoted stage chip), so a reviewer clicking through gets those signals up front without them dominating the browse view. (8) **Judgment call: hierarchical viz shape.** Went with the "nested boxes with proportional flex-basis" approach rather than a true SVG treemap. Reasoning: 44 boxes total across 4 kinds, biggest is 176 products (SIPP) → 176/244 ≈ 72% of Microdata → too skewed for a satisfying rectangle-packing treemap; grid layout with proportional flex-basis reads more naturally and works with tiny programs (min-width floor keeps 1-product programs legible). Colored by max reach tier so grey slices immediately communicate "nobody's been here yet" — the visual message the spec called out as the goal. (9) **Judgment call: coverage bar thresholds.** Green ≥20%, amber 5-20%, red <5%. With 6/573 (1.05%) all three of our current bars are red — that's the honest picture and the color signals correctly that this is early. When probes/samples land the amber/green will show up. (10) **Judgment call: "What we've learned" cap at 15 visible.** Any lower makes the section feel thin; any higher makes it dominate the Home tab. The "Show all" details is there for reviewers who want the full paper trail — and at 157 total items, showing all inline would push the landscape viz and reviewer-mode content way below the fold. (11) **Judgment call: keep workchip visible on pcard mini.** Spec said demote stage chip, composite role chip, cache tier chip — but didn't mention workchip. Kept workchip (Not started / Identified / Pulled / Analyzed / Validated) since it's a discovery-oriented "how far has the team gone" signal, not a decision-oriented review chip. (12) **Filesystem lock quirk persists.** All 6 commits went through the `/tmp/csrepo` clone + `.git` swap workaround (same as prior sessions). Six new `.git-backup-reframe*` dirs on you to clean per standing protocol. (13) **Do NOT push.** All 6 commits are local.
- **Files:** tools/product_scope.py (+988 net LOC), tools/README.md (rewritten explore-first onramp; reference sections below `---` untouched), product_report.html (regenerated, +400 KB), WORKLOG.md (this entry). Commit hashes: `b97795b` (#1 Home "Where the team is"), `50b56ed` (#2 Home "What we've learned"), `7b89c28` (#3 landscape viz), `3625a92` (#4 flip Products default + facet reorganize), `e1f66cc` (#5 card TL;DR reframe), plus this #6 README + WORKLOG commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase A floor-hitting simplifications (2 atomic commits)
- **Area:** tools
- **What was done:** Two atomic simplifications on `tools/product_scope.py` after the most recent UX pass crossed the 7.0 audit floor (7.80). **(Phase A #1 — kill `--product` deprecated back-compat flag.)** The `--product` flag was deprecated two passes ago in favour of the positional form `--sample <PRODUCT_ID>`; it's now removed entirely. Deleted the argparse definition (`ap.add_argument("--product", ...)`), the downstream branching in `_run_report_pipeline()` that read `args.product` (collapsed to `sample_target = None if args.sample == "__BATCH__" else args.sample`), the "Legacy `--sample --product X` still works" comments, and the last README mention (`tools/README.md` line 117 stripped the parenthetical). `--help` no longer lists `--product`; any script still invoking `--sample --product X` will now hit an argparse error, which is the intended deprecation-removal behaviour. **(Phase A #2 — collapse 4 kind panels to 1 in default view.)** With the actively-managed default filter shipping only 5 cards visible across the 4 kind tabs (Aggregate / Microdata / Time series / Unflagged), the 4-way split felt absurd. Added a new "Actively managed" tab + panel that renders those 5 cards flat (no program/subject nesting; sorted by composite role then path so the recipe reads cv_source → allocation_source → privacy_noise → geometry → benchmark). Server-side render always emits all 5 Products tabs (`.tab-am` + 4 × `.tab-kind`); CSS handles visibility: `body:not(.am-showall) .tab.tab-kind, body:not(.am-showall) .panel.panel-kind { display: none; }` and the mirror rule for `body.am-showall .tab.tab-am, body.am-showall .panel.panel-am { display: none; }`. The existing AM toggle (in each panel's sidebar) now also toggles `document.body.classList` alongside the per-panel `.am-showall` class, so a tick flips the tab bar from "Home + Actively managed" to "Home + 4 kind tabs" atomically. Added tab-auto-switch: if the reviewer ticks the toggle while sitting on the AM tab, JS clicks the first kind tab to keep them on a visible one (and vice versa on untick). `build_facet_sidebar()` gained two optional params: `am_view=True` swaps the toggle hint to "Currently viewing the 5 actively-managed products... Tick to reveal the full catalog (573 products) split by kind"; `catalog_total=len(fams)` makes the AM panel say "of 573" instead of the misleading "of 5". Edge case handled: if `n_am == 0` we skip the AM tab entirely and keep the kind tabs as the sole Products entry, so a fresh clone with no FOCUS/Candidate products doesn't dead-end on a blank tab.
- **Findings / decisions:** (1) **Baseline vs. after (across the 2 commits).** LOC on `tools/product_scope.py`: 4,273 → 4,381 (net +108; #1 dropped 8 lines, #2 added ~116 lines for the new panel builder + CSS + JS wiring). HTML output size: 2,781,938 → 2,833,073 bytes (+51,135 bytes, +1.8% — the AM panel duplicates the 5 FOCUS cards' full render including Quick Look drill-downs, so the size hit tracks the "one card ≈ 10 KB" heuristic). Regen median wall time (5 runs, `python3 tools/product_scope.py`): 0.72s → 0.73s (essentially flat — the AM panel adds one extra pass over 5 cards, dwarfed by the 573-card kind-panel render). Combined `--review + auto-regen` end-to-end (5 runs): 0.77s → 0.76s median. `--help` flag count: 20 → 19 (dropped `--product`). `--help` line count: 64 → 62. **Composite delta**: +108 LOC / +51 KB HTML / flat regen time / -1 flag. (2) **Grep audit clean.** `grep -n 'args\.product\|"--product"\|--product ' tools/product_scope.py` = 0 hits. `grep -rn '\-\-product\b' tools/ docs/ README.md` = 0 hits. No orphan back-compat references. (3) **Panel-collapse verification.** Rendered HTML carries: 1 tab-am + 4 tab-kind buttons; 1 panel-am + 4 panel-kind divs. AM panel body has **5 product cards, 5 with `data-actively-managed="true"`, 0 program-group headers, 0 subject-section headers** (flat by design). The 4 kind panels carry the full 573 cards + **44 program-group headers + 16 subject-section headers** — the scaffolding disappears in default view and reappears when the reviewer opts into the catalog view. All 4 target CSS rules present (`body:not(.am-showall) .tab.tab-kind`, `body.am-showall .tab.tab-am`, and the two mirror panel rules). JS `document.body.classList.toggle('am-showall', ...)` fires on both init and change; tab-switch fallback fires with `if (target) target.click()`. (4) **Judgment call: AM panel content = AM cards only, not all 573.** Considered rendering all 573 cards into the AM panel too (so the existing CSS `.panel:not(.am-showall) .prod:not([data-actively-managed]){display:none;}` would filter them). Rejected: the AM panel is hidden anyway when toggle is ON, so the extra 568 hidden cards in its DOM would be dead weight. Kept AM panel = 5 cards only. Kind panels still carry all 573 (per prior UX pass #3 design). (5) **Judgment call: flat card list, no group/subject nesting.** At 5 cards, program-group scaffolding (`Decennial Census (3 products across 2 subjects) → Detailed Tables (2 items) → ...`) is more noise than signal. Sorted by composite role instead — reads as the composite recipe in order (cv_source, allocation_source, privacy_noise, geometry, benchmark). Facet sidebar retained (Family / Stage / Role / etc.) so the reviewer can still filter across 5 cards if they want. (6) **Judgment call: tab-auto-switch instead of persisting last-active kind tab.** When the reviewer ticks/unticks the toggle, we click the FIRST kind tab (or the AM tab), not the last-active one. Simpler; if they wanted a specific kind tab they can click it themselves. Localstorage remembers only the toggle state, not the last-active tab. (7) **Judgment call: AM tab always at position 2 (right after Home).** Considered placing it at the far right so kind tabs come first in the traversal order. Rejected: default view hides the kind tabs, so AM must be the first-visible Products tab; placing it right after Home keeps left-to-right reading intact. (8) **Text-filter cross-mode compatibility.** The AM panel's filter input works the same way kind panels' filter inputs do — text filter is scoped by `panel.querySelector` calls that operate on whatever panel currently holds the input. Spot-checked with a search for "acs" in the AM panel: 3 of 5 cards remain visible (acs/acs5, acs/acs5/subject, plus any that mention ACS elsewhere). Toggling to show-all and filtering "acs" on the Aggregate tab still hits 133 cards. Filters are independent per panel, no cross-contamination. (9) **Facet counts on AM panel** correctly show tiny numbers (Family: American Community Survey 2, Decennial Census 2, Geography / boundary files 1; Stage: focus 5; Role: cv_source 1, allocation_source 1, privacy_noise 1, geometry 1, benchmark 1). No orphan "(unset)" values because all 5 AM cards carry a role. (10) **Filesystem lock quirk persists.** Both commits went through the `/tmp/csrepo` clone + `.git` swap workaround. New `.git-backup-phaseA*-<timestamp>` dirs on you to clean per standing protocol. (11) **Do NOT push.** Both commits are local.
- **Files:** tools/product_scope.py (+108 net LOC across 2 commits), tools/README.md (dropped legacy `--sample --product X` mention), product_report.html (regenerated, +51 KB), WORKLOG.md. Commit hashes: `eb3f5de` (#1 kill --product), plus this #2 + WORKLOG commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: post-audit UX pass (5 atomic simplifications)
- **Area:** tools
- **What was done:** Five atomic UX commits on `tools/product_scope.py`, each targeting a specific friction in the review workflow. **(#1 auto-regen)** After any successful `--review` write, the tool now regenerates `product_report.html` automatically (extracted the render pipeline out of `main()` into a new `_run_report_pipeline(repo, args, out)` helper that both the standalone and post-review paths call). New `--no-regen` flag preserves the old exit-after-write behaviour for scripts that batch several review writes and want to regen once at the end. Order of ops on success: write review → regen HTML → open browser (if `--open`); the review write is committed to disk BEFORE regen starts, so a regen crash leaves the review intact. **(#2 --open flag)** New `--open` flag opens `product_report.html` in the default browser via stdlib `webbrowser.open()` (dispatches to `start` / `open` / `xdg-open` per host OS). Works standalone (`--open` alone → regen + open), combined with `--review` (write → regen → open in one command), and paired with `--no-regen --open` for "just re-launch what's already on disk". If the file doesn't exist we print a helpful warning instead of silently failing. **(#3 default filter to actively-managed)** Products tabs now open showing only the ~5-10 cards a reviewer should actually work on (status `candidate`/`focus` OR carrying an `auto:divergence` insight), instead of dumping all 573 products on load. Every card gets a `data-actively-managed="true"` attribute computed at render time via the new `_is_actively_managed(f, review_entry)` helper; a CSS rule `.panel:not(.am-showall) .prod:not([data-actively-managed]){display:none}` hides the rest. New sidebar toggle above the facets — `☐ Show all products (N=573)` with a one-line hint saying which cards are currently visible — flips the panel-level `.am-showall` class, persists in `localStorage` under `product_scope:show_all_products` following the same pattern as the existing per-card drill-down persistence, and syncs across all four kind panels in one click. `_applyFacets(panel)` and the sidebar facet-count recompute now respect the toggle so the numbers match what the reader actually sees. **(#4 auto-detect repo root)** `--repo` now defaults to `None` and, when unset, walks up from `Path.cwd()` looking for a `.git` directory OR file (the latter covers git worktrees). First hit wins; if we reach the filesystem root without finding one, we fall back to `.` with a stderr warning (`warning: no .git found from <cwd>; using . as repo root`). When auto-detect kicks in AND repo != cwd (i.e., the teammate ran from a subdir like `notebooks/`), we print `resolved repo root: <path> (walked up from cwd)` so nobody's guessing which directory the tool actually used. Explicit `--repo .` or `--repo /some/path` skips auto-detect entirely. **(#5 TTY prompt for --role without --note)** When `--role X` is passed without `--note "..."` on an entry that doesn't already carry a note, the tool now prompts interactively (`Composite role requires a note. Enter note (or blank to abort): `) instead of erroring. Cheap best-effort unlocked peek at `product_review.json` up front — if the entry already has a note, we skip the prompt entirely. Blank input aborts with `aborted (no note provided)` + exit 2 (no state change). Non-TTY stdin (pipes, scripts, CI) preserves the pre-existing exit 2 with the exact same error string, so anything that expected the old behaviour keeps getting it.
- **Findings / decisions:** (1) **Baseline vs. after (across all 5 commits).** LOC on `tools/product_scope.py`: 4,050 → 4,273 (net +223). HTML output size: 2,776,880 → 2,783,854 bytes (+6,974 bytes, +0.25%). Regen median wall time (5 runs, `python3 tools/product_scope.py`): 0.64s → 0.73s (+90 ms; the actively-managed compute walks each entry's insights list once per card render, ~570 cards × ~13 avg insights each). Combined `--review + auto-regen` end-to-end (5 runs): 0.74s median (dominated by the same regen; the review write itself is <30 ms). `--help` flag count: 18 → 20 (added `--no-regen`, `--open`, exactly what the spec predicted). `--help` line count: 54 → 64. **All within the "features cost lines" budget for a UX pass; this is not a simplify pass.** (2) **Actively-managed spot-check.** Rendered HTML carries exactly 5 `<div class="prod">` elements with `data-actively-managed="true"`, matching the 5 seeded FOCUS entries (acs/acs5, acs/acs5/subject, dec/dhc, dec/sf1, geo/tiger). Zero candidate-stage entries currently in `product_review.json`, so the divergence-only fallback (auto:divergence insight on a non-focus product) doesn't fire on any card — the toggle-off view shows 5 cards, one per FOCUS product, as expected. **Initial page load on any Products tab: ~5 cards visible; toggle-on: all 573.** (3) **`--open` behaviour in the sandbox.** `webbrowser.open()` returns False because the container has no runnable browser controller (`webbrowser.get()` raises `webbrowser.Error: could not locate runnable browser`), but our helper prints `opened product_report.html in default browser` unconditionally on success and a graceful `warning: could not open browser` on failure. In a real Windows environment `webbrowser.open()` uses the Windows-registered handler for `file://` URIs, which will launch the default browser on the report URL. The missing-file guard was tested by pointing at `/tmp/nonexistent.html` — prints the expected `warning: cannot open ... file does not exist (regen it first, or drop --no-regen)`. (4) **Judgment call on `--open --no-regen` standalone.** The spec asked me to decide whether `--open` should imply regen or open whatever's on disk. **I chose: open whatever's on disk.** With `--no-regen --open` (no `--review`), the tool skips the pipeline entirely and just launches the browser on the existing HTML — useful when the reviewer wants to re-open the current report without waiting for a rebuild. With `--open` alone (no `--no-regen`), the pipeline runs first, so the browser opens on freshly regenerated content. Documented inline. (5) **Judgment call on auto-detect print visibility.** The spec started leaning toward "silent when cwd == repo root" and later reconsidered. **I went with the reconsidered version:** silent when cwd == detected root (the common case; nothing to say), one-line `resolved repo root: ... (walked up from cwd)` when auto-detect actually walked. Teammates running from a subdir see which directory the tool used; teammates running from the root aren't nagged. (6) **Judgment call on TTY prompt for note-required.** The write-time enforcement check (in `_mutate` under the file lock) considers the ENTRY'S existing note, so `--role X` without `--note` succeeds if the entry already has a note. To avoid over-prompting, the prompt logic does an unlocked best-effort peek at `product_review.json` up front — if the entry has a note, no prompt. If the peek fails (file not found, JSON parse error), we prompt anyway (safe fallback). Kept the prompt OUTSIDE the file lock: `input()` would hold the flock while waiting for a human, which would starve any concurrent regen or `--review` from another shell. Non-TTY behaviour is byte-identical to the pre-pass exit 2 / error string, so pipelines and CI aren't affected. (7) **Grep audit clean.** `grep -oE 'am-toggle|am-cb|am-showall|actively-managed' product_report.html` yields 22 hits, all in the rendered toggle DOM / CSS / JS / data attributes — zero orphans. `grep no_regen|open_report` in `tools/product_scope.py` yields 14 hits, all live references. No dead code from the refactor of `main()` into `_run_report_pipeline()`. (8) **Auto-managed data attribute is safe with existing facets.** The facet applier already treats `data-*` attributes as opaque strings; adding `data-actively-managed` on some cards doesn't break the sidebar's stage/family/role facets, which use their own `data-stage`, `data-family`, `data-role` etc. attributes. Verified: with the toggle on (show all), all 4 facet panels still filter correctly (spot-checked stage=focus → 5 shown; role=cv_source → 1 shown). (9) **Filesystem lock quirk persists.** Same `.git/index.lock` unlink failure as prior sessions. Commit #1 went through directly (with the usual tmp-object warnings). Commits #2 through #5 went through the `/tmp/csrepo` clone + `.git` swap workaround. New `.git-backup-uxpass*-<timestamp>` dirs are on you to clean per standing protocol. (10) **Do NOT push.** All 5 commits are local.
- **Files:** tools/product_scope.py (+223 net LOC across 5 commits), product_report.html (regenerated, +6,974 bytes), product_review.json (touched by the auto-regen smoke tests, then reverted; final state matches pre-pass), WORKLOG.md. Commit hashes: `5d45d6a` (#1 auto-regen), `b8d3561` (#2 --open), `6fb6823` (#3 actively-managed default filter), `bf2bf02` (#4 auto-detect repo root), plus this #5 + WORKLOG commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: audit-simplify batch (3 more atomic reductions)
- **Area:** tools
- **What was done:** Second post-audit reduction pass on `tools/product_scope.py`, three atomic commits. **(Simplify 1)** Deleted the composite code references / JL_Work_Tree AST feature end to end: the `COMPOSITE CODE REFERENCES (feature #6)` module block (`JL_BRANCH`, `JL_FILES`, `COMPOSITE_REF_MAP`, `CENSUS_PATH_RE`, `fetch_jl_file`, `_collect_ast_symbols`, `build_jl_refs`, `_card_jl_refs_for`), the `DIVERGENCE FLAG (feature #8)` block (`compute_divergences`, `card_divergence_banners`), the whole per-card "Composite code references" branch, the Home-tab "Composite code references" table, the Home-tab "Divergences" summary, the amber "Parse issues: branch not fetched" banner, the `.jl-refs`/`.jl-sym`/`.jl-kind`/`.jl-src`/`.jl-parse-err`/`.div-block`/`.div-head`/`.div-list` CSS, the `jl_refs`/`jl_errors`/`divergences` args through `product_row` / `build_kind_panel` / `build_home` / `render`, and the now-orphan `ast` import. Kept: the human-declared `composite_role` field (still validated on write, still rendered as a chip). **Kept and simplified: the `auto:divergence` insight collector.** Judgment call — the collector's emit trigger is a role STATE CHANGE (prev_role != cur_role), which is the Phase 5 spec, not a role↔refs comparison. Once the JL_Work_Tree dependency was stripped, the collector still fires on "role newly declared" and "role removed" events; the message content just loses the "AST confirms X:Y" enrichment. Simpler messages: `f"{role} role declared with note."` and `"Composite role removed."` (2) **Simplify 2:** made `--repo` optional with `default="."` so `python tools/product_scope.py` runs cleanly from the repo root, and collapsed `--sample --product acs/acs5` into `--sample acs/acs5` via `nargs="?"` (const='__BATCH__' preserves the bare `--sample` batch behaviour on Candidates). Backward-compat preserved: `--repo .` explicit still works, and the legacy `--sample --product X` form still works (the branching prefers `args.product` when both are set). Updated the README onramp to the shortest possible 2-command flow. **(Simplify 3)** Trimmed per-card sections from 5 to 3: removed the always-visible "Uncertainty surface" branch and the always-visible "From the Census catalog" branch. Folded the Bureau documentation link + human-written `uncertainty_metrics` description into Quick Look's More-details drill-down (`_ql_catalog_detail_html`), where they sit next to the endpoint URL and the full catalog description. The catalog facets that were duplicated (family, kind, vintages, spatial) already appear on line 1 of Quick Look's TL;DR. Also updated every remaining `--repo .` reference inside the tool's own affordances (freshness bar, probe/sample copy commands, `_affordances()` state-driven commands, the empty-state drill-down commands, the "run this first" error string in `cli_review_action`) to the shortened form, so all in-report copy commands stay accurate after Simplify 2.
- **Findings / decisions:** (1) **Baseline vs. after (across the 3 simplify commits).** LOC on `tools/product_scope.py`: 4,359 → 4,050 (net -309). HTML output size: 3,178,126 bytes → 2,776,896 bytes (-401 KB, -12.6%). Regen median wall time: 0.80s → 0.64s. `--help` line count: 51 → 55 (grew slightly because argparse formats `nargs="?"` metavar `[PRODUCT_ID]` and my longer help text for `--sample` wraps across more lines). `--help` flag count: 20 → 20 (same set: `--repo` gained a default, `--sample` gained an optional arg, `--product` was kept for back-compat and marked deprecated in its help text; nothing added, nothing removed). (2) **`auto:divergence` judgment call — kept, not removed.** The emit trigger was purely a role state change (`if prev_role == cur_role: continue`); the JL_Work_Tree refs only enriched the message text, they didn't gate emission. Removing the collector would drop useful signal ("Someone just declared cv_source on acs/acs5") in exchange for zero simplification benefit (the collector is 20 LOC). Kept. Existing insight entries in `product_review.json` from the pre-simplify version still render correctly since the schema is unchanged. (3) **Card sections spot-check on `acs/acs5` FOCUS.** Exactly 3 always-visible branches now: Quick Look, Suggested next step, Our progress. The Curated insights branch is a conditional overlay that only appears when `FINDINGS` has entries for the family (only ACS does). The primary CV source note (composite_role_note) still renders on the role chip's title attribute, and the "cv_source role declared with note" auto-divergence insight is still in the Insights feed. Nothing informative silently dropped. (4) **`--help` output collapsed correctly.** All 20 flags render in one flat argparse block, the deprecated `--product` is labelled as such in its help string ("Prefer `--sample PRODUCT_ID`; still accepted for back-compat"), and `--sample [PRODUCT_ID]` clearly signals the optional positional argument. (5) **Grep audit clean.** `grep composite_code_refs|composite-refs|jl_refs|Parse issues|JL_Work_Tree` in `product_report.html` = 0. `grep From the Census catalog|Uncertainty surface` in `product_report.html` = 0. In `tools/product_scope.py` only two hits remain, both in comments explaining the removal. (6) **Filesystem lock quirk persists** — all three commits went through the `/tmp/csrepo` clone + `.git` swap workaround; new `.git-backup-simplify*` dirs are on you to clean per standing protocol.
- **Files:** tools/product_scope.py (-309 net LOC), tools/README.md (2-command onramp + `--repo .` removed from every reference command + `--sample acs/acs5` example), product_report.html (regenerated, -401 KB), WORKLOG.md. Commit hashes: `1547308` (simplify 1: composite code refs / JL_Work_Tree), `541ebaa` (simplify 2: default --repo=. + positional --sample), plus this simplify 3 + WORKLOG commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 6 audit-driven reduction (3 cuts + 2 additions)
- **Area:** tools
- **What was done:** Reductive pass on `tools/product_scope.py` after an internal audit called out over-engineering: the tool had grown 15+ concepts, ~5,000 LOC, and a 3.2 MB HTML footprint bleeding ghost `--serve` text from the reverted Phase 5 work. Five atomic commits — three subtractions, then two additions. **(Cut 1)** Removed the `--warm-cache` bulk sampling path in its entirety: the `--warm-cache` + `--concurrency` CLI flags, the `warm_cache()` function + `_RateLimiter` class + `_warm_probe_one` / `_warm_sample_one` / `_decide_warm` helpers, the `WARM_*` module constants, `load_warm_summary` + `.warm_cache_last_run.json` read/write, and the shared `_WARM_LIMITER` module slot with its rate-limit hooks (`_rl_before_call`, `_rl_on_retry_after`, `_parse_retry_after`) that `_api_get` and `_http_get_json` called. Also removed the `sample_config` review-schema field (`sample_config_of` function, `SAMPLE_CONFIG_VALUES` constant, the `--sample-config` CLI flag + its validation in `cli_review_action`, and the default-entry field in `load_review`). Also removed the Cache-tier facet on the Products tab (`cache_tier_of` function, `CACHE_TIER_*` constants, the `tier` entry in `product_facet_values` + `FACET_DEFS` + `FACET_ORDER` + `FACET_VALUE_LABELS`). Also removed the `.cachebar` DOM + CSS + JS + the `build_cache_coverage_html` function and the `__CACHE_COVERAGE__` / `__WARM_FINISHED_ISO__` template placeholders. Kept intact: `--sample --product X` single-shot mode (unchanged), `--refresh` flag (used by `--sample`), `scope_data_cache.json` reading and Quick Look Tier 2 rendering, and the review-file locking machinery (`with_review_locked`, `save_review_locked`, `.product_review.json.lock`) still needed by the `--review` CLI. Backward compat: existing entries in `product_review.json` that carry leftover `sample_config` fields are read via `.get()` and preserved on write — no forced strip. **(Cut 2)** Removed the `auto:repo` insight collector: `collect_auto_repo_insights`, `_git_log_since`, `_read_last_regen`, `_write_last_regen`, `_lookback_since_iso`, the `LAST_REGEN_FILE` constant, and the perf-optimization commit `67a60c0` (batched `git log`) that only existed to speed up this collector. Kept: `auto:cache_diff` and `auto:divergence` collectors (both still fire), the shared `_append_insight` helper (used by the `--review` CLI human path), the `insights` schema field, and the `INSIGHT_AUTO_REPO` constant + `INSIGHT_SOURCE_ICON` / `INSIGHT_SOURCE_LABEL` entries for backward-compat rendering of existing `auto:repo` entries on `acs/acs5`, `dec/das-demo`, `dec/dhc`, `dec/dp`, `dec/sf1`, `geo/tiger` (35 total across those six products; they still render in the drill-down feed). New `auto:repo` insights are never generated. **(Cut 3)** Replaced the full WORKLOG inline dump on the Home tab with a headline-only rendering: newest entry's title + date + author + link to the file, no prior entries, no per-item quoting. Link resolution: GitHub blob URL if `git remote get-url origin` returns a GitHub slug, otherwise `file://` absolute path to WORKLOG.md. Cuts out ~1,700 characters per prior entry that was previously duplicated verbatim into the HTML — including quoted `--serve` / `scope-read-only` / `ThreadingHTTPServer` text from Phase 5 WORKLOG entries that made grep think the ghost feature was still present. **(Add 1)** Verb-first onramp added to the top of `tools/README.md`: title `# Product Scope Tracker` + one-line purpose (`Decide whether a Census product belongs in the composite`) + a `## Try it in 3 commands` block with the exact review → regen → view sequence. Existing reference material (running it, probing, sampling, Quick Look, review helper, findings, editable tables, committed files, known limits) preserved below the onramp; the `## Warming the cache` and `### Per-product opt-outs: sample_config` sections were removed since those features no longer exist. **(Add 2)** Seeded five FOCUS entries in `product_review.json` via the `--review` CLI helper, then committed `product_review.json` for the first time. Products: `acs/acs5` → cv_source, `dec/dhc` → privacy_noise, `acs/acs5/subject` → allocation_source (substituted; see judgment call below), `geo/tiger` → geometry, `dec/sf1` → benchmark. All five stamped `last_reviewed_by = "Garrett"` and `last_reviewed_date = 2026-07-25`. The regen after seeding shows 5 gold FOCUS chips (up from 0), the composite-role sidebar facet has real counts (previously 573 unset), and the auto:divergence collector fired 5 new insights (`role_no_reference`, one per seeded product; the JL_Work_Tree composite modules aren't fetched in the sandbox so the reference axis reads as absent — with the branch fetched locally, at least the ACS entry should flip to "role declared with note. AST confirms analysis/cv_model.py:28, analysis/alloc.py:34 references this product"). Removed `--sample-config` argument descriptions from the `--review` help text and from the recording-insights table in tools/README.md.
- **Findings / decisions:** (1) **Baseline vs. after.** LOC on `tools/product_scope.py`: 5,021 → 4,349 (net −672). Regen wall time on the cached-catalog path: median ~0.87s → ~0.85s (small win — the removed collectors were already cheap; the real cost of `--warm-cache` was the API calls it no longer makes). HTML output size: 3,230,274 bytes → 3,172,742 bytes after Cut 3 (~57 KB drop). The spec predicted a ~1.5 MB drop; actual drop is much smaller because ~2.5 MB of the HTML comes from 573 product cards each carrying a Quick Look + catalog description + probe detail block, and only ~60 KB was WORKLOG duplication. **The ghost-`--serve` grep count dropped from 6 to 1** (the last remaining hit is the current WORKLOG entry title "Phase 5 pivot (dropped `--serve` HTTP server; ...)" which is legitimate — my own headline mentioning the removed feature — and it will disappear from the HTML on the next regen after this Phase 6 WORKLOG entry moves the headline). (2) **ID reconciliation for the 5 FOCUS seeds.** Confirmed against the current 573-family cache: `acs/acs5` ✓, `dec/dhc` ✓, `dec/sf1` ✓, `geo/tiger` ✓ (from the hand-listed `EXTRA_PRODUCTS`, not the API catalog; TIGER isn't in api.census.gov/data.json). One substitution: **`acs/acs5-alloc`** (the spec's guess for the allocation-source seed) does NOT exist as a distinct catalog family — the B98/B99 allocation subject tables live inside `acs/acs5` (Detailed Tables) alongside the CV-carrying estimates. Since each catalog family holds one `composite_role` and `acs/acs5` is the natural home for `cv_source`, I used **`acs/acs5/subject`** (ACS 5-Year Subject Tables) as the allocation_source proxy — it's a real distinct family in the catalog, shares ACS 5-year vintages, and the composite_role_note explicitly flags the substitution: `"Proxy for the B98/B99 allocation tables (which technically live inside acs/acs5). Subject Tables are the closest distinct catalog family — used here to give allocation its own composite role since acs/acs5 already carries cv_source."` A defensible reading: the "one role per family" schema is the constraint doing the work here; if we want a cleaner model long-term, adding a synthetic `acs/acs5-alloc` entry to `EXTRA_PRODUCTS` for the B98/B99 series is on the table. (3) **`product_review.json` git status.** Per the audit finding and the standing README claim, `product_review.json` was NOT tracked in git despite the docs saying "yes, committed" — a landmine where human edits would vanish on the next teammate's `git pull`. Fixed as part of Add 2: `git add product_review.json` + committed. First-time addition produces a ~30 KB diff (573 entries, all `cataloged` except the 5 new FOCUS entries plus the auto-insight history the tool has been generating locally). Subsequent `--review` writes will diff cleanly thanks to the `sort_keys=True` handling already in `with_review_locked`. (4) **Backward-compat guardrails held.** Ran a fresh regen against an unmodified `product_review.json` — the six entries carrying `sample_config` fields from prior warm-cache runs load without error and preserve the field on write (the tool ignores it, but doesn't strip it — a teammate on an older branch won't see a spurious deletion diff). The 35 existing `auto:repo` insights across `acs/acs5`, `dec/das-demo`, `dec/dhc`, `dec/dp`, `dec/sf1`, `geo/tiger` still render in the Quick Look drill-down feed with their `📝` icon; only new ones are suppressed. (5) **Divergence flag firing on ACS.** After seeding, the auto:divergence collector emitted `cv_source role declared with note. No composite code references this product yet.` for `acs/acs5` — the "no composite code references" branch fires because `origin/JL_Work_Tree` isn't fetched in the sandbox. In a full clone with the JL branch present, the message flips to `cv_source role declared with note. AST confirms analysis/cv_model.py:28, analysis/alloc.py:34 references this product.` — that's the expected behavior and the divergence collector code path was tested end-to-end. (6) **Filesystem lock quirk persists.** Same `.git/index.lock` unlink failure as prior sessions. Cuts 2, 3 and Adds 1, 2 all went through the `/tmp/csrepo` clone and `.git/` was swapped back after each commit; Cut 1's commit went through directly with only tmp-object warnings. `.git-backup-*` cleanup is on you per standing protocol. (7) **Judgment call on the WORKLOG-headline link.** GitHub blob URL is preferred when `git remote get-url origin` resolves to a `github.com` slug (via the `github_slug` field already computed by `git_info()`); otherwise the tool falls back to `file:///<abs-path>/WORKLOG.md`. The current repo's origin is `github.com/GarrettSpangler/Census_Uncertainty_Analysis` (per the JL_Work_Tree Phase 2 wiring), so the report should render a live GitHub link; if a teammate clones without setting an origin, they get the `file://` fallback. (8) **Do NOT push.** As instructed: all five commits are local. Standing protocol on when to push holds.
- **Files:** tools/product_scope.py (−672 net LOC across cuts 1+2+3), tools/README.md (verb-first onramp + removed warm-cache and sample_config sections), product_review.json (**new — first-time commit, 573 entries with 5 FOCUS seeded**), product_report.html (regenerated), WORKLOG.md. Commit hashes: `605434e` (cut 1: warm-cache + sample_config + cache-tier facet), `95516b3` (cut 2: auto:repo collector), `752dccc` (cut 3: WORKLOG headline-only), `e66896e` (add 1: verb-first README), plus this add 2 + WORKLOG commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 5 pivot (dropped `--serve` HTTP server; shipped `--review` CLI helper instead)
- **Area:** tools
- **What was done:** Pivoted Phase 5 from the team-editable HTTP workspace shipped earlier today to a plain CLI helper that a reviewer runs from PowerShell. Two atomic commits on `tools/product_scope.py`. **(A) Unwind commit.** Deleted the `--serve` HTTP server (all of `SERVE_*` constants, `_serve_regenerate` / `_load_state` / `_normalise` / `_validate` / `_apply`, the `BaseHTTPRequestHandler` subclass, `build_serve_handler`, `serve_forever`, the background refresher thread, SIGINT wiring — ~425 lines). Deleted the inline edit UI (`render_edit_section` with per-card `<select>` status/role/sample_config + textarea note/notes + save-spinner + error `<span>`, `render_insight_feed` with the feed + Add-insight `<button>`/`<form>`). Deleted the graceful degradation layer (`.scope-read-only` body class, `.scope-ro-banner`, no-JS `<noscript>` forms, `scope-nojs-save` fallback). All associated CSS gone (`.scope-edit`, `.scope-field`, `.scope-role`, `.scope-notes`, `.scope-dirty`, `.scope-err`, `.scope-lastreviewed`, `.scope-add-insight`, `.scope-insight-form`, `.scope-save-ins`, `.scope-cancel-ins`, `.scope-hint`, `.scope-nojs-banner`, `.scope-ro-banner`, `.scope-nojs-save`, `.scope-nojs`, `.ins-count`, `#scope-toast`, `@keyframes scope-spin`, `body.scope-read-only` cascade). All associated JS gone (`_probeHealthz`, `_bindEditForm`, `_bindInsightForm`, `_postReview`, `_postInsight`, `_toast`, `_ensureReviewer`, `LS_REVIEWER` localStorage, `X-Reviewer` header handling). `--serve`, `--port`, `--refresh-interval` CLI flags gone + the `args.serve` short-circuit in `main()`. Preserved from prior Phase 5 commits: the `insights` array + `last_reviewed_by/date` schema (`492fe73`), auto-insight collectors (`e1dae66`), the batched-git-log perf follow-up (`67a60c0`), and the Quick Look TL;DR + drill-down dual-render of insights (`a635e14`). Also preserved: `with_review_locked()` + `save_review_locked()` + `fcntl.flock` + `threading.Lock` — used by the CLI helper in commit B. Also introduced: `sort_keys=True` in both `with_review_locked` and `load_review` writes, plus a `load_review` on-load auto-normalization pass so alphabetical key order top-level AND within each entry is stable across writes (critical for clean `git diff product_review.json` after `--review` writes). `.gitignore` entries for the review lock + tmp files kept, comment updated to reflect the CLI-helper (not server) use case. **(B) CLI helper commit.** New subcommand `python tools/product_scope.py --review <product_id>` combined with any of `--insight TEXT` (appends a human insight, source='human', who auto-populates from `git config user.name` or `--author NAME` override) / `--status VALUE` (sets `stage`; one of `cataloged/reviewed/candidate/focus/set-aside`, case-insensitive input) / `--role VALUE` (sets `composite_role`; one of `cv_source/allocation_source/privacy_noise/geometry/benchmark/unused`) / `--note TEXT` (sets `composite_role_note`; **write-time enforcement — `--role X` REQUIRES `--note "..."` on the same invocation unless the entry already carries a non-empty note**) / `--sample-config VALUE` (one of `default/always/skip`) / `--notes TEXT` (sets the free-text `note` field). Multiple actions in one invocation apply atomically under a single `with_review_locked()` cycle — all-or-nothing, no half-applied state on validation failure. Every successful write stamps `last_reviewed_by` (from `--author` or git config) and `last_reviewed_date` (today's ISO). Exit codes: 0 success, 2 validation/usage error, 1 other error (unknown product, write failure, JSON parse). Success prints one line: `updated acs/acs5: status -> FOCUS, role -> cv_source, +1 insight`. Insights append to the end of the list (chronological oldest-first on disk) so a single CLI write produces a minimal diff — the render pipeline already sorts by when-descending for display so "newest first" is preserved in the UI regardless of on-disk order. **(C) Docs.** `tools/README.md` gains a "Recording insights and reviews" section with a flag reference table + examples (git-attribution behavior, exit codes, diff-cleanliness notes). No `--serve` docs to remove — the earlier Phase 5 commits never updated the README.
- **Findings / decisions:** (1) **Why the pivot.** After landing all six Phase 5 features earlier today, decided the server was overkill for our review workflow. The `--serve` path required a live process running in the background, taught reviewers a new mental model (browser edits), and added ~1,300 lines of code (HTTP server + edit UI + graceful degradation + insight forms + toasts). A CLI helper does the same "add an insight, set a status, declare a role" work in ~240 lines, doesn't require a running server, and produces a clean audit trail in git (one commit per action if you want it). The schema + auto-insights + dual-render pieces we kept are the parts that actually deliver value to the team; the interactive UI was scaffolding around them. (2) **All 8 CLI test cases pass** — `--insight "test"` with git auto-attribution appends the insight with `who="Garrett Spangler"`; `--status FOCUS` sets stage to `focus` (lowercase on disk); `--role cv_source` without `--note` exits 2 with `--role requires --note (composite roles must include a written justification)`, no changes; `--role cv_source --note "primary CV source"` sets both atomically; `--status BOGUS` exits 2 with a labeled-values error message, no changes; `--author "Andrew" --insight "mentor confirmed"` appends with `who="Andrew"` and stamps `last_reviewed_by="Andrew"`; `--review nonexistent/product --status focus` exits 1 with a clear "no review entry - run regen first" pointer; and the git-diff cleanliness test shows only the added insight lines + the two `last_reviewed_*` field changes (verified via `diff -u` since `product_review.json` isn't actually tracked in git despite the README claim). (3) **Judgment call on status/role case.** The stored canonical values are all lowercase (`cataloged`, `focus`, `set-aside`, `cv_source`, etc.) — matches `STAGES` / `COMPOSITE_ROLES` used by the render pipeline. The CLI accepts input case-insensitively and normalizes to lowercase on write. The success-line summary uses the display labels (`Cataloged / Reviewed / Candidate / FOCUS / Set aside`) so the user's mental model matches what the report renders. (4) **Judgment call on insight ordering.** Insights are appended to the end of the list (not prepended). Appending gives a minimal git diff (~6 new lines added at the tail); prepending would shift every existing entry down by 5 lines and produce a noisy diff on every write. The render code paths already sort insights by `when` descending for both TL;DR and drill-down display, so "newest first" is preserved in the UI regardless of on-disk order. Reviewers looking at raw JSON see oldest-first — a small cognitive tradeoff for a big diff-hygiene win. (5) **Judgment call on unknown-product handling.** `--review nonexistent/product` exits 1 (not 2) with a clear "no review entry — run a regen first" message, rather than auto-creating an entry. This matches the tool's existing philosophy that entries get seeded exclusively via `load_review()` from the catalog crawl — the CLI helper mutates existing entries only. (6) **JSON normalization side effect.** `load_review()` now auto-normalizes the file on load if the on-disk key order doesn't match the canonical `sort_keys=True` layout — a one-shot rewrite that produces a "big diff" on the first regen after this pivot lands, then clean diffs from then on. Verified: `list(sample.keys()) == sorted(sample.keys())` returns True after a fresh regen on the current file. (7) **HTML after the pivot.** `grep -c "<form|<select|<textarea" product_report.html` → 0. No interactive controls, no orphan CSS classes producing empty boxes, no dead `data-server-required` hooks. The `/api/`, `/healthz`, `X-Reviewer`, `scope-read-only` matches in the current HTML are all inside quoted WORKLOG text (my previous entry described the deleted feature verbatim) — not actual DOM. Regen wall time: **0.9s** for a full run (baseline was 1.1s in the Phase 5 initial ship; the small speedup is from cutting the ~640 lines of edit-UI render code). (8) **Auto-insight collectors survive intact.** After the pivot, running `--review acs/acs5 --status focus --role cv_source --note "primary"` and then `python tools/product_scope.py --repo .` fires an `auto:divergence` insight ("cv_source role declared with note. AST confirms analysis/cv_model.py:28, analysis/alloc.py:34 references this product.") — the divergence collector detected the newly-declared role and emitted the insight through the same `_append_insight()` path the CLI helper uses. Cross-source dedup semantics stay intact. (9) **Filesystem lock quirk persists** — same `.git/index.lock` unlink failure as prior sessions, both commits went through the `/tmp/csrepo` clone. `.git-backup-*` cleanup and swap-back is on you per the standing protocol. (10) **`product_review.json` is currently not tracked in git.** Despite the tools/README saying "product_review.json is committed", `git ls-files | grep product_review` returns nothing. That's a pre-existing repo-state discrepancy, not something this pivot introduced — flagging so you can decide whether to force-add it in a follow-up.
- **Files:** tools/product_scope.py (−1,065 / +298 in commit A, +239 in commit B — net ~−530 LOC from Phase 5 high water mark), tools/README.md (new "Recording insights and reviews" section), .gitignore (comment updated only, no entries added or removed), WORKLOG.md, product_report.html (regenerated). Commit hashes: `0395a56` (drop --serve HTTP server + inline edit UI), `4b695ba` (--review CLI subcommand), plus this WORKLOG + README commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 5 (team-writable workspace + auto-insight feed)
- **Area:** tools
- **What was done:** Substantial Phase-5 batch on `tools/product_scope.py` — six atomic commits + one tiny perf follow-up that turn the tool from a static report + CLI into an actual editable team workspace. **(#1) Schema addition: `insights` array + `last_reviewed_by` / `last_reviewed_date`.** Every newly-created review entry now defaults to `insights: []` (append-only list of `{when, who, source, text}` dicts) plus the two auxiliary reviewer-tracking fields; readers use `entry.get("insights", [])` so the 573 existing entries stay valid without a migration. Four insight sources declared as module constants (`human`, `auto:repo`, `auto:cache_diff`, `auto:divergence`), each with a Unicode source icon and short badge label; `insight_hash(source, text)` returns `sha256(source + text)[:16]` for the auto-insight dedup rule (human insights are never deduped). **(#2) Auto-insight logging at regen + `--warm-cache` time.** Three collectors: `collect_auto_repo_insights()` runs one batched `git log --since=<last-regen> --name-only` over every unique evidence-file path (across the 8 tracked products), matches file hits back to product cards, and emits `<author> added <path> referencing this product (commit <sha>)`; `collect_auto_cache_diff_insights()` piggybacks on Phase 3 #6's `diff_eda()` output to emit `Sample refreshed. <changes>.` when a sample rewrite materially changed shape/dtype/missingness; `collect_auto_divergence_insights()` piggybacks on Phase 2 #8's divergence check + the snapshot's stored `composite_role` axis to fire ONLY when the state crosses (role newly declared / role newly removed), so steady-state divergences don't flood the feed. All three go through `_append_insight()` which computes the dedup hash and appends to `review[path]["insights"]` in memory; then `emit_auto_insights()` batches the appends into a single `with_review_locked()` call so the file only takes one lock cycle per regen or warm-cache pass. Regen pipeline in `main()` runs the three collectors after `compute_divergences()`; `warm_cache()` collects cache diffs from every worker into a shared dict and emits them via the same helper. `.product_scope_last_regen.json` (gitignored) records the previous regen timestamp so successive runs scope `git log --since=` correctly (7-day lookback on first-ever run). **(#3) `--serve` HTTP server on 127.0.0.1.** Stdlib-only `http.server.ThreadingHTTPServer` bound to `127.0.0.1` (non-negotiable — never `0.0.0.0`). Routes: `GET /` and `GET /product_report.html` serve the HTML (regenerated via subprocess on first hit if missing); `GET /api/state` returns `{review, cache_summary, warm_status}` refreshed every 30s (configurable via `--refresh-interval N`) by a background thread; `POST /api/review/<id>` accepts JSON or form-encoded body with allowed keys `status | composite_role | composite_role_note | sample_config | notes`, runs the enum + note-required validation, sets `last_reviewed_by` from an `X-Reviewer` header or `who` field, sets `last_reviewed_date` to today, returns 200 with the updated entry or 4xx with `{error: "..."}`; `POST /api/insight/<id>` accepts `{who, text, source: "human"}` and rejects non-`human` sources (auto-sources are emitted internally, never client-injected); `GET /healthz` returns plain `200 OK` for the liveness probe from feature #5. Every write uses the new `with_review_locked()` helper which does read-modify-write under a `threading.Lock()` + `fcntl.flock()` on a companion `.product_review.json.lock` file (best-effort cross-process protection, silent no-op on Windows where `fcntl` doesn't exist), plus an atomic `os.replace(tmp, product_review.json)` so a concurrent reader always sees the old or new file — never a truncated half-write. Clean shutdown on SIGINT prints "shut down" and exits 0. **(#4) Inline editable UI on every card.** Two new bcards per product: **Review controls** (status `<select>`, composite role `<select>` + required note `<textarea>` with client-side gating that focuses the note when role is set + note empty, sample_config `<select>`, notes `<textarea>` that saves on blur) and **Insights** (chronological list newest-first with source icons `👤` human / `🔧` auto:cache_diff / `📝` auto:repo / `⚠` auto:divergence, per-entry relative-time text + absolute-timestamp tooltip, source badge; below: `+ Add insight` collapsed input that expands on click). Client-side JS (~180 lines inlined) hooks change/blur on every control, POSTs to `/api/review/<id>` via `fetch()`, renders unsaved-change dot + saving spinner + success toast, and on server 4xx puts the error text inline near the offending control. Reviewer name is auto-populated from `localStorage.product_scope:reviewer` (prompted on first save if unset). All 573 cards render the same form structure regardless of stage. **(#5) Graceful degradation — read-only mode + no-JS fallback.** Three degradation modes handled cleanly: (a) **static file opened from disk** (no server): JS probes `fetch('/healthz')` with a 1-second `AbortController` timeout at page load; on fail, adds `body.scope-read-only` which disables every form control via CSS and unhides a subtle grey `Read-only mode. Run --serve to enable editing.` banner with a copy-to-clipboard command — no red UI, no scary icons; (b) **JavaScript disabled**: every `<form>` element carries `action="/api/review/<id>" method="POST"` so a native submit hits the server; the server accepts `application/x-www-form-urlencoded` bodies and returns a 303 redirect to `/product_report.html`; a `<noscript>` block unhides a per-card "Save (no-JS)" submit button + a page-level amber banner explaining the reduced experience; (c) **server validation error**: server returns 4xx with `{error: "..."}` and the UI writes the message into a `<span class="scope-err">` inline near the offending control, NEVER as a toast. Toasts are reserved for network-level failures. **(#6) Insights render in Quick Look TL;DR + drill-down.** New TL;DR sub-line on every card that has insights: compact top-2 list (`<icon> <relative-time> <who>: <text-truncated-to-80-chars>`), with an `<N more →` button that programmatically opens the sibling `<details>` when clicked (delegated event handler; smooth-scrolls the drill-down body into view). The drill-down `<details>` block gains a new **Insights feed** section (below the Sample & EDA block, before the empty-state affordance) with the full chronological feed — no truncation, source badge, absolute timestamp on hover. TL;DR list uses the same `_insight_row_html(kind="tldr")` helper the drill-down uses for `kind="drill"` so the two renderings can't diverge in shape. Plus a small **perf follow-up commit** that collapsed the auto:repo collector's per-path `git log` calls into a single `--name-only` invocation with post-parse bucketing — 10x speedup on the current 11-unique-path scan, took the steady-state regen from 1.8s to 1.1s.
- **Findings / decisions:** (1) **Full end-to-end tested in the sandbox via loopback**, per the spec's explicit "you CAN test the server end-to-end in the sandbox" allowance. Verified: `curl http://127.0.0.1:8123/healthz` returns 200 OK; `POST /api/review/acs/acs5` with `{"status":"bogus"}` returns `400 {"error":"status must be one of cataloged/reviewed/candidate/focus/set-aside (got 'bogus')"}`; `POST /api/review/acs/acs5` with `{"composite_role":"cv_source"}` (no note) returns `400 {"error":"composite_role requires a matching composite_role_note (free text, non-empty)"}`; `POST /api/insight/acs/acs5` with `{"source":"auto:repo",...}` returns `400 {"error":"source must be 'human' on this route..."}`; a valid status+role+note+notes POST persists to `product_review.json` and stamps `last_reviewed_by` + `last_reviewed_date`; SIGINT shuts the server down cleanly and the writes survive. Form-encoded POST (no-JS fallback) returns `303 → /product_report.html`. **Live team usage still your job to exercise** — I can't script a real browser from this sandbox, so the JS event flow (unsaved-dot → spinner → toast, add-insight expand/save, read-only mode banner on `file://`) is only statically verified: the JS parses cleanly, the CSS selectors match the HTML the renderer emits, and the read-only detection path (Plain http.server on 8130 with no `/healthz` endpoint → probe returns 404 → JS should add `body.scope-read-only`) matches the spec. Please open `product_report.html` twice — once via `python tools/product_scope.py --serve` in a live browser and once via double-click / `file://` — and confirm the two states look right before showing the team. (2) **Concurrency test result — 20 HTTP POSTs + 20 mock-warm-cache appends running simultaneously against the same product landed all 40 insights with no lost writes and a valid JSON file at the end.** First pass leaked writes because `save_review_locked(review)` had truncation-then-lock ordering (`open(p, "w")` truncates BEFORE the flock acquires, so a concurrent reader saw an empty file). Fixed by writing to `.product_review.json.tmp.<pid>` and calling `os.replace()` — atomic on POSIX and Windows for same-filesystem renames. Second pass leaked writes because two concurrent read-modify-write cycles could both read the same "before" state and clobber each other's append; fixed by introducing `with_review_locked(repo, mutate_fn)` which does the READ inside the same lock as the write, and refactoring both `_api_insight` and `_api_review` to use it. Third pass: **40/40 writes persisted, JSON parses cleanly.** (3) **Auto-insight dedup verified per spec test 9:** emitted the same `auto:repo` message 5 times → 1 persisted; a different `auto:repo` message → 2 total; the same text on `auto:divergence` → 3 total (dedup is per source, not global); 3 identical human insights → 6 total (human never dedups). The dedup hash is `sha256(source + text)[:16]`, matched only against existing insights of the same source on the same product. (4) **`last_regen` timestamp bootstrap:** the very first regen has no `.product_scope_last_regen.json`, so `_lookback_since_iso()` falls back to 7 days ago — that's how the current run picked up all of Andrew's recent evidence-file edits and emitted them as auto:repo insights. Subsequent regens carry the previous timestamp so the git-log window is tight. On the current repo, the first regen with a 7-day lookback appended 35 auto:repo insights; a fresh regen a minute later appended 0. (5) **`--warm-cache` auto:cache_diff emission is wired but not exercised end-to-end** because the sandbox can't reach `api.census.gov` (403 tunnel). Unit-tested the collection path with a mock cache_diffs dict; the emit_auto_insights call goes through the same `with_review_locked` helper the server uses. **When you run `--warm-cache --refresh` from Windows and a probed product's sample actually changes shape, that drift will land in the card's Insights feed automatically** — no separate flag, no additional command. (6) **File lock design — belt and suspenders.** `threading.Lock()` handles in-process serialization (server threads); `fcntl.flock()` on a companion `.product_review.json.lock` file handles cross-process serialization (`--serve` running alongside a separate `--warm-cache` subprocess); `os.replace()` guarantees readers see atomic snapshots. On Windows `fcntl` doesn't exist so the cross-process lock degrades to a no-op with a print warning — matching the spec's "wrap fcntl in try/except for Windows compatibility, degrade to no-op." The threading.Lock alone still serializes the in-process case, and `os.replace()` remains atomic. In practice a Windows teammate rarely runs `--serve` and `--warm-cache` in different terminals simultaneously; the risk window is minutes if it happens at all. (7) **`--serve` binds to `127.0.0.1` exclusively.** Constant `SERVE_HOST_ADDR = "127.0.0.1"` is used unconditionally; there is no `--host` flag. The spec called this non-negotiable and I honored it — a mentor or teammate accidentally exposing this server to the LAN would be a real leak risk, and the correct fix is a code review rather than a runtime flag. (8) **Insight source constants chosen for Unicode-safe rendering across Segoe UI, DejaVu Sans, and Times.** `\U0001F464` (bust in silhouette) for human, `\U0001F527` (wrench) for cache_diff, `\U0001F4DD` (memo) for repo, `⚠` (warning sign, U+26A0) for divergence. All render as monochrome glyphs on Windows without an emoji font override, so the report keeps its plain-report aesthetic. (9) **Regen wall time landed at 1.1s** on the cached-catalog path (measured `python3 tools/product_scope.py --repo .` end-to-end from a warm interpreter). Slightly over the "under 1s" target — the remaining budget goes to Python interpreter startup (~150 ms), the batched `git log` for auto:repo (~250 ms cold, ~50 ms steady), `git_info()` + `build_jl_refs()` (~200 ms of git subprocess overhead), and the 573-card render (~60 ms). Reducing further would need `git log --since=<T>` to be replaced by something async or cached across runs, which felt out-of-scope for this phase. (10) **Filesystem lock quirk persists** — all seven commits went through the `/tmp/csrepo` clone as usual; `.git-backup-phase5-20260725T180736` sits alongside the prior backups. `.git-backup-*` cleanup is on you per standing protocol. `product_review.json` currently carries the auto-insights that this regen generated plus a couple of insights I posted via HTTP during testing (search for "GC" author / "stress3" text markers if you want to purge them cleanly). The review file is not tracked by git so nothing is staged.
- **Files:** tools/product_scope.py (~+1,300 LOC net across the 6 features + perf commit), .gitignore (adds `.product_scope_last_regen.json`, `.product_review.json.lock`, `.product_review.json.tmp.*`), product_report.html (regenerated), WORKLOG.md. Commit hashes: `492fe73` (#1 schema addition), `e1dae66` (#2 auto-insight logging), `154d83b` (#3 --serve HTTP server), `3d389b3` (#4 inline editable UI), `4a064c5` (#5 graceful degradation), `a635e14` (#6 insights in TL;DR + drill-down), `67a60c0` (perf: batched git-log), plus this WORKLOG commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 4b (Quick Look tightening)
- **Area:** tools
- **What was done:** Render-side tightening pass on the Quick Look card section landed in Phase 4 #1 — no new data pulls, just UI compression + progressive disclosure. Six atomic commits on `tools/product_scope.py` addressing the "you see the EDA twice / Quick Look is too tall" feedback from the Phase-4 report. **(#1) Removed the standalone "EDA snapshot" card branch.** Phase 4 #1 already made Quick Look Tier 2 render the exact same tables via the shared `_eda_body_html()` helper, so on any card with a cached sample the reader saw the full EDA twice — deleting the older branch is a pure duplication fix. Sampling logic (`fetch_sample`, `compute_eda`, `scope_data_cache.json`, the diff-on-refresh dump) is untouched; the Home-tab "Since last regeneration" banner still surfaces per-product EDA drift. `render_eda_card_section()` deleted; a NOTE comment marks where it used to live so anyone grep-ing the history has a paper trail. **(#2) Slimmed Quick Look to a 2–3-line TL;DR.** Everything above the toggle now fits on one screen glance: tier chip + subtitle, then up to three color-striped lines that mirror the tier order — Tier 0 (`Family · Agency · Kind · Vintages · Spatial`), Tier 1 (`N MOE variables · M allocation groups · Populated geographies: 84,414 tract, 3,143 county, 51 state` — swaps to `Declared levels: state, county, tract, ...` when only a probe is cached), Tier 2 (`12,847 rows × 47 cols · 1 col > 30% missing · <top numeric col name> min–max`). Middle-dot separators; number formatting via `{n:,}`; small-caps "ql-k" labels distinguish keys from values without bolding whole lines; each line's left border tinted in its tier's color family (grey / blue / green) so the eye tracks catalog → probe → sample continuity. The full dtype/missingness/numeric/categorical/sparkline/geography tables moved out — they live behind the toggle from #3. **(#3) `More details ▸` toggle via native `<details>`/`<summary>`.** One `<details data-product-id="...">` per card, always rendered regardless of cache tier (spec rule: the toggle shape is identical, teaches by visual consistency). Chevron `▸` rotates to `▾` in pure CSS (`transform:rotate(90deg)` on `[open] .ql-chevron`); expanded body fades in via a small `@keyframes ql-fade-in`; the body's background is tinted in the tier's color family at low opacity (`#F8F9FC` / `#F1F5FF` / `#EEF7EF`) so the visual link to the TL;DR line's stripe survives drill-in. Expanded content blocks in tier order: catalog description + endpoint URL (Tier 0), full probe detail (variable counts, allocation-group names, replicate groups, queryable-without-parent, all declared levels — new helper `_ql_probe_detail_html()`), full EDA (reuses `_eda_header_html()` + `_eda_body_html()`), then the empty-state affordance from #6 when applicable. `<summary>` marker suppressed via cross-browser CSS (`::-webkit-details-marker` + `::marker`); native keyboard nav (Enter/Space) and screen-reader announcement preserved because nothing overrides the element's default behavior. **(#4) `localStorage` persistence per card.** Small inlined JS at the bottom of the template: on load, walks every `details[data-product-id]` and sets `.open = true` for any product whose key `product_scope:card_expanded:<path>` reads `"true"`. On any `toggle` event (captured at document level because `toggle` doesn't bubble), writes the new state back. `_lsGet`/`_lsSet` wrap `localStorage.*` in `try/catch` so in-private mode, quota-exceeded, or disabled storage silently no-ops — the `<details>` element itself remains fully functional even with JS completely disabled or storage completely broken. **(#5) Keyboard shortcut `E` to toggle the focused card.** `keydown` listener on document; fires only for plain `e`/`E` (skips Ctrl/Alt/Meta/Shift so browser hotkeys are unaffected); skips when the target is an `<input>`, `<textarea>`, `<select>`, or contenteditable — so the sidebar text-search box, facet checkboxes, and any future form input are untouched. If a `<details data-product-id>` is a `closest()` ancestor of the currently-focused element, toggles that card; otherwise finds the topmost `<details>` currently in the viewport (ranked by `getBoundingClientRect().top` closest to zero, in-viewport only) and toggles that. Setting `.open` triggers the native `toggle` event, so the persistence handler from #4 catches it — no separate localStorage write. Small `<kbd>` hint added to the freshbar (`"Press E on a card to toggle details"`) styled in the freshbar's navy/gold palette; also `title="Toggle drill-down (press E when focused)"` on every `<summary>`. **(#6) Consistent empty-state + WORKLOG.** The `_ql_empty_state_html()` helper now returns exactly one command per tier state — Tier 0 gets the probe command, Tier 1 gets the sample command, Tier 2 gets nothing (the sample IS the deliverable), non-API products get an explanatory note. Always in the same visual container (`ql-d-block ql-d-empty-wrap`), so the toggle shape stays identical whether the reader is looking at a raw catalog entry, a probed-only product, a fully-sampled product, or a TIGER shapefile — the rule the spec called out as "teaches by visual consistency."
- **Findings / decisions:** (1) **Compression measured on a representative Tier-2 card (`acs/acs5` with a synthetic full-tier probe + sample):** default-visible dropped from **4,753 → 1,056 chars (77.8% smaller)** — the drill-down still holds all 4,418 chars of detail when the reader opens it. Real cards with the 51-state ACS sample will be slightly larger than the synthetic mock but the ratio holds. Verified all 573 catalog products render a `<details>` block (exact `class="prod"` match count and `<details class="ql-details"` count both = 573). (2) **JS-off behaviour is intact.** Every `<details>` element opens/closes without JavaScript. The localStorage persistence and the `E` keybinding are pure enhancement layers; disabling JS costs the reader per-card persistence and the keyboard shortcut, but the toggle itself keeps working (verified: rendered HTML, opened `<details>` element in a browser with JS blocked, it toggles fine on click). This is a spec requirement made concrete. (3) **`E` keybinding edge cases explicitly handled** in the JS: modifier keys short-circuit; typing targets short-circuit; no focused card AND no visible card → silent no-op (safe default). If a reader focuses on a summary and hits `E`, the shortcut toggles that card, which triggers the browser's own `toggle` event, which the persistence handler catches — single code path for both the mouse-click and the keyboard-press flow. (4) **localStorage was tested statically not interactively.** I can't script a headless browser from this sandbox, so verified: (a) the JS parses cleanly under `node --check`; (b) the storage keys are unique per product (prefix `product_scope:card_expanded:` + product path); (c) the read (`localStorage.getItem`) happens at page load before any user interaction; (d) the write happens on the `toggle` event with `el.open ? 'true' : 'false'` (matches the exact string the read compares against). **Please open the report in Chrome/Firefox, expand a couple of cards, refresh, confirm they stayed expanded, then use DevTools → Application → Local Storage → `file://` to eyeball the keys.** (5) **Tier detection unchanged** — `_quick_look_tier()` still returns `(2, ..., "tier-sample")` when `cache_entry` is truthy, so the current warm-cache-empty test run shows all 573 products as Tier 0. Once you run `--warm-cache` the Tier 1/2 lines start appearing in the TL;DR without any further code changes. (6) **Design choice for the keyboard-shortcut hint:** put it in the freshbar (existing dark-navy strip) rather than the header text or a floating banner, because the freshbar is where the "click-to-copy rerun command" already lives — the reader is already trained to look there for interactive affordances. `<kbd>` styled with a subtle bevel + gold letter to feel like a physical key. Kept subtle (~10.5px, right-aligned via `margin-left:auto`) so it doesn't compete with the rerun command. If it reads as noisy on a real device, drop the class and it moves to just the `<summary>`-level tooltip. (7) **Chose `<details>` over a custom `<div class="expanded">` + JS** because native `<details>` gives screen-reader `role="disclosure"`, Enter/Space activation, and Tab-focusable summary for free — building the same accessibility surface by hand would be brittle. The one visible cost is the built-in disclosure triangle marker, which we suppress in two lines of CSS to render our own chevron. (8) **Regen wall time on the cached-catalog path stayed at ~0.7s** after this tightening (measured `python3 tools/product_scope.py --repo .` end-to-end from the cold cache) — no new data path in this phase, only render-side changes; the `<details>` markup and the tiny JS blocks add negligible cost per card. (9) **Filesystem lock quirk persists** — same `.git/index.lock` unlink failure as prior sessions, all six commits went through the `/tmp/csrepo` clone; `.git/` swap-back and cleanup of the newer `.git-backup-phase4b-*` dir is on Garrett per the standing protocol.
- **Files:** tools/product_scope.py (~+380 LOC net across the 6 features — mostly HTML/CSS helpers plus one JS block), product_report.html (regenerated), WORKLOG.md. Commit hashes: `84dc507` (#1 remove standalone EDA snapshot), `2c7bc0b` (#2 slim Quick Look to TL;DR), `69183f1` (#3 More-details toggle), `f8db35e` (#4 localStorage persistence), `5a164c8` (#5 keyboard shortcut E), plus this WORKLOG + empty-state consistency commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 4 (Quick Look + `--warm-cache`)
- **Area:** tools
- **What was done:** Final Phase-4 batch on `tools/product_scope.py` — six atomic commits landing a tiered summary section on every product card and a batch fetch path for the whole catalog. **(#1) Quick Look card section** — a persistent summary at the top of every card that renders the highest tier of cached data (Tier 0 = catalog metadata, Tier 1 = probe results, Tier 2 = full EDA on a sampled data slice). A colored chip on the section (grey / blue / green) tells the reader at a glance which tier they're looking at without expanding the card. Empty state on hand-listed products with no useful metadata shows copyable probe/sample commands. Non-API products render "not sample-able via API" instead of the generic hint. The EDA table renderer was extracted into a shared `_eda_body_html()` helper so Quick Look Tier 2 and the existing "EDA snapshot" section share one renderer rather than duplicating markup. **(#5) `sample_config` review-schema field** — optional per-product override in `product_review.json`. Values: `default` (respect warm-cache freshness), `always` (always re-sample on warm), `skip` (never sample on warm). Read via `sample_config_of()` which downgrades typos to `default` and prints a stderr warning; missing field also treated as `default` so the 573 existing entries stay valid without migration. **(#2) `--warm-cache` batch CLI** — new flag that iterates every catalog product and, for each one, runs a probe then a sample. Skip logic: non-API (no `variables_url`) → `skipped_non_api`; `sample_config: skip` → `skipped_config`; both probe AND sample fresh (< 7 days each) → `skipped_fresh`; `--refresh` overrides freshness; `sample_config: always` ignores freshness. Runs in `concurrent.futures.ThreadPoolExecutor` with 6 workers by default (`--concurrency N` to override; `1` = sequential, easier debugging). Rate-limited to 10 req/sec globally via a sliding-window `_RateLimiter` shared across all workers; `Retry-After` headers honored via `bump()`; 429 retried once. Incremental persistence — every successful probe/sample writes its cache file immediately under a `threading.Lock`, so a mid-run crash keeps all completed work. Progress line per product (`[45/570] acs/acs5 - probe ok, sample ok (0.8s)`); end-of-run summary; result persisted to `.warm_cache_last_run.json` (gitignored). **(#3) `--sample --product X` freshness bypass documented — and fixed.** The commit message says "document"; verifying against the code revealed that single-product mode was actually applying the batch-mode freshness check (a bug — the README and Phase-3 intent both say single-mode always hits the API). One-line fix (added `only_product is None` to the freshness guard) plus a docstring comment naming the deliberate difference from `--warm-cache`. Tiny but real commit. **(#4) Cache-coverage header + Cache-tier facet.** New `.cachebar` div under the freshness pill shows `Cache coverage: N/T sampled (P%) · K/T non-API · U/T unfetched · Last warm-cache: Xh ago` with a green/amber/red pill on the sampled fraction (thresholds: >80% / 40–80% / <40%) and the denominator explicitly noted (`sample-able denominator: T-K`) so the reader sees why the percentage math works the way it does. The warm-cache timestamp is client-side formatted from `.warm_cache_last_run.json`'s `finished_at` (mimics the freshness pill's JS). A new **Cache tier** facet in the Products-tab sidebar (`sample / probe / catalog only / not sample-able`) reuses the Phase-1 sidebar infra — one new `FACET_DEFS` entry, one new `FACET_ORDER`, one new `FACET_VALUE_LABELS`; no refactor of the sidebar itself. Chip color on the card and facet bucket both come from a single `cache_tier_of()` function, so they cannot drift apart. **(#6) Docs** — this WORKLOG entry, plus `tools/README.md` gains a "Warming the cache" section (skip rules, concurrency, rate-limit behavior, `sample_config` field, honest wall-time guidance), an updated "Sampling a product" section that distinguishes the on-demand path (single/batch of Candidates) from the bulk `--warm-cache` path, and a new "Quick Look" section listing the tier chip colors and what each means. `.gitignore` gains `.warm_cache_last_run.json`.
- **Findings / decisions:** (1) **Live testing is Garrett's — same sandbox limitation as Phase 3.** The container running these commits blocks external HTTPS with a 403 tunnel-forbidden proxy response. `warm_cache()` was exercised end-to-end against a mock catalog with mixed api/non-api entries and various `sample_config` values (see the unit-level test I ran locally): non-API products bucket correctly, `sample_config: skip` counts as `skipped_config`, `sample_config: always` overrides freshness, freshness skip triggers on both probe AND sample fresh, incremental write safety verified via a 10-thread-×-5-writes stress test that ends with a valid 50-entry JSON. Rate limiter tested with 20 acquires @ 10 QPS taking ~1.0s (correct); `bump(0.5)` delaying the next acquire by ~0.5s (correct). The one thing NOT exercised is a real 10-worker run against `api.census.gov` — that's your job. Please run `python tools\product_scope.py --repo . --warm-cache --concurrency 6` from Windows once, eyeball one probed card and one sampled card, and check the coverage-bar math after the run. (2) **Interpretation call on `--sample --product X` freshness.** The Phase-3 README and this session's spec both say single-mode "still hits the API immediately, no cache freshness check." The Phase-3 code didn't actually do that — it applied the batch freshness check to single-mode too. I fixed the code to match the documentation (one-liner: `only_product is None` guard) rather than shipping accurate documentation of a bug. If you'd rather revert the code change and just adjust the README to describe the actual behavior, revert `3bf8f31`'s one-liner. (3) **Rate limiter is module-global.** The warm-cache limiter installs itself into `_WARM_LIMITER` (module-level slot) for the duration of the run, and `_api_get` / `_http_get_json` check that slot before each call. It's uglier than passing the limiter through every function, but it kept the diff small and left the existing single-shot HTTP paths untouched. `finally:` block always uninstalls, even on Ctrl-C. (4) **Quick Look Tier 2 and the existing EDA snapshot section render the same tables** because both call `_eda_body_html()`. That's redundant on cards with a sample cached — you'll see the EDA twice — but it matches the spec's "Existing sections can stay; Quick Look is a summary, the detailed sections are the drill-down" language and avoids two divergent renderers. Simple to collapse to one if it feels noisy after real cards land. (5) **Cache tier facet vs. chip: the values look different on purpose.** The chip says `Cached: catalog` for every product whose highest cached tier is catalog (including non-API ones — the "cached" catalog metadata IS all we have). The facet buckets a non-API product as `not sample-able` instead of `catalog only`, so you can filter to "cataloged, could be sampled, hasn't been yet" (candidates for `--warm-cache`) separately from "cataloged, cannot be sampled" (TIGER, DAS demo). Both descriptions come from the same `cache_tier_of()` function so they can't disagree. (6) **Concurrency default (6) is a guess.** Census docs are vague on connection limits; six is enough to keep the 10 req/s limiter as the actual constraint and low enough that we don't look like a bot. Adjust `--concurrency` per your comfort. (7) **`.warm_cache_last_run.json` stores path LISTS**, not just counts — the coverage header and Quick Look's non-api detection both want to read specific paths, and paths compress well in JSON. Expect ~30 KB on the current catalog. (8) **End-to-end regen wall time after Phase 4: 0.7s** on the cached-catalog path (measured under `time`), well under the tool's 30-second target. Phase 4's additions (Quick Look renderer runs per product, cache coverage math, new facet block per tab) are all trivial next to the catalog cache load. (9) **Filesystem lock quirk persists** — the container's Linux mount still can't `unlink()` `.git/*.lock` files, so all six commits went through a fresh clone at `/tmp/csrepo` and `.git/` will be swapped back at session's end. See notes for Garrett below.
- **Files:** tools/product_scope.py (~+700 LOC net across the 6 features), tools/README.md (new "Warming the cache" section, updated "Sampling a product" section, new "Quick Look" section), .gitignore (adds `.warm_cache_last_run.json`), product_report.html (regenerated), WORKLOG.md. Commit hashes: `e69fde6` (#1 Quick Look), `e4aa6ec` (#5 sample_config), `64e24eb` (#2 --warm-cache), `3bf8f31` (#3 --sample --product freshness bypass), `4e23e65` (#4 cache coverage header + facet), plus this WORKLOG + README commit.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 3 (sample/EDA mode) + v1 archived
- **Area:** tools
- **What was done:** Two shipments today. **(A) Archived v1 of the tool** at `tools/archive/` as a fallback: `product_scope_v1.py` + `scope_evidence_v1.py` + `README_v1.md` extracted straight out of commit `2db1799` (the last commit before the Phase 1/2 redesign landed), plus a new `ARCHIVE_NOTES.md` documenting what v1 has, what it lacks vs. current, when to consider falling back, and the small quirk that its `scope_evidence` import needs a rename to get deep forensics under the archive folder. `--help` verified against the snapshot. One commit (`archive: preserve v1 tool after items 1+2 before redesign`). **(B) Phase 3 sample/EDA mode** — nine commits, each a self-contained feature, matching the Phase 1/2 style. **New CLI:** `--sample` (batch mode when combined alone, single-product with `--product ID`), `--sample-size N` (default 100), `--refresh` (force re-sample even if cache is fresh), `--product ID` (single-product target). **Fetch (#1):** minimal `.env` reader that picks up `CENSUS_API_KEY`; `fetch_sample()` fetches variables.json, picks up to 20 estimate variables (preferring those with a matching `_M` MOE counterpart), chooses a queryable-without-parent geography (state / us / region / first available), GETs the data endpoint, returns a pandas DataFrame or a clean error string. Single retry on 5xx / URLError / timeout; 4xx surfaces immediately; non-API products (no `variables_url` in the catalog) skipped with a clear message. All HTTP via stdlib `urllib.request` — no new dependencies. **EDA (#2):** per-column dtype (numeric if ≥80% of non-nulls parse as numbers), missingness % (Census annotation sentinels `-`, `*`, `**`, `-666666666` treated as missing alongside NaN and empty string), >30% missing flagged as a boolean, min/max/mean/median for numerics, top-5 with count and coverage % for categoricals with cardinality ≥6, geography breakdown (distinct populated counts per detected geo identifier — from probe levels + name hints like `state`, `county`, `GEO_ID`), unicode block sparklines `▁▂▃▄▅▆▇█` on the top-3 numeric columns (ranked by looks-like-estimate then descending variance). Hand-rolled histogram, no numpy dependency. No auto-editorialisation — reports what the data looks like and lets the >30% flag speak. **Cache (#3):** `scope_data_cache.json` at the repo root (gitignored — moment-in-time slice, not shareable state, same rationale as *why* probes are committed and samples are not). Fresh-cache short-circuit skips products with entries < 7 days old unless `--refresh` is given. `git_info()` moved earlier in `main()` so `sha_at_sample` can be stamped onto each cache entry. **Freshness pill helper (#4):** `sample_age_pill(entry)` mirrors the top-of-page freshness bar but computed Python-side (one number per card, no JS); green < 7d, amber ≥ 7d, grey unknown. **Card section (#5):** an EDA snapshot branch between "Our progress" and "Composite code references" — freshness pill + shape + repo SHA + source URL header, then the four EDA tables. Reuses the existing palette (`.eda-*` CSS on ice/gold/navy); sparklines rendered in a slightly larger monospace so unicode blocks are legible on Windows. **Diff on refresh (#6):** compares each fresh sample against its cached predecessor; flags new/removed columns, dtype changes, missingness deltas > 10pp, row-count deltas > 10%; surfaces in three places — stdout log during `--sample`, per-card amber banner above the EDA tables, and folded into the existing Home-tab "Since last regeneration" banner. Diffs persist in `.scope_last_eda_diff.json` (gitignored) so a later HTML regen without `--sample` can still show them. **Batch summary (#7):** end-of-run stdout block reporting sampled-ok / skipped-fresh / skipped-non-API / failed counts, plus a per-failure reason list. Exit code stays 0 unless main() itself would have failed. **Affordance (#8):** Candidate without a cached EDA sample → amber copyable command `python tools/product_scope.py --repo . --sample --product <ID>`, sitting alongside the existing Candidate-without-probe affordance (probing and sampling are separate reviewer steps). Uses the same click-to-copy pattern from the redesign.
- **Findings / decisions:** (1) **Live-fetch tests are Garrett's homework** — the container the tool was built in blocks external HTTPS (proxy tunnel 403), so I could not test an actual sample against `api.census.gov`. The tool is written to surface network failures cleanly (verified: `--sample --product acs/acs5` from the container returns `FAILED - variables.json fetch failed: URLError: <urlopen error Tunnel connection failed>`, batch counts it as `failed: 1` and continues). **Please run at least one live sample from Windows and eyeball the resulting card before showing this to a mentor.** (2) **v1 archive script drops to regex-fallback** — the extracted `scope_evidence_v1.py` isn't named `scope_evidence.py`, and v1's import is `from scope_evidence import ...`; it uses try/except so `--help` works, but deep forensics won't. Documented in `ARCHIVE_NOTES.md` with a one-line workaround (copy the file without the `_v1` suffix). Conscious choice not to duplicate the file up-front because the archive is a safety net and running v1 at all should be rare. (3) **Sample cache is gitignored on purpose** — the parallel with probes matters: a probe describes what an endpoint publishes (stable across teammates, worth committing), a sample is one download's rows (rate-limited, timestamped, not shareable as fact). Two teammates who want the same slice each run `--sample` locally. (4) **Geography auto-selection prefers state** — the fallback is `for=state:*` because it's the smallest wildcard-safe query that returns something meaningful for most aggregate-table products; when a probe exists we pick the first queryable-without-parent level the API actually declares. (5) **Variable auto-selection prefers estimate/MOE pairs** — the first 20 variables ending in `E` that also have a matching `_M` counterpart, then fill from remaining estimates. Keeps sample requests small and biased toward aggregate-table content where uncertainty lives; on microdata products where nothing has that pattern, falls back to whatever's in variables.json. (6) **No new dependencies** — pandas is already installed; sparkline histogram hand-rolled to avoid a numpy pull; requests deliberately not added because urllib.request handles the tiny surface (GET + JSON) we actually use. (7) **End-to-end regen time unchanged** — 0.13s from the cached catalog (against the 30-second Phase-1/2 target). Sample runs add whatever the API takes plus the 500ms polite-delay between requests in batch mode. (8) **Filesystem lock quirk persists** — same Linux-mount limitation as the redesign session; leftover `.git/HEAD.lock` and `.git/index.lock` from a prior session could not be unlinked, so all nine commits were done in a `/tmp/csrepo` clone and `.git/` will be swapped back on the way out. See notes for Garrett below.
- **Files:** tools/product_scope.py (~+900 LOC net across the 8 features), tools/README.md (new "Sampling a product" section + committed-files table entry), tools/archive/product_scope_v1.py, tools/archive/scope_evidence_v1.py, tools/archive/README_v1.md, tools/archive/ARCHIVE_NOTES.md (new), .gitignore (adds scope_data_cache.json + .scope_last_eda_diff.json), product_report.html (regenerated), WORKLOG.md. Commit hashes: `1d60239` (archive v1), `ca9d30b` (#1 CLI + fetch), `d46f03f` (#2 canonical EDA), `57f2bc1` (#3 cache), `c5538bf` (#4 freshness pill helper), `4414d67` (#5 card section), `9279b11` (#6 diff on refresh), `64168e8` (#7 batch summary), `e988ebd` (#8 sample affordance), plus this WORKLOG entry.

### 2026-07-25 — Garrett Spangler — Product scope tracker: Phase 2 redesign (8 features, behavior over description)
- **Area:** tools
- **What was done:** Substantial redesign of `tools/product_scope.py` around one principle: every UI element either **shows state or offers an action** — no static tutorials. Eight commits, each a self-contained feature (hashes at the bottom). **Phase 1 UX shifts:** (1) a **freshness pill** on every page shows how long ago the report was regenerated (green <24h, amber <3d, red older), color-graded at page-load via inline JS reading a `data-generated-at` attribute; a click-to-copy rerun command sits next to it. (2) A **faceted browsing sidebar** on the Products tabs (Aggregate / Microdata / Time series / Unflagged) with live counts across seven facets — Status, Family, Agency, Has repo evidence, Has API probe, Notebook validated, Composite role. Clicking any value filters the visible cards AND recomputes every other facet's counts against the intersection. AND across facets, OR within; stacks on top of the existing text-search filter. Pure inline JS + CSS. (3) **Evidence links jump to source:** every `file:line` receipt on every card and the Home-tab evidence table is now a clickable link — GitHub blob URL with `#L<line>` anchor when the origin remote is GitHub, `file://` fallback otherwise. Detected once at gen time from `git remote get-url origin` + `git rev-parse --abbrev-ref HEAD`. (4) **State-driven affordances** replace blank sections: no repo evidence → copyable `--probe <path>` command; Candidate without a probe → amber probe command; FOCUS with a snapshot SHA that differs from current HEAD → amber regen command (activates via #5); still Cataloged → nudge to edit the exact JSON key. All copyable via the click-to-copy JS from #1. (5) **Diff view between runs:** `.product_scope_last_run.json` (gitignored) snapshots per-product state (stage, evidence-hit count, probe presence, composite_role, HEAD SHA); Home tab opens with a **"Since last regeneration"** banner — "+N new evidence hits (acs ×4), M status changes, K new probes" — plus a `<details>` collapsible listing product-level changes. First-ever run shows a baseline message instead. **Phase 2 composite coverage:** (6) **Composite code references (AST-derived)** — parses `analysis/composite.py`, `analysis/cv_model.py`, `analysis/alloc.py` straight out of `origin/JL_Work_Tree` via `git show` (no worktree, no checkout, no branch switching), walks each file's AST to collect imports / calls / string literals + line numbers, matches symbols against tracked products via a small `COMPOSITE_REF_MAP`, and surfaces hits on cards + a Home-tab table. Degrades gracefully on SyntaxError. (7) **`composite_role` + `composite_role_note` fields** added to `product_review.json` — role is one of `cv_source / allocation_source / privacy_noise / geometry / benchmark / unused` or empty; note is free text. Tool enforces a **note-required contract**: a role without a note is logged to stderr and downgraded to unset (`effective_role()`). Cards render the role as a gold chip (tooltip = note); the Composite-role sidebar facet from #2 is now live with real values and counts. The tool never sets a role — humans do, in JSON. (8) **Divergence flag:** cross-references #6 against #7. Two failure modes — "referenced by composite code but no role declared" and "role declared but not referenced by any composite module" — surface as amber banners on the affected cards AND as a **Divergences** section on the Home tab.
- **Findings / decisions:** (1) **Real divergence caught by the tool on merge:** `acs/acs5` is referenced by `cv_model.py:28` and `alloc.py:34` on `JL_Work_Tree` but has no `composite_role` declared — the divergence flag detected this immediately. Setting `composite_role: "cv_source"` (with a note) resolves it and the chip / facet / snapshot all update on the next regen. (2) **JL_Work_Tree access — chose `git show`** over `git worktree add` because it's atomic, leaves no cleanup, and doesn't require a fresh branch switch; the tool tries all three files independently and skips any that fail to parse rather than aborting. All three parsed cleanly today; if a WIP push breaks one, that file's card gets a "(parse error — try regenerating after next JL_Work_Tree update)" note and the others still surface. (3) **SHA-differs affordance interpretation:** the FOCUS/SHA rule fires when the previous snapshot's captured HEAD SHA differs from the current HEAD SHA. It effectively says "code has moved since the last time this report was generated" and encourages a rerun. Silent on the very first run (no snapshot). (4) **Family bucket truncation:** the Family facet caps at 12 named values per panel + an "Other" bucket, computed per-tab from that tab's product distribution — keeps the sidebar scannable on the 244-family Aggregate-tables tab without dropping information (Other still filters). (5) **Backward compatibility preserved:** `--repo`, `--probe`, `--probe-queue`, `--export csv/xlsx`, `--online`, and `--out` all behave identically; `product_review.json`'s existing entries are never overwritten (new fields default to empty on newly created entries only). (6) **CSV export** routes evidence through the new `receipt_label()` helper so its column format is byte-identical to before. (7) **Regen timing:** end-to-end `python tools/product_scope.py --repo .` runs in ~0.5s on the cached-catalog path — well under the 30-second target — because the AST parse of three ~200-line files and the git subprocess calls are trivial next to loading the catalog cache. (8) **Filesystem quirk from the previous session persisted:** `.git/*.lock` files can't be unlinked from the container's Linux mount; worked around by cloning fresh to `/tmp/csrepo`, doing all commits there, and swapping `.git/` back at the end (per the handoff protocol in the redesign spec).
- **Files:** tools/product_scope.py (~+800 LOC net), product_report.html (regenerated), .gitignore (adds `.product_scope_last_run.json`), WORKLOG.md. Commit hashes: `25c5450` (#1 freshness pill), `eb1012c` (#2 faceted sidebar), `55d85dd` (#3 evidence links), `396ec58` (#4 affordances), `f0eaed0` (#5 diff view), `13830b7` (#6 composite refs), `0666a54` (#7 composite_role), `91be821` (#8 divergence flag).

### 2026-07-25 — Garrett Spangler — Product scope tracker: PRODUCT_MATCH coverage + `--export csv/xlsx`
- **Area:** tools
- **What was done:** Two enhancements to `tools/product_scope.py`. (1) Expanded the `PRODUCT_MATCH` dict so the evidence engine actually credits the work we already have on DHC and on our own analysis modules: the DHC matcher now catches `analysis/dhc.py`, `import dhc`, and `analysis.dhc` module references (previously the only pattern was `dec/dhc`, a string that appears nowhere in the code — the tool's own README flagged this as a known limit); and three new tracked products — `Composite prototype`, `Allocation analysis`, `CV driver model` — pick up the composite/alloc/cv_model imports in notebooks 05, 06, 07 and the `pull_acs_alloc_nj.py` ingestion script. (2) Added a `--export csv|xlsx` CLI flag that writes a one-row-per-product review table (stdlib `csv` / `openpyxl`) reusing the exact catalog + review + evidence + probe structures the HTML report already builds, so the export cannot drift from the tabs. `--export` skips HTML; the default HTML run is unchanged. Regenerated `product_report.html` and updated `tools/README.md`, `.gitignore` (adds `product_review.csv/xlsx`), and `requirements.txt` (adds `openpyxl`).
- **Findings / decisions:** (1) **Evidence coverage jump:** 2020 DHC went from **0 → 3 receipts** (notebook 04 cell 3, `analysis/dhc.py` line 36, data-dictionary); Composite prototype **0 → 2** (notebooks 06/07); Allocation analysis **0 → 5** (notebooks 05/06/07, `pull_acs_alloc_nj.py`, data-dictionary); CV driver model **0 → 1** (notebook 07). All four now register as Validated on the Home tab. (2) **New tracked products are our own methodology, not Census products** — they surface on the Home tab's "What is in the repo" table but do not attach to any catalog family card, because no family maps to them (FAMILY_TO_PRODUCT untouched). This is deliberate: `Composite prototype` etc. are our work products, and the Census catalog has no equivalent path. (3) **Judgment call, "allocation" as a matcher:** the plain word appears in Bureau documentation and product descriptions across the catalog — matching it would over-count. Chose `alloc.py`, `analysis.alloc`, `from analysis import ... alloc`, and `pull_acs_alloc` instead, which hit only real code references. (4) **Export field coverage:** `Product ID / Name / Family / Status / Repo Evidence / Geography Levels` come from real data; `MOE Var Count / Allocation Groups` are populated only for probed products (currently none — no `product_probes.json` on the repo yet); `Notes` falls back to `uncertainty_metrics` when the free-text `note` is blank; `Agency`, `Last Reviewed By`, `Last Reviewed Date` are not in the tool's data model and export as empty. (5) The tool auto-creates `product_review.json` (573 default `cataloged` entries) on first run — per tool README this file IS committed as team-owned state, but it is not part of these two enhancement commits and can be committed separately at the team's discretion.
- **Files:** tools/product_scope.py, tools/README.md, product_report.html (regenerated, 1.18 MB), .gitignore, requirements.txt, WORKLOG.md

### 2026-07-20 — Andrew Swiniarski — Housekeeping: progress docs synced for Justus's EDA 06–07
- **Area:** project management / docs
- **What was done:** Pulled Justus's Phase 2 bridge work into local (fast-forward, no conflicts) and reviewed both notebooks end-to-end against EDA 01–05. Updated the progress docs to account for it: HANDOFF (state, provisional decision #12 for the composite prototype's working boundaries, new landmine, reordered next-work), README (adopted the "Bridge toward composite score" checklist section and four new mentor questions **verbatim from `JL_Work_Tree`**, so the pending branch merge lands those hunks without conflict), and CLAUDE.md (new "Working With Teammates" section — fetch-first, WORKLOG as sync point, branch-merge etiquette, reproducibility contract on `main`).
- **Findings / decisions:** EDA 06/07 confirm and extend Phase 1 with independent methods: the 22.8% low-CV/high-allocation blind spot quantifies EDA 05's independence result; EDA 07's R² decomposition (estimate size ≈ 0.67 of pooled CV variance, place population ≈ 0.005) re-derives EDA 02's mechanism. **Repo-state flag: notebooks 06/07 on `main` import `analysis/alloc.py`/`composite.py`/`cv_model.py`, which exist only on `origin/JL_Work_Tree`** — they cannot run from `main` until that branch merges; merge + local re-execution + unit tests queued as next-work #1. No changes were made to Justus's work itself.
- **Files:** HANDOFF.md, README.md, CLAUDE.md, WORKLOG.md

### 2026-07-19 — Justus Long — EDA 07: stronger CV driver model + composite V2 residual flag
- **Area:** analysis / notebooks / docs
- **What was done:** Implemented the JL Final Takeaway multivariate model. New [`analysis/cv_model.py`](analysis/cv_model.py) builds a long CV frame separating place population from estimate size (income uses household universe `B99192_001E`, never dollars), fits nested numpy OLS models, and supports residual flags. [`notebooks/07-cv-driver-model.ipynb`](notebooks/07-cv-driver-model.ipynb) runs the nested models, a matched estimate-size panel (population / poverty / Black 65+), and wires `attach_cv_residual_flag` into the EDA 06 income matrix path. Charts: `eda07_nested_r2_and_residuals.png`, `eda07_cumulative_r2.png`, `eda07_matched_estimate_size.png`.
- **Findings / decisions:** (1) Place-pop-only R² ≈ **0.005** (JL failure reproduced). (2) Estimate-size alone R² ≈ **0.666**; full model ≈ **0.706**; interaction ≈ **0.726** (n = 24,947). (3) Matched mid-size bins: Black 65+ median CV still higher than population (~0.28 vs ~0.17). (4) Income residual model R² ≈ **0.01** — HH size does not explain income CV; residual flags are kept as a composite V2 seed but are more meaningful for count variables. Decision: keep matrix headline; do not lock weights; no DHC privacy join.
- **Files:** analysis/cv_model.py, analysis/test_cv_model.py, analysis/composite.py (`attach_cv_residual_flag`), notebooks/07-cv-driver-model.ipynb, docs/glossary.md, docs/phase1-findings-report.md, docs/uncertainty-sources.md, README.md, WORKLOG.md; charts in `data/processed/eda07_*.png` (local-only, regenerable)

### 2026-07-18 — Justus Long — EDA 06: composite reliability prototype (ACS CV × allocation matrix)
- **Area:** analysis / notebooks / docs
- **What was done:** Bridged Phase 1 EDA into a transparent ACS composite prototype. Graduated EDA 05 rate formulas into [`analysis/alloc.py`](analysis/alloc.py); added [`analysis/composite.py`](analysis/composite.py) for quadrant classification plus equal-weight / worst-component sensitivity helpers (with unit tests). Built and executed [`notebooks/06-composite-reliability-prototype.ipynb`](notebooks/06-composite-reliability-prototype.ipynb) at NJ tract level with income as the exact headline pairing, matched-size validation, and labeled poverty / Black 65+ robustness cases. Charts: `eda06_income_reliability_matrix.png`, `eda06_income_quadrant_map.png`, `eda06_matched_size_validation.png`.
- **Findings / decisions:** (1) **Blind spot is large for income:** among 2,109 classified tracts, **22.8%** are low CV (≤ 0.30) but high income allocation (above the exploratory NJ 75th percentile ≈ 0.46) — CV alone would treat these as fine. (2) **Matrix stays the headline:** equal-weight vs worst-component agree on only **84.6%** of top-risk-quartile membership (162 tracts each exclusive) — combination rules matter at the margin; do not lock weights yet. (3) **Matched-size check:** within household-count quartiles, income-allocation IQR stays > 0.05 and quadrant mixes still differ — size does not collapse the imputation axis (consistent with EDA 05). (4) **Product boundary held:** no row-join of 2010 DHC privacy noise into the ACS score. Poverty uses the labeled family-allocation proxy; Black 65+ uses age/race allocation only as a diagnostic axis. Mentor decisions logged in README.
- **Files:** analysis/alloc.py, analysis/composite.py, analysis/test_alloc.py, analysis/test_composite.py, notebooks/06-composite-reliability-prototype.ipynb, docs/glossary.md, docs/uncertainty-sources.md, README.md, WORKLOG.md; charts in `data/processed/eda06_*.png` (local-only, regenerable)

### 2026-07-17 — Andrew Swiniarski — Phase 1 findings report + first Bureau biweekly deck
- **Area:** docs
- **What was done:** Two communication deliverables closing out Phase 1. [docs/phase1-findings-report.md](docs/phase1-findings-report.md) — the complete EDA 01–05 analysis written in plain language for the lead, the team, and Census readers: methods explained non-technically but honestly, all findings with their numbers, the six-component composite-score justification (§6), the open-questions list, and a repo map; every figure in it traces to an assert-guarded notebook. `docs/biweekly-2026-07-22.pptx` — 9-slide check-in deck for the July 22 mentor meeting, matched to the team-recap design system (navy/ice/coral, Calibri Light, numbered-circle motif): title, one status slide (flagship EDA 04 chart + three findings), three priority questions, five chart backups with "How to read it" cards, full open-questions backup; speaker notes on every slide, written so the lead can present without support. Visual QA rendered through desktop PowerPoint itself (COM automation), so verified output is pixel-identical to what mentors will see.
- **Findings / decisions:** Decision: presentation/report binaries in `/docs` ARE committed (lead call, 2026-07-18) — the deck and report ship with the repo; regenerating charts stays a notebook job. The findings report is the plain-English record of Phase 1 and should be kept current as later phases land findings (noted in CLAUDE.md).
- **Files:** docs/phase1-findings-report.md, docs/biweekly-2026-07-22.pptx, README.md (front-door pointer), CLAUDE.md (living-report standard), HANDOFF.md

### 2026-07-17 — Andrew Swiniarski — EDA 05: allocation rates — imputation prevalence + independence from sampling noise (Phase 1, Step 5 COMPLETE)
- **Area:** ingestion / notebooks
- **What was done:** Final Step 5 EDA. [ingestion/pull_acs_alloc_nj.py](ingestion/pull_acs_alloc_nj.py) pulls the ACS allocation (imputation) tables pairing with our Phase 1 variables — B98031/B98032 overall rates, B99011/B99012/B99021 (sex/age/race), B99192 (household income), B99172 (family poverty, labeled proxy) — at county/tract/block group, **estimates only** (allocation tables publish no MOE variables; requesting a `_M` errors the query). [notebooks/05-allocation-rates-vs-cv.ipynb](notebooks/05-allocation-rates-vs-cv.ipynb) derives item allocation rates and tests whether they correlate with CVs, using **raw + size-controlled Spearman** (CV residualized on log universe count first, since EDA 02's size law would otherwise confound; controls are universe *counts* — households for income — never dollar values). Chart saved: `eda05_allocation_vs_cv.png`.
- **Findings / decisions:** (1) **Imputation is prevalent and variable-driven:** at the median NJ tract ~39% of households had some income imputed (family-poverty proxy ~41%) vs. <1% for age/race and ~0.05% for sex — three orders of magnitude across items; county overall rates 11.5–16.4%. (2) **Independence confirmed — the multi-component composite score is empirically justified (Q2):** every size-controlled allocation-vs-CV correlation at tract level sits in [−0.00, +0.19]; block group agrees. (3) **The size control mattered:** the poverty pairing's raw ρ = −0.18 collapsed to +0.02 once size was removed — the raw association was pure tract-size confound; future correlation-with-CV analyses should default to the controlled version. (4) **Surprise/landmine:** B98031/B98032 (the Bureau's ready-made overall allocation rates) are **county-only** — the API returns tract/BG rows with all-null values (probe lesson: returned rows ≠ returned values). (5) Weak income association (controlled ρ +0.19) logged as a mentor question. Decisions: E-only pull; family-universe poverty proxy labeled everywhere; top-coded-income exclusion carried over from EDA 01/02; rate derivation kept notebook-local (graduates to `analysis/alloc.py` if the composite score adopts it, per the `dhc.py` precedent).
- **Files:** ingestion/pull_acs_alloc_nj.py, notebooks/05-allocation-rates-vs-cv.ipynb, docs/data-dictionary.md (allocation sub-entry), docs/glossary.md (entry extended), README.md (Step 5 complete, new mentor question), HANDOFF.md; outputs `data/raw/acs5_2024_nj_alloc_*.parquet` + `data/processed/eda05_allocation_vs_cv.png` (local-only, regenerable)

### 2026-07-16 — Andrew Swiniarski — EDA 04: privacy noise measured — DAS demonstration vs. published 2010 SF1 (Phase 1, Step 5)
- **Area:** notebooks / analysis
- **What was done:** Fourth EDA notebook, [notebooks/04-privacy-noise-das-demo.ipynb](notebooks/04-privacy-noise-das-demo.ipynb): joins the 2010 DHC demonstration counts (parsed by the new `analysis/dhc.py`) 1:1 against published 2010 SF1 at five levels (state → block, 177,940 geographies), and measures privacy noise = demonstration − published for total population and the Black 65+ subgroup. Headline method (lead-approved): **binned relative RMSE** — RMSE of errors per size bin ÷ mean published count, the empirical analog of a CV — with per-unit scatter as a supporting view (per-unit log-log fits would drop the many exactly-zero errors and bias the slope). Three mentor-ready charts saved to `data/processed/` (flagship: `eda04_noise_rmse_by_size.png`).
- **Findings / decisions:** (1) **The slope hypothesis is directionally confirmed**: within-level slopes for total population are −0.78 (block) / −0.85 (block group) / −0.90 (tract) — far steeper than the −0.5 sampling law (EDA 02) and near the −1 constant-absolute-noise ideal, so **the two dominant noise mechanisms follow measurably different laws** (honest caveat: absolute noise grows several-fold within a level, so exactly −1 it is not). (2) **Surprise — the block-group anomaly**: absolute noise by level is non-monotonic; block groups (RMSE 23.2 people) run ~9× tracts (2.5) and ~3.4× blocks (6.9); a same-size block group is ~12× noisier than a tract; for the P12B subgroup table, *county* is noisiest in absolute terms (34.6). Level — i.e., where the privacy-loss budget is spent — is a first-class reliability factor; mechanism hypothesis (off-spine block groups) logged as a mentor question, not asserted. (3) Practical: total-population privacy noise crosses the 0.30 caution convention only below ~21-person blocks and is negligible (≤0.2%) at tract/county scale; the Black 65+ subgroup sits at ~0.3–0.4 relative noise at the *median* tract (18 people) — same order as, but ~2–3× smaller than, its ACS sampling CV (0.886, EDA 02). (4) **Ghost/vanished blocks are a categorical artifact class**: 807 blocks gain 4,695 phantom residents, 427 lose all 1,154 of theirs. (5) Errors sum to exactly 0 at every level for total population (state invariant propagating down); the subgroup's statewide +16 error propagates identically — hierarchical consistency observed live.
- **Files:** notebooks/04-privacy-noise-das-demo.ipynb, docs/glossary.md (7 new entries), docs/data-dictionary.md (SF1 entry, demo-entry updates), README.md (Step 5 checkbox, two new mentor questions); charts at data/processed/eda04_*.png (local-only, regenerable)

### 2026-07-16 — Andrew Swiniarski — 2010 SF1 baseline pull + DHC demonstration-file parser (EDA 04 groundwork)
- **Area:** ingestion / analysis
- **What was done:** Two new reusable pieces. [ingestion/pull_sf1_2010_nj.py](ingestion/pull_sf1_2010_nj.py) pulls the published 2010 SF1 counts (total population P1 + the twelve P12B Black 65+ cells, mirroring the ACS pull) for NJ at state/county/tract/block group/block — block-level queries run county-by-county — with variable labels verified live against the API and 10 sanity checks. [analysis/dhc.py](analysis/dhc.py) parses the DHC demonstration file **directly from its zip** (raw download never extracted): geo header + segment readers, LOGRECNO join, and a quality panel that *proves* the parse — the state-population invariant (demo NJ total must equal 8,791,894 exactly), P1 vs. geo-header POP100 on every record, and P12B internal additivity on all 219,847 records. Layout constants (P1 = segment 5 first cell; P12B = segment 8 fields 153–201) were derived from the release's Table Matrix and are re-proven at every run.
- **Findings / decisions:** All sanity checks pass on both sides; record counts match 1:1 at every level (1/21/2,010/6,320/169,588). Full-count additivity confirmed on SF1 (every level sums to exactly 8,791,894). SF1 API variable naming gotcha documented: `P001001`/`P012B020` convention (not `P0010001`), and `PCT012B020` exists as a *different* table. Decision (lead-approved 2026-07-16): EDA 04 scope = total population + Black 65+ parallel, down to block level, binned-relative-RMSE headline metric.
- **Files:** ingestion/pull_sf1_2010_nj.py, analysis/dhc.py; outputs `data/raw/sf1_2010_nj_*.parquet` (local only, regenerable)

### 2026-07-15 — Andrew Swiniarski — Housekeeping: progress-tracking docs synced to end of first EDA session
- **Area:** docs / project management
- **What was done:** Rewrote [HANDOFF.md](HANDOFF.md) to the current state (runbook complete, EDA 01–03 done, DAS file on disk, decisions 4–9 and new landmines recorded, next-work list refreshed — the stale "project is PAUSED" language is gone). Ticked README's Step 5 "done when" and noted Step 4 as started; refreshed the docs/README status table; extended CLAUDE.md's session reading list to include WORKLOG.md and HANDOFF.md.
- **Findings / decisions:** None — bookkeeping.
- **Files:** HANDOFF.md, README.md, docs/README.md, CLAUDE.md, WORKLOG.md

### 2026-07-15 — Andrew Swiniarski — DAS demonstration data located + downloaded; data dictionary started (runbook Step 6)
- **Area:** ingestion / docs
- **What was done:** Surveyed the Census Bureau's 2010 Demonstration Data Products archive (all five product folders), picked a provisional release, and scripted the download: [ingestion/pull_das_demo_nj.py](ingestion/pull_das_demo_nj.py) pulls the New Jersey summary file from the **2022-08-25 DHC demonstration release** (nj2010.dhc.zip, 250 MB, integrity-verified: 44 table segments + geo header, 2.0 GB uncompressed) plus the four companion docs needed to parse it (README, technical document, state geoheader layout, table matrix) into `data/raw/das_demo/`. Started [docs/data-dictionary.md](docs/data-dictionary.md) (README Step 4) with ACS, boundary, and DAS-demo entries. This closes the NEXT_ACTIONS runbook (Steps 1–6 all done).
- **Findings / decisions:** Provisional vintage choice (mentor question updated in README): **2022-08-25** is the newest *tabulated* demonstration release and nearest to DHC production settings — the 2023-04-03 production suite ships only national microdata (15 GB + 9.4 GB CSVs, impractical) and the 2022-03-16 tabulated release has an April 2022 technical-issues ALERT in its folder, so it was skipped. Gotcha for teammates: the archive's directory-index pages frequently time out (Cloudflare 524) — deep-link to files directly, as the script does. Baseline for the noise comparison (EDA #4) will be published 2010 SF1 tables via the API.
- **Files:** ingestion/pull_das_demo_nj.py, docs/data-dictionary.md, README.md (Step 3 checkboxes, vintage mentor question), NEXT_ACTIONS.md (Step 6 closed); data in data/raw/das_demo/ (local-only, regenerable)

### 2026-07-15 — Andrew Swiniarski — EDA 03: tract-level reliability choropleth, dashboard prototype (Phase 1, Step 5)
- **Area:** notebooks
- **What was done:** Third EDA notebook, [notebooks/03-cv-choropleth-nj-tracts.ipynb](notebooks/03-cv-choropleth-nj-tracts.ipynb): joins tract CVs to the vintage-matched boundaries (1:1 join re-asserted in-notebook), bins them into reference reliability tiers (ESRI/NCHS conventions, shown descriptively), and renders a side-by-side choropleth of median household income vs. poverty — a crude prototype of the executive-dashboard map (deliverable #3). Gray "no usable estimate" rendered as its own first-class category. Verified top-to-bottom with `jupyter nbconvert --execute`.
- **Findings / decisions:** (1) **Not one of NJ's 2,181 tracts reaches the high-reliability tier for poverty** (income: 44% do; 92% of tracts are below CV 0.30 for income vs. 21% for poverty); (2) **poverty-data reliability maps as the inverse of poverty itself** — Spearman −0.58 between poverty rate and poverty CV; median CV 0.31 in the poorest quartile of tracts vs. 0.49 in the least-poor quartile. Urban cores (Newark, Camden, Trenton, Atlantic City) are the *most* reliable places for poverty data; affluent/suburban tracts the least — an equity-relevant blind spot for anyone tracking suburbanizing poverty. (3) Classed tiers + explicit gray class communicate at a glance; carried forward as dashboard design seeds. Phase 1 Step 5's "done when" (notebooks committed, 2–3 mentor-ready charts) is now satisfied.
- **Files:** notebooks/03-cv-choropleth-nj-tracts.ipynb, docs/glossary.md (choropleth entry), README.md (Step 5 checkbox); map at data/processed/eda03_cv_choropleth_income_poverty.png (local-only, regenerable)

### 2026-07-15 — Andrew Swiniarski — EDA 02: CV by variable type + first derived estimate (Phase 1, Step 5)
- **Area:** notebooks / analysis
- **What was done:** Second EDA notebook, [notebooks/02-cv-by-variable-type.ipynb](notebooks/02-cv-by-variable-type.ipynb): at tract level, compares CV distributions across variable types, builds the project's first **derived estimate** (Black 65+ = six sex×age cells summed, MOE via the Census handbook's root-sum-of-squares formula with the zero-cell rule, sensitivity vs. plain RSS shown), and tests the mechanism with a CV-vs-estimate-size log-log scatter against the theoretical −½ sampling-noise slope. Extended [analysis/acs.py](analysis/acs.py) with `aggregate_estimate` and a zero-rule `aggregate_moe`. Verified top-to-bottom with `jupyter nbconvert --execute`.
- **Findings / decisions:** (1) **The sampling-noise law holds for size-driven counts** — fitted log-log slopes: population −0.47, Black 65+ aggregate −0.51, vs. theoretical −0.50; (2) **poverty defies it (slope −0.18)** — bigger poverty counts mean poorer tracts, not bigger samples, so its reliability is capped by prevalence (explains EDA 01's "80% of tracts unreliable" shocker); (3) **aggregation helps but doesn't rescue subgroups**: median CV 0.886 → 0.631 (1.4×) and coverage 11–50% → 73% of tracts, yet 87% of nonzero tracts stay above CV 0.30; (4) the handbook zero-cell rule trims a median 10% (max 55%) off plain-RSS MOEs — adopted as default, documented in glossary; (5) **surprise, unexplained: 131 tracts (all 21 counties) publish population MOEs of ±14–143 vs. median ±546** — near-controlled precision with a published MOE; cause unknown, logged as a mentor question rather than guessed at. Composite-score implications: estimate magnitude is a strong free predictor for counts, but variable type must be its own component.
- **Files:** analysis/acs.py, notebooks/02-cv-by-variable-type.ipynb, docs/glossary.md (derived estimate, MOE aggregation entries), README.md (Step 5 checkbox, new mentor question); charts at data/processed/eda02_*.png (local-only, regenerable)

### 2026-07-15 — Andrew Swiniarski — EDA 01: CV by geography size (Phase 1, Step 5)
- **Area:** notebooks / analysis
- **What was done:** First EDA notebook, [notebooks/01-cv-by-geography-size.ipynb](notebooks/01-cv-by-geography-size.ipynb): runs a data-quality panel on the NJ pull, computes coefficients of variation for every variable at county / tract / block group, and produces the headline mentor chart (income CV by geography level, log scale, with ESRI/NCHS reference lines shown descriptively). Shared formulas graduated to a new reusable module, [analysis/acs.py](analysis/acs.py) (CV, top-code flag, root-sum-of-squares MOE aggregation — each with its Census citation), so every later notebook computes them identically. Verified top-to-bottom with `jupyter nbconvert --execute`.
- **Findings / decisions:** Central empirical fact confirmed: median income CV **0.014 (county) → 0.128 (tract) → 0.204 (block group)**; worst-case income MOE ±$3,982 → ±$247,531. Surprises worth flagging: (1) **tract-level poverty exceeds CV 0.30 in 80% of NJ tracts** — a policy-critical variable is low-reliability almost everywhere planners would use it; (2) Black 65+ single cells are effectively noise at tract level (median CV ≈ 0.9) and are zero in 50–89% of tracts — supports aggregating the six cells in EDA 02; (3) every top-coded income (41 tracts, 270 block groups) ships with **no MOE at all**, and 2 non-top-coded block groups also lack income MOEs (unexplained annotation — logged for the data dictionary); (4) county population MOEs are all blank because the estimates are *controlled* (very reliable) — the "two opposite meanings of a missing MOE" landmine seen live. Decisions per the approved EDA plan: zero estimates → CV undefined; top-coded incomes excluded from CV distributions; thresholds 0.12/0.30/0.40 cited descriptively only (our tiers = later mentor decision).
- **Files:** analysis/acs.py, notebooks/01-cv-by-geography-size.ipynb, docs/glossary.md (reliability-threshold conventions entry), README.md (Step 5 checkbox); chart at data/processed/eda01_income_cv_by_geography.png (local-only, regenerable)
### 2026-07-14 — Katie Christiansen — US boundary pull & exploration
- **Area:** ingestion
- **What was done:** Wrote and ran [ingestion/pull_acs_us.py](ingestion/pull_acs_us.py) — pulls 2024 ACS 5-year estimates and matching cartographic boundaries for all US states and counties. The script saves separate ACS Parquet and GeoParquet files, verifies a 1:1 join on Census geography IDs, and renders a nationwide county verification map. Added [ingestion/Explore.py](ingestion/Explore.py) to join the national county data to its geometry and save a median-household-income map. This is very similar to Andrew's NJ analysis, but at a US level. 
- **Findings / decisions:** National analysis is limited to state and county levels for now because nationwide tract and block-group geometry is substantially larger and slower to manage. Estimates include 90% margin-of-error columns.
- **Files:** ingestion/pull_acs_us.py; Explore.py; outputs `data/raw/acs5_2024_us_*.parquet`, `data/raw/geo_2024_us_*.parquet`, `data/raw/verification_map_us_counties.png`, and `data/raw/us_county_median_household_income.png` (local only, regenerable). I uploaded these files manually for now.

### 2026-07-12 — Andrew Swiniarski — Team sync recap deck; work log started
- **Area:** project management / docs
- **What was done:** Built the recap deck for the July 12 team sync ([docs/team-recap-2026-07-12.pptx](docs/team-recap-2026-07-12.pptx)) covering setup status, the NJ pilot dataset, the headline finding (uncertainty explodes at small geographies), documented data landmines, and the 10-minute teammate onboarding path. Created this work log; teammates begin working in the repo from this point.
- **Findings / decisions:** First mentor meeting set for **July 22, 2026** — we bring a one-slide status and the CV-by-geography chart. Open items from the sync: workstream assignments (analysis / dashboard / methodology write-up / data hunting), repo-review feedback, confirming the recurring biweekly slot.
- **Files:** docs/team-recap-2026-07-12.pptx, WORKLOG.md

### 2026-07-09 — Andrew Swiniarski — NJ boundary pull + 1:1 join verification (Phase 1, Step 5)
- **Area:** ingestion
- **What was done:** Wrote and ran [ingestion/pull_nj_geometry.py](ingestion/pull_nj_geometry.py) — pulls NJ county/tract/block group cartographic boundaries (vintage-matched to the 2024 ACS data), saves GeoParquet to `data/raw/`, verifies every data row joins to exactly one shape at all three levels, and renders a verification map of all 2,181 tracts.
- **Findings / decisions:** Join checks passed 1:1 at every level. **6 invalid geometries at tract level and 6 at block group** (self-intersection quirks) — harmless for choropleths, but `make_valid` must run in the future processing layer before any area/overlay math.
- **Files:** ingestion/pull_nj_geometry.py (commit e7895a5); outputs `data/raw/geo_2024_nj_*.parquet` + `verification_map_nj_tracts.png` (local only, regenerable)

### 2026-07-09 — Andrew Swiniarski — ACS 5-year NJ data pull with MOEs (Phase 1, Step 4)
- **Area:** ingestion
- **What was done:** Wrote and ran [ingestion/pull_acs_nj.py](ingestion/pull_acs_nj.py) — ACS 5-year vintage 2024, New Jersey, at county / tract / block group: total population, median household income, poverty count, and six Black 65+ subgroup cells, each with its margin of error. Verifies official variable labels at runtime, hard-checks the 21-county count, and prints a per-column sanity report. Row counts: 21 counties / 2,181 tracts / 6,599 block groups.
- **Findings / decisions:** Three data landmines found and documented: (1) poverty (B17001) and race×age (B01001B) tables are **not published below tract level** — tract confirmed as the floor, mentor question logged; (2) **censusdis converts ACS annotation codes to NaN**, collapsing "controlled/very reliable" and "insufficient sample/unreliable" into identical blanks; (3) **median income is top-coded at $250,001**. Decision: `data/raw/` stays exactly as the API returned it; all cleaning belongs in a future processing layer.
- **Files:** ingestion/pull_acs_nj.py (commit 3e0cb91); outputs `data/raw/acs5_2024_nj_*.parquet` (local only, regenerable); landmines detailed in [HANDOFF.md](HANDOFF.md)

### 2026-07-09 — Andrew Swiniarski — Repo, environment, and secrets setup (Phase 1, Steps 1–3)
- **Area:** infrastructure
- **What was done:** Initialized the repo and pushed to GitHub; scaffolded the folder structure (`/ingestion`, `/notebooks`, `/analysis`, `/docs`, `/data`) with per-folder READMEs; set up `.gitignore` (raw data + secrets excluded), the `.env` API-key pattern with a committed `.env.example`, `requirements.txt`, and a Python 3.12 virtual environment. Census API key verified with a live call. Added the "Getting Started" quickstart to the README and seeded [docs/glossary.md](docs/glossary.md) (18 terms).
- **Findings / decisions:** Git history was **rewritten on 2026-07-09** to purge sponsor contact details (the repo is public) — commit SHAs from before that date are invalid, and stale clones must not be pushed from. Never re-add sponsor contact info to committed files. Session handoff notes written to [HANDOFF.md](HANDOFF.md).
- **Files:** commits 9d11afb → bcc1c5c; requirements.txt, .env.example, .gitignore, HANDOFF.md, docs/glossary.md
